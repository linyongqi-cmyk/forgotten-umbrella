# 投稿导入系统 使用说明（Google 表单 → 本地记录）

> 这套东西干嘛的：把你 Google 表单收到的投稿（投稿者填的信息 + 上传的照片）
> **一条命令**拉到本地，自动生成 `submission(pending)` 里的记录草稿——信息都预填进
> 编辑器对应的框，你打开编辑器只需**核对 + 补坐标 + 补伞的细节**。已经导入过的
> 投稿会自动跳过，不会重复。全程只读你的 Google，绝不改动/删除你 Google 里的东西。

---

## 一次性准备（阶段 0，大概 10-15 分钟，只做一次）

### 第 1 步：把表单回答连到一张 Google 表格
1. 打开你的 Google 表单 → 上方「回答 / Responses」标签。
2. 点右上角绿色的**表格图标**「关联到试算表 / Link to Sheets」，建一张回答表。
3. 打开那张表，看浏览器地址栏，形如：
   `https://docs.google.com/spreadsheets/d/`**`这一长串就是表格ID`**`/edit`
   把中间那一长串 **表格 ID** 复制下来，等下要用。

### 第 2 步：在 Google Cloud 开通读取权限（拿密钥）
> 这是让本机脚本有资格读你表格和云端硬盘照片的钥匙。

1. 打开 https://console.cloud.google.com/ ，用**表单所属的那个 Google 账号**登录。
2. 顶部建一个项目：「选择项目 → 新建项目」，名字随便（如 `forgotten-umbrella`）→ 建立。
3. 开通两个 API（左侧菜单「API 和服务 → 程式库 / Library」里搜索）：
   - 搜 **Google Sheets API** → 点进去 → 启用。
   - 搜 **Google Drive API** → 点进去 → 启用。
4. 设定「OAuth 同意画面 / OAuth consent screen」：
   - 选 **外部 / External** → 建立。
   - 填 App 名称（随便）、你的邮箱做支援邮箱和开发者邮箱 → 一路保存。
   - 在「测试使用者 / Test users」里**把你自己的邮箱加进去**。
   - 发布状态保持「测试 / Testing」就行（只有你自己能用，正好）。
5. 建密钥：「凭证 / Credentials → 建立凭证 → OAuth 客户端 ID」：
   - 应用程式类型选 **桌面应用 / Desktop app** → 名字随便 → 建立。
   - 弹窗里点**下载 JSON**。
6. 把下载的那个 JSON 文件**改名为 `google-credentials.json`**，放进项目的 `scripts/` 文件夹里。

### 第 3 步：写一个小配置文件
在 `scripts/` 文件夹里新建 `submissions.config.json`，内容（把表格 ID 换成第 1 步复制的）：
```json
{ "sheetId": "这里粘第1步复制的表格ID", "sheetRange": "A1:ZZ100000" }
```

### 第 4 步：第一次登录
在项目根目录终端跑：
```
npm run submissions:auth
```
会自动弹出浏览器让你点「继续 / 同意」（测试阶段可能提示「未验证」，点「进阶 → 前往…（不安全）」继续即可，因为这是你自己的应用）。
成功后终端会说「授权 OK」，并把登录令牌存起来，以后不用再登。

---

## 平时怎么用（阶段 0 之后，每次收投稿）

**先试跑**（只看会导入哪些、不写任何文件）：
```
npm run submissions:pull -- --dry-run
```
确认没问题后**正式导入**：
```
npm run submissions:pull
```
它会：读表格新回答 → 下载照片到 `filebox/records/submission(pending)/form_日期_序号/` →
生成 record.json（署名/时间/地点/内容段落都预填好）→ 重建 `data/umbrellas.json`。

然后打开本地编辑器（`npm start`），逐条核对：**补坐标、补伞的颜色/把数、调地址层级**，就完成了。

---

## 几个要知道的点

- **命名**：导入的记录文件夹叫 `form_20260704_1` 这种（日期+序号）。你可以在编辑器用「显示名」改成对外好看的名字，不影响文件夹。
- **投稿者写的文字**（观察メモ/环境/追加メモ/天气记忆）会预填进「**内容的段落**」，你在编辑器里能直接改/删/排序。
- **HEIC 照片**：iPhone 传的 HEIC 会照样下载保留，但浏览器打不开，所以前端会显示「待手动转换」提醒（不显示裂图）。你自己把它转成 jpg、在编辑器替换掉就正常了。
- **不会重复导入**：已经导入过的投稿用「主图的云端文件 id」记在 `scripts/.submissions-state.json` 里，下次自动跳过。
- **坐标/天气**：表单没问坐标，所以导入时没有坐标、也没抓天气。你在编辑器把坐标点好之后，再点主图「获取天气」即可。
- **令牌过期**：测试阶段的登录令牌大约 **7 天**会过期。如果哪天 `submissions:pull` 报授权错误，重跑一次 `npm run submissions:auth` 重新登录就好。

---

## 私密文件（都已在 .gitignore，不会上传到仓库）
- `scripts/google-credentials.json`：Google 密钥
- `scripts/.google-token.json`：登录令牌
- `scripts/submissions.config.json`：你的表格 ID
- `scripts/.submissions-state.json`：已导入记录（去重用）
