# 翻译流程 / Translation SOP

> 用户说「帮我翻译…」或「按翻译流程翻译」时读这个。**先看「翻译规则」（永远适用），再按量选路径：
> 几处→直接改；很多处→走批量流程。** 全程大白话中文，改真实数据前有疑问就问。

## 能翻的地方（不只记录字段）
- **记录**：`filebox/records/**/record.json`（和 `filebox/hidden/**`）里的 `title`{ja,en}、`locationText`（显示地址，翻英文写回原字段）、`blocks[].text`{ja,en}（段落/对话）。
- **UI 文案**：`data/texts.json`（9 段类型说明 + 统计页说明，双语 ja/en）——直接改就是源，**不用 build**。
- **其它**：用户在页面上指哪段文字，就按同样规则翻。

## 翻译规则（最重要，永远适用）
- **方向**：中文 → 补齐**日语 + 英语**；日语本身没问题、只缺英文 → 只补英文。
- **别覆盖用户写的正规日语**：全汉字 ≠ 中文（`閉店清掃`/`初回記録`/`判断不能` 都是日语）。拿不准就问。
- **地址**：格式 `[具体位置], [区/Ward], [都府县]`，跟这条记录的 `locationLevels` 罗马音层级走、省略「City」那级（和现有 "…, Chuo Ward, Chiba" 一致）。专有名词（神社/车站/街名/店名）**不确定就联网查官方罗马字**（例：伏見稲荷大社=Fushimi Inari Taisha、上鳥羽口駅=Kamitobaguchi Station；7-11=7-Eleven、全家=FamilyMart、ローソン=Lawson）。
- **对话**：保留说话人前缀，ja「投稿者：/編者：」↔ en `Sender:` / `Editor:`；换行 `\n` 保留。
- **缺字别乱补**：PDF 有时漏了字（不是空格，如 `え込み`→应是`植え込み`）——列出来问用户，别自己猜着补。
- 翻完把 `@@` 记号去掉。术语：被遗忘的伞 = forgotten umbrella；其它固定译法拿不准就问。

---

## 路径 A：少量修改（几处）——直接改，别搞复杂
1. 用户指出哪几处（或我扫一下 `@@` 找到那几处）。
2. 直接改对应字段（`title.ja/en`、`locationText`、`blocks[i].text.ja/en`、或 `data/texts.json`…）。
3. 改了**段落/对话块**就顺手重算该记录 `story` = 各 text/dialogue 块 ja 用 `\n` 拼起来。
4. 改的是**记录**就 `npm run records:build`（绝对路径 node，见文末）；改 `data/texts.json` 不用。
5. 给一句简短前后对照。**不需要样本确认、不需要脚本、不需要长篇。**

## 路径 B：大批量（很多处 / 全库扫）——才走这套
1. **普查侦测**待翻内容，三个信号：① 含 `@@`；② ja 含汉字但**无假名**=疑似中文（⚠️会误报全汉字日语，逐条判）；③ ja 有内容但 en 空=缺英文。出一份「待翻清单」自己核。
2. **样本先确认**：先翻 3~5 条代表（含地址/标题/段落各一）给用户看格式和语气，点头再批量。
3. **安全写回**：翻译做成映射（`{id,field,ja?,en?}`）→ 写回前先验证序列化器 `stringifyRecordWithComments` **无损往返**（读→序列化→再读不丢字段）→ 先 `--dry-run` 看「旧→新」对照 → 确认后写回 → 改了块的重算 `story` → 翻译里不带 `@@`。
4. `npm run records:build` 重建 → **复扫确认 `@@` 清零**。
5. 未经用户同意不 commit/push。

---

## 技术要点 / 坑（需要时看）
- 写回记录必须用 `scripts/record-utils.mjs` 的 `stringifyRecordWithComments`（带注释）；它是**固定字段白名单**，别往记录塞新字段（会被丢）。
- 「文件名/文件夹」用**文件夹名** `path.basename(path.dirname(file))`，别用 `media[0].id`（可能是插图）。
- 跑脚本用绝对路径 node：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`。
- 日语「显示地点」暂不做（见 `未来可能需要注意的事.md`）。
- 顺带：PDF 提取的日文常夹换行残留空格——只删「至少一边挨着假名/汉字/日文标点」的半角空格，英文/数字间空格和换行保留，英文正文不动。
