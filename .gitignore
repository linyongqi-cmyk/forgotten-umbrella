server.err.log
server.out.log
*.log
.DS_Store
Thumbs.db
node_modules/
.env
.env.*

# Legacy pre-migration image source (~400MB). Superseded by filebox/records/.
# Keep on disk for reference, but never commit it to the repo.
filebox/choice/

# 「修改记录」软删除回收站（删除的标点先移到这里，可撤回恢复）。本地临时，不进仓库。
filebox/.trash/

# Office 临时锁文件（打开 xlsx/docx 时生成）
~$*

# Google 投稿导入的私密文件（绝不进仓库）：
#  - google-credentials.json：从 Google Cloud 下载的 OAuth 客户端密钥
#  - .google-token.json：首次授权后保存的登录令牌
#  - .submissions-state.json：已导入投稿的记录（去重用）
#  - submissions.config.json：你的表格 ID 等配置
scripts/google-credentials.json
scripts/.google-token.json
scripts/.submissions-state.json
scripts/submissions.config.json
