# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-03，前端 v131**。
> **本次任务完成智能体：Claude Code。以后每次更新本文件都必须写明完成任务的智能体（codex / Claude Code 等）。**

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **95 条记录**（真实数据，**绝不能乱删**）。本轮我把 91 个 record.json 重新序列化（加了天气字段+统一格式，用户原有编辑都原样保留）。仍有一批用户在补的投稿伞 `record.json`。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，4173 被占用时不要换随机端口看地图，先释放 4173。
- 本地浏览器可验证布局：注意**首屏是欢迎页**，`app-shell` 在 `body.is-entered` 前是 `visibility:hidden`——预览里要先 `document.body.classList.add('is-entered')` 才看得到内容；点标点没有全局函数，可临时在 `selectUmbrella` 处挂 `window.__fu` 调试钩子（记得提交前删）。

## ✅ 最近做完（v131 天气图例优化 + v130 🅐🅑🅒，均 Claude Code）
- **v131（本轮）天气图例优化**（用户看 IMG_8680 24h一直下雨图例一大排太丑）：① **夜间版(月亮)只在 now 出现**——变化段划分改用不带昼夜的 `weatherCategory`，晴/多云间晴的月亮版只在渲染 now 时按拍摄钟点单独判；② **中间图例最多 4 个(总≤6)**——`reduceWeatherPoints` 反复删「持续最短」的中间段，头尾不动；③ **取消小雨，归入中雨**（原 51/53/61/80 → rain 斜线2条），图标 11→10 种。全量 83 主图：中间>4 的 0 条、相邻重复 0 条。
- **v130 🅒 天气图例**：合并算法按用户「问题X」方案=**不丢点、只把挨太近的整体推开**（相邻永不重复）。
- **v130 🅐 短内容整体居中（方案A）**：`is-focus-short` 类 + flex auto 外边距把「内容+Back to map」整组居中；长内容 footer 固定底、内部滚。
- **v130 🅑 放大淡入淡出**：进入 opacity 0→1；退出先播 200ms 淡出(`.is-collapsing`)再收起。
- **未验证风险**：天气横轴在真实详情页的**最终视觉** + 放大的**淡入淡出/FLIP 切换动画**，因无头地图投影未就绪只验证到逻辑/类/时序层，**需真机再确认一眼**。

## ⏳ 待办（历史遗留，与本轮无关）

<details><summary>（已完成，留档）原 🅐🅑🅒 详细方案</summary>

### 🅐 详情页短内容居中（方案A）—— **本会话进行中，用户已定方案A**
问题：短内容（如 IMG_8508/IMG_8632）现在**顶到面板最上面**，下方一大片空白，不居中。
根因：v127 的居中靠 `.focus-scroll{flex:1}` 撑满固定高度面板 + 里面 `.focus-stage{margin:auto 0}` 居中（**居中要滚动区有多余空间**）。v129 为了让「Back to map 贴内容下」把 `.focus-scroll` 改成 `flex:0 1 auto`（滚动区缩到内容高）→ margin:auto 没空间可分 → 居中失效顶到顶。两个诉求（居中 vs footer 贴内容）纯 CSS 相互冲突。
**方案A（要 JS 判断是否溢出，已有 `updateFocusScrollHint` 在测溢出可复用）**：
- **短内容（不溢出）**：把「内容 `.focus-stage` + Back to map(`.focus-footer`)」当**一个整体**在面板里**垂直居中**，footer 自然贴在内容正下方。
- **长内容（溢出）**：内容顶端对齐 + 内部滚动，footer **固定面板底部不随滚动**。
- 实现建议：给面板加个 class（如 `is-focus-overflow`）由 JS 按 `scroll.scrollHeight>scroll.clientHeight` 切换；不溢出时 footer 进 `.focus-scroll` 内、和 stage 一组用 margin:auto 居中；溢出时 footer 回到滚动区外固定底部。或等价的 flex 方案。关键：**short=居中且footer贴内容；long=顶端滚动且footer固定底不滚**。改完两种记录都验。文件：`styles.css`(`.focus-scroll`/`.focus-stage`/`.focus-footer`)、`app.js`(`updateFocusScrollHint` 附近加溢出 class 切换)。

### 🅑 放大进入/退出加淡入淡出 —— **本会话进行中**
现状：进入放大只有 `focus-expand-pop`（opacity 0.5→1，200ms，且尺寸瞬间切=看不出淡入）；**退出完全没淡出**（`closeExpandedImage` 直接摘 `.is-expanded` 类、瞬间收起）。
要做：进入 opacity 0→1 完整淡入；**退出要先播淡出再收起**（难点：现在立刻收起没给动画时间）——在 `closeExpandedImage`(app.js~4430) 里改成先加「正在退出」态 opacity→0、`transitionend` 后再真正清理，或用退出 class。文件：`app.js`(`expandImageAt`/`closeExpandedImage`)、`styles.css`(`.is-expanded`/新增退出态)。

### 🅒 天气图例规则+图标重做 —— **交给新窗口做（用户已讨论确认，见下）**
**两个 bug（已用真实数据验证 IMG_8632、IMG_8508）：**
- **问题X（合并算法 bug，违反「只显示变化才画图例」）**：`renderFocusWeatherAxis`(app.js~2626) 的最小间距合并是靠**丢点**——两图例<13% 就丢一个，但**只丢、不检查丢完后相邻两点是否同一天气**。结果丢掉中间短暂变化、留下它前后两个相同类=相邻重复。实测 8632 显示「阴@6 阴@21 晴@94」(两个都 code3)；8508 显示「阴 雨(51) 雨(51) 雨(53) 雨(63)」(4个雨同图标)。
- **问题Y（图标太粗）**：`weatherCategory`(app.js~2552) 只 7 类，所有雨强度(51/53/61/63/65/80-82)全归 `rain` 一个图标，雪同理。大雨小雨画的一样。

**修法（核心原则：判断变化的粒度 = 图标的粒度，1:1）：**
1. **重写合并算法（修X）**：不再盲目丢点。改成「**反复找持续时间最短的中间那段天气→并进相邻→若并完相邻两段变成同一类就合并成一段→重复**，直到都放得下(间距≥MIN_GAP)或只剩两端」。保证：**相邻图例永远不同类**、保留持续最久(最有代表性)的天气、两端(-24h/now)始终保留。（`weatherChangePoints` 产出的 runs 相邻本就不同类，问题全在后面的 min-gap 丢点，重写那段即可。）
2. **图标做细（修Y）**：把 `weatherCategory` 扩成下面 11 类、`weatherIconSvg`(app.js~2569) 每类画一个极简线条 SVG（viewBox 0 0 24 24、stroke=currentColor、无填充、共用云路径+不同雨滴/雪花/闪电）。**参考图3 的视觉语汇但自己描，别直接抄**（版权）。`singleWeatherIconFor`(补充/细节单图例)会自动受益，不用另改。

  WMO code → 11 图标映射：
  | 图标 | WMO code |
  |---|---|
  | 晴 clear | 0 |
  | 多云间晴 partly | 1,2 |
  | 阴 cloudy | 3 |
  | 雾 fog | 45,48 |
  | 小雨 light-rain | 51,53,61,80 |
  | 中雨 rain | 55,63,81 |
  | 大雨 heavy-rain | 65,82 |
  | 雨夹雪 sleet | 56,57,66,67 |
  | 小雪 light-snow | 71,77,85 |
  | 中大雪 snow | 73,75,86 |
  | 雷雨 thunder | 95,96,99 |

  **两个待用户最终确认的点**（用户倾向已写默认）：① 白天/夜晚是否分（晴↔月亮）——**默认不做**，只做这 11 个；② 24h 完全没变时——**默认 -24h/now 两端各画一个**（一样也行=表达一直没变）。

  **验证脚本**（新窗口改完用它跑，确认没有相邻重复、强度能区分）：读 `data/umbrellas.json`，对 IMG_8632/IMG_8508 跑改后的 `weatherCategory`+合并算法，打印最终图例序列——8632 期望类似「阴→晴」不再阴-阴；8508 期望能看出雨的强度变化或阴/雨交替、无相邻同图标。
  **版本号**：改完前端记得四处 +1（当前 v129→v130）。

</details>

### 其他既有待办
1. **12 个投稿伞没抓到天气**：因为它们的时间**只有日期没有钟点**（如「2024.10」「2025.04.27」），抓逐时天气需要具体到小时。等用户把这些点的时间补到「年月日 时:分」后，在编辑器点它主图的「获取天气」即可。
2. **v129 真机复查**（放大切换动画/天气横轴在图上65%左对齐/-24h·now对齐端点图例/补充细节图详情页显天气/Back to map 短贴内容长固定底）——注意 🅐🅑🅒 会再改这几处。
3. **Open-Meteo 档案对太近日期有几天延迟**：真机若某新标点抓不到，多半是日期太近，等几天或改用 forecast 接口的 `past_days`。
4. **地图筛选默认值可固化**（`#map-layers`）：普通/卫星1/卫星2 各一套；真机调好后把值写进默认。
5. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
6. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664（remarks 有记）。
7. **投稿二期（Google 表单自动同步）未做**；**视频裁剪未做**。

## 关键子系统（速查）
- **详情面板排布（v127 推倒重写，v129 微调）**：`.focus-image-panel` 是**固定高宽的框**（`height:calc(100vh - --focus-top - --focus-bottom)`，`overflow:hidden`）；里面 `.focus-scroll`(flex 纵向滚动)+`.focus-stage`。**v129：`.focus-scroll` 改 `flex:0 1 auto`**——短内容时滚动区只占内容高、footer(Back to map)贴内容正下方；长文章时压到剩余高、内部滚、footer 固定面板底部不滚。放大(灯箱)`.is-expanded` 用 `inset:0+margin:auto` 居中；**v129：切换图片的 FLIP 缩放动画 `flipExpandedPanel` 去掉了旧的 `translate(-50%,-50%)`、改纯 `scale()`**（修「切换不同比例图先跳左上再缩回」的怪动画）。窄屏(≤820px)仍是底部抽屉。
- **天气联动（v128 按每张图存，v129 横轴挪回图上）**：数据在 `media[].weather`（+`media[].showWeather`）。主图=拍摄前 24h 逐时（画横轴）；补充/细节=只拍摄当时 1 点（单个图例）。**v129 展示位置**：主图横轴由 `renderFocusInfo` 直接拼在**覆盖信息块 `.focus-overlay-info` 底部**（图片之上，place 行下面），不再有独立 `#focus-weather` 元素——因此放大灯箱里随整块覆盖信息 `display:none` 一起隐藏（=放大页不显示天气）。补充/细节图的单个图例现在**详情页图注**(`renderFocusBlockHtml`)和**放大灯箱角标题**里都显示。抓取：编辑器每张图「获取天气」→`/api/fetch-weather`（带 `mediaId`，`editor-api.mjs::fetchWeather`）。**weather+showWeather 有 4 处会被重建丢掉**，都已补保留：`normalizeMedia`/`applyMediaMetadata`/`mergeRecordMediaWithFolder`/`build-umbrellas.mjs`。批量脚本 `scripts/fetch-all-weather.mjs`。`weather.mjs::fetchWeatherData(lat,lon,time,{hoursBefore})`，Open-Meteo 免费档案无 key。
- **横轴渲染（v131 改）**：`renderFocusWeatherAxis(weather)`。`weatherChangePoints` 用**不带昼夜的 `weatherCategory`** 划变化段+头尾（末尾合并连续同类防相邻重复）→ `reduceWeatherPoints` 把**中间图例删到 ≤4 个**（删「持续最短」的中间段、头尾不动、删完再 collapse 同类）→ **不丢点、只把挨太近(<13%)的整体推开**留最小间距（right→now 钉真实位、极端才退化等距）→ 线段只画在图例空隙实现遮挡。**夜间版(月亮)只在 now 那一点渲染**：marks 里最后一点用 `weatherCategoryAt(hourly[last].code, hourly[last].time)`（`isNightHour` 18:00–翌6:00 近似），其余点用白天版。**图标 10 种**（v131 取消小雨后）：`weatherCategory`=clear/partly/cloudy/rain(中雨,含原小雨)/heavy-rain/thunder/light-snow/snow(8 类)+clear-night/partly-night 两个 now 专用夜版；`weatherIconSvg` 极简线条（中雨=斜线2、大雨=斜线3、暴雨+闪电、小雪云+雪花、大雪只雪花、夜=月亮）。`singleWeatherIconFor`(补充/细节单图例=拍摄当时=now)仍用 `weatherCategoryAt`，夜里出月亮。**v129：横轴宽度=文字栏 65% 左对齐；两端标签 `-24h`/`now` 用 `left:pos[0]%`/`pos[末]%`+`translateX(-50%)` 各自对齐首/尾图例正下方**（不再 space-between 贴容器两端）。`singleWeatherIconFor(media)`=补充/细节单图例。CSS `.focus-weather`(width:65%)、`.fw-track/.fw-seg/.fw-mark/.fw-icon/.fw-axis`、`.focus-photo figcaption .fw-single`+`.focus-expanded-caption .fw-single`。
- **聚焦模糊 / 模糊度调整（v123）**：`#blur-adjust` 打开即实时预览；参数存 localStorage `fu-blur-settings`，CSS 变量 `:root --fb-*-n/-a`。
- **地图标点**（`renderMapMarkers`/`markerZIndex`）：原地更新 + distinct zIndex + `optimized:true`，不要整批销毁重建。
- **编辑器三栏 / 修改记录**：预览栏+修改记录栏+编辑栏绑定，有标点打开才一起出现；三条 `.panel-resizer` 可拖大小，存 `fu-panel-sizes`。
- **图片裁剪**：`media[].crop` 非破坏性；封面+灯箱共用 `#focus-image`。

## 最近 5 轮
- **v131（本轮，Claude Code）**：天气图例三优化。① 夜版(月亮)只在 now 出现（变化段用基础分类 `weatherCategory` 划、now 单独判昼夜）；② 中间图例最多 4 个（`reduceWeatherPoints` 删最短中间段，头尾不动）；③ 取消小雨归入中雨（斜线2条），图标 11→10。验证：IMG_8680 缩到 5 个、全量 83 主图中间>4 与相邻重复都 0、夜版 7 条抽查只在 now 出月亮。真实详情页视觉真机再确认。
- **v130（Claude Code）**：🅐🅑🅒 三件。🅒 天气合并算法**不丢点只推开**（修相邻重复，全量 0 重复）+ 图标扩到 **11 种**（雨斜线 1/2/3、暴雨带闪电、小/大雪、雾并阴、晴/多云间晴分昼夜换月亮）。🅐 短内容用 `is-focus-short`+flex auto 外边距整组居中、长内容 footer 固定底。🅑 放大进入 opacity 0→1、退出先 `.is-collapsing` 淡出 200ms 再收起。本地无头验证：8508 图例无重复/间距够、短记居中(上下各 66px)、长记 footer 固定、退出时序正确、11 图标截图正确。**淡入淡出/FLIP 真机再确认**。
- **v129（Claude Code）**：详情页 6 项。① 放大切换怪动画修复=`flipExpandedPanel` 去掉多余 `translate(-50%,-50%)`、改纯 `scale()`（v127 已把居中改 inset+margin，那 translate 就多余了）。② 天气横轴从「主图正下方独立 `#focus-weather`」挪回**图片之上**（覆盖信息块底部，`renderFocusInfo` 拼），宽 65% 左对齐。③ 放大灯箱不再显示天气横轴（②带来的，随覆盖信息块隐藏）。④ 两端标签 `-24`→`-24h`、各自对齐首/尾图例正下方。⑤ 补充/细节图天气图例进详情页图注（之前只放大时有）。⑥ `.focus-scroll` 改 `flex:0 1 auto`=短内容 Back to map 贴图片下、长文章固定底部不滚。本地无头浏览器实测全部通过（FLIP 动画真机再瞄）。
- **v128（Claude Code）**：每张图加「☁获取天气」按钮+「显示天气」勾选框（按 mediaId 抓、主图24h/补充细节1点）；主图天气横轴重画=去温度、两端只 -24/now、中间只变化点图例、强制最小间距合并太频繁的变化、连线在图例处断开(遮挡)；补充/细节放大时角标题放单个当时图例；修 4 处会丢天气字段的地方。（v129 又把横轴挪回图上、标签改 -24h）
- **v128 数据（Claude Code）**：天气从 `record.weather` 改成**按 media 存**；`weather.mjs` 加 `hoursBefore`、时间解析放宽；`scripts/fetch-all-weather.mjs` 一次性迁移+抓所有主图（83 个，12 投稿伞只有日期跳过）；`createRecord` 自动抓主图天气。批量重写 91 个 record.json。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结。
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）。
6. **`交接.md` 每次重写只留最近 5 轮，并且必须署名本次任务由哪个智能体完成。**
7. 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
