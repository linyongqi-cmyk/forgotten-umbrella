# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-02，前端 v120**。

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **92 条记录**（真实数据，**绝不能乱删**）。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**（重要更正！）：Google Key 授权了 `127.0.0.1:4173`，所以 **preview_start 用默认 4173 端口时地图正常显示**（能看标点/卫星/模糊/缩放）。⚠️ 若 4173 被占用自动换了随机端口（如 62497）就会 RefererNotAllowed 看不到——那时先 `pkill -f "node server.js"` 释放 4173 再 preview_start。验证地图相关时可临时在 `init()` 末尾加 `window.__t={state,els,...}` 调试钩子（用完删掉、别提交）。

## ⏳ 待办
1. **地图筛选默认值可固化**（`#map-layers`，仅编辑模式）：现在**普通/卫星1(文字なし)/卫星2(文字あり)各一套**（v120），13 类每类一排单选「自動/表示/淡化/隠す」+缩放阈值，存 localStorage。真机调好后把想要的值告诉我写进代码做正式默认（普通=`mapStyles`；卫星 base=`SATELLITE_BASE_STYLES`全关文字；卫星1默认全 auto=干净；卫星2默认=`defaultSat2Set()` 开主要文字）。
2. **模糊标点视觉微调**（T2 已重构：聚焦模糊点时其他标点变模糊图标 `blurredMarkerIcon`，选中的清晰）：模糊程度在 `blurredMarkerIcon` 的 `stdDeviation`(现1.7)/`opacity`(现0.82) 调；模糊层蒙版仍是 `.focus-blur-approx-layer`。真机看看糊的程度是否合适。
3. **投稿伞坐标/地址层级多为大概或缺失**（很多没填 locationLevels→「場所」落到「海外」）：需逐个补。`rednote_kankan(2)` 投稿时间要手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。
5. **投稿二期（Google 表单自动同步）未做**：用户说先记住。方案见 memory。
6. **视频裁剪未做**（图片裁剪已含自由比例，内容图+封面+灯箱都生效）。如需可补。
7. **T1/T3 真机复查**：标点打开下跳(T1)、缩放闪烁(T3)已按逻辑修好（投影起点 / `optimized:false`），但属动画/时序类本地难截图确认，真机再确认一次最稳。

## 关键子系统（速查，细节看代码/修改记录）
- **投稿 vs 自拍**：record 有 `submissionType`(contributed/own)、`submitter`、`submissionTime`、`locationApprox/timeApprox` 等。透传链 `record-utils.mjs`→`build-umbrellas.mjs`→`editor-api.mjs`（改后端要杀 node）。
- **地图 4 色标记**（`markerCategory`/`MARKER_COLORS`）：深红=有标题自拍/红=普通自拍/深绿=有故事投稿/绿=普通投稿。标点 `optimized:false`（各自DOM，防缩放z序闪烁）。筛选 `#map-filter`（`state.markerFilter`，编辑模式也生效）。「待改」勾选：自拍点变黑/投稿变白。
- **模糊地址标点**（T2 v120）：投稿伞勾「模糊地址」聚焦时——底图模糊层 `.focus-blur-approx-layer`(OverlayView 注入 marker 下方 pane)+**其他标点换成模糊图标**（`blurredMarkerIcon`=data-URI SVG feGaussianBlur，白描边）、选中的仍清晰蓝。`blurApproxFocusId()` 判定；`updateMarkerIcons()` 应用；hover/rebuild/关闭都恢复（关闭时**先去 `is-blur-approx` 类再** `updateMarkerIcons`，否则会误糊）。
- **地图相机动画**（`animateMarkerToFocus`/`zoomToDefaultAroundMarker`/`recenterFocusedMarker`）：起点一律用投影 `getLatLngScreenPoint`（和 `getCenterForMarkerScreenPoint` 同一视觉中心偏移约定），**不要再用 DOM rect**（会让第一帧位置算错→标点下跳，已删 `getMarkerButtonScreenPoint`）。
- **详情页**（单栏）：A1(ID/标题) 浮主图左上、A2+C(地点/时间/信息，词条 grid 自适应列宽) 浮左下；下面 D1+D2 按 blocks 原序单流。段落/对话都有白色左竖线。关联链接=白字+链接 logo+下划线延到 logo。图片放大镜（补充/细节/封面/视频都有），点开进灯箱（图可平移缩放、视频原生播放）。视频=中间大播放键+默认静音、字幕在顶部。投稿伞隐藏 type/object/state + 补充/细节隐藏文件名。
- **档案 Archive 一页两态**（`.archive-scope-tab` own/contributed）：照片卡网格 + 交叉表/总览。卡片右下角角标（多图/插图/文本/视频）；✎ 开编辑、双击跳地图。About 页 4 数字框在右栏、凡例在其下。
- **编辑器**（仅 localhost，`setupEditor`/`openEditor`/`saveEditor`/`renderFlow`）：两列（字段+内容图文），左预览实时。内容块 text/dialogue/photo：段落&对话双语、都可 ▾ 折叠；对话「对话」标签在顶、每句左侧竖排工具栏(✕/↑/↓)。图片块=缩略图+角色下拉+**左下角裁剪钮**。CSS 坑：`display:flex/grid` 会盖 `[hidden]`，要配 `[hidden]{display:none}`。ESC 逐层退。
- **地址层级**（编辑器，`onLevel2/3Change`/`collectLevelsForSave`/`hydrateLevels`，数据 `data/japan-areas.json`）：都道府县→市→区级联下拉（datalist，焦点清空显全列表、blur 还原）。选府县**不自动填子级**（v119，以前会乱塞第一个市/区）；每级下拉末尾有「unknown」=该级未知（存字面 `"unknown"`，如 `["Tokyo","unknown"]`；lvl1 也有「未知 Unknown」=整条 `["unknown"]`）。存 romaji（`locationLevels`）。
- **地图信息筛选**（`#map-layers`，仅编辑模式，v120 重构）：**三套独立** `state.mapCategoryState={roadmap,sat1,sat2}`，`activeMapBaseKey()` 按 mapBase+mapLabels 选（roadmap / 卫星1文字なし / 卫星2文字あり）。卫星统一 `effectiveMapTypeId()="hybrid"`，`SATELLITE_BASE_STYLES` 一句关掉**所有**文字(`{elementType:"labels",visibility:"off"}`)+关道路线→干净不漏；`categoryStyleRules()` 按当前套叠加显示。弹窗每类一排 4 按钮单选(`.map-layer-seg`，`data-map-set/data-map-vis`)，标题显示在调哪套。存 localStorage `fu-map-layers`（旧格式自动迁移）。
- **图片裁剪**（v117 起，v120 加「自由」，非破坏性）：`media[].crop={aspect,scale,posX,posY,(ar)}`（null=原图）。`aspect`：`free`=原图(natural ar)/`custom`=**自由**(拖右下角把手，ar=box比例)/`1:1`等固定。`openCropModal`（比例/缩放/拖动/自由把手/保存/还原）。内容图用 `cropStyles()`；封面+灯箱共用 `#focus-image` 用 `applyFocusCrop()`+`cropAspectRatioNumber()`。透传含 editor-api `sanitizeCrop`（认 custom+ar）。
- **视频媒介**：`.mp4/.mov/.webm/.m4v`，`isVideoFile()`，视频不当封面；`server.js` 已补 MIME。

## 最近几轮（详见 修改记录.md）
- **v120**（一批11项）：T1 标点打开下跳→动画起点改投影(删 `getMarkerButtonScreenPoint`)；T2 模糊地址只选中清晰、其余标点变模糊图标；T3 缩放闪烁→`optimized:false`；T4 裁剪加「自由」比例(拖把手)；**T5/6/7 卫星地图信息重构**(统一 hybrid+base 关全部文字，卫星1/卫星2 各一套筛选，弹窗一排单选)；T9 档案折叠钮改纤细 chevron；T10 档案选中框红→白；T11 语言菜单加 ✓。
- **v119**：① 地图筛选拆普通/卫星两套（本轮 v120 再拆成三套）；② 打开详情页强制回顶显主图（`openFocusMode` scrollTo top）；③ 地址层级：选府县不自动填子级、unknown 可存/回填。
- **v118**：① 修视频放大后无法播放（`pauseFocusVideos` 只针对内页 `.focus-video`）；② 裁剪真正应用到封面+灯箱（`normalizeMedia` 补 `crop` 透传 + `applyFocusCrop`）。
- **v117**：图片裁剪功能（编辑器内容区，非破坏性，全链路透传+网站显示）。
- **v116**：灯箱视频缩略图修复；视频放大不先错位；关联下划线延到 logo；对话折叠+标签移顶；段落左竖线。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
