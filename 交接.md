# 交接 / Handoff（新窗口先读这个）

> 新会话先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。截至 **2026-06-27，前端版本 v92**。

## 当前状态
- 项目正常跑：`npm start` → http://127.0.0.1:4173/ 。改了 `record.json` 要 `npm run records:build`；改了 `scripts/editor-api.mjs` 要**杀 node 重启**预览（Mac：`pkill -f "node server.js"` 后再 preview_start，否则动态 import 有缓存）。
- **Mac 上 node 是绝对路径** `/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（`.claude/launch.json` 里；shell 里 `npm`/`node` 不在 PATH，要用绝对路径跑脚本）。
- 71 条记录是用户真实数据，**绝不能乱删**；改前先核对。
- **改前端就把版本号一起 +1**（`index.html` 两处、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。当前 **v92**。
- **本地预览看不到地图**：Google Maps API Key 限了域名，127.0.0.1 上地图不渲染。详情页/统计要靠预览 MCP 用 DOM 脚本测（`state`/`editor` 是模块变量，window 上取不到，只能查 DOM）；像素级视觉用户在自己浏览器看。

## 投稿系统（v92，本轮新做，**一期：编辑系统**）
> 背景：要接受外部投稿的伞。投稿伞**不是作者拍的、信息常不完善**（地点常只到城市、可能没时间/没伞属性、照片常无 EXIF）。用户已有现成 Google 表单（带文件上传题→强制 Google 登录）。优先级：先改编辑系统，作者**自己先把别人发来的投稿伞录进去**；Google 自动同步往后放。

- **record.json 新增 6 字段**（旧 71 条默认 own、完全兼容，build 用默认值兜底；没跑 `records:format` 重写全部文件）：
  - `submissionType`："contributed"=投稿 / "own"或空=自己拍的
  - `submitter`（投稿者署名）、`submissionChannel`（渠道/日期，仅内部）、`submitterNote`（投稿者原话）
  - `locationApprox` / `timeApprox`（布尔，地点/时间是否「大概」）
  - 透传链：`scripts/record-utils.mjs`（序列化注释）→ `scripts/build-umbrellas.mjs`（进 umbrellas.json）→ `scripts/editor-api.mjs`（save/create 消毒）。
- **编辑器**（`app.js`）：抽屉里「来源 Source」单选（自己拍的/投稿的），切「投稿」展开投稿字段块（`.editor-contrib`）；左侧「新增标点」旁加来源下拉（`editor.addSource`），投稿时新建默认 `locationApprox=timeApprox=true`。`getEditorSource()` 取值，openEditor 填充、saveEditor 入 payload。
- **详情页**（`renderFocusHeader`/`renderFocusArticle`）：投稿伞标题旁「投稿」徽章（`.focus-badge`）、`locationApprox`/`timeApprox` 给地点/时间加「約 / approx.」前缀、`submitter` 显示「投稿者：xxx」（`.focus-credit`）、`submitterNote` 作引用块（`.focus-note`）。文案在 `UI_TEXT`（contributedBadge/submitterCredit/approxPrefix）。
- **统计**（`renderStats`）：两套并列，顶部「自分で撮影／投稿」切换（`.stats-scope`，`state.statsScope`，点击委托在 archiveContent click handler）；`statsScopeItems(scope)` 按 submissionType 过滤，`buildStatsUnits(items)`/`renderStatsOverview(items)` 改成收 items 参数。About 页 `#stat-submissions`（投稿数）= contributed 计数，`#stat-count` 仍是总数。

## 待办：投稿系统二期（Google 表单自动同步，暂缓）
- 等编辑系统稳定、有真实投稿后再做。用户的 Google 表单带文件上传题→**外部读不到题目（需登录）**，做同步时直接看「回复汇总表」列名映射字段。
- 思路（计划文件 `~/.claude/plans/claude-md-scalable-riddle.md` 有详版）：网站加「投稿」入口嵌表单 → 本机脚本 `scripts/pull-submissions.mjs` 拉 Google 表格(CSV发布)+Drive(公开链接)、内置 fetch 零依赖 → 投稿先落 `submission(pending)` 待审核分类 → 作者本机审核(补双语/拖地图定位) → 现有「移动分类」转正 → push 上线。

## 最近几轮（详见 修改记录.md）
- **v92**：投稿系统一期（见上）。
- **2026-06-27 迁移到 Mac**：装 Node 24（`~/.local`）；修 launch.json node 路径；更新 CLAUDE.md 过期信息；删迁移手册。
- **2026-06-27 清理数据**：删 22 个孤儿缩略图 + 本地日志；IMG_8062 补 linkedId。
- **2026-06-27 修线上插图**：加 `.nojekyll`（`_` 开头文件被 Jekyll 忽略）。
- **第27轮 v88→v91**：详情页文字全强制白色（`.focus-image-panel *`，保留蓝色跳转链接）；About 统计三卡右移。

## 工作约定（详见 CLAUDE.md + memory）
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——合适检查点直接 `git commit`，预览链接需要时才给。
5. 改过文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main（单人不开分支）；**只有用户说「上线/同步」才 push**；push 前先 `git fetch` 看远端。
