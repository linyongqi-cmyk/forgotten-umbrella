# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-02，前端 v119**。

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **92 条记录**（真实数据，**绝不能乱删**）。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **本地看不到地图**（Google Key 限域名）：地图/标点/模糊效果**需真机看**；详情页/编辑器/档案用预览 MCP 的 DOM 脚本验证（`state`/`editor` 是模块变量，window 取不到，但 localhost 上编辑器可用，可从档案卡 ✎ 打开）。

## ⏳ 待办
1. **T7 模糊地址真机验证**：投稿伞勾「模糊地址」后聚焦用 OverlayView 把模糊层注入 marker 下面的 pane（`.focus-blur-approx-layer`：内圈 blur(1px)+外圈 `::after` blur(6px) 渐变蒙版无接缝；中心白雾 0.3、边缘黑 0.24；`draw()` 把 `--cx/--cy` 对准标点）。可调项都在该 CSS 类 + `--blur-inner/--blur-outer/--cr`。`rednote_621639248` 可看。**需真机确认视觉。**
2. **T8 地图图层调试面板**（`#map-layers`，仅编辑模式）：**普通/卫星各一套**（v119）13 类「自動/表示/淡化/隠す」+缩放阈值，存 localStorage。真机调好后把想要的值告诉我写进 `mapStyles`（普通默认）/`satelliteStylesForZoom`（卫星默认）。
3. **投稿伞坐标/地址层级多为大概或缺失**（很多没填 locationLevels→「場所」落到「海外」）：需真机逐个补。`rednote_kankan(2)` 投稿时间要手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。
5. **投稿二期（Google 表单自动同步）未做**：用户说先记住。方案见 memory。
6. **视频裁剪未做**（图片裁剪已全链路生效：内容图 + 封面 + 灯箱放大，v118）。如需可补。

## 关键子系统（速查，细节看代码/修改记录）
- **投稿 vs 自拍**：record 有 `submissionType`(contributed/own)、`submitter`、`submissionTime`、`locationApprox/timeApprox` 等。透传链 `record-utils.mjs`→`build-umbrellas.mjs`→`editor-api.mjs`（改后端要杀 node）。
- **地图 4 色标记**（`markerCategory`/`MARKER_COLORS`）：深红=有标题自拍/红=普通自拍/深绿=有故事投稿/绿=普通投稿。筛选 `#map-filter`（`state.markerFilter`，编辑模式也生效）。凡例/筛选标签双语按序（`MARKER_LABELS`+`markerLabel()`）。「待改」勾选：自拍点变黑/投稿变白。
- **详情页**（单栏）：A1(ID/标题) 浮主图左上、A2+C(地点/时间/信息，词条 grid 自适应列宽) 浮左下；下面 D1+D2 按 blocks 原序单流。段落/对话都有白色左竖线。关联链接=白字+链接 logo+下划线延到 logo。图片放大镜（补充/细节/封面/视频都有），点开进灯箱（图可平移缩放、视频原生播放）。视频=中间大播放键+默认静音、字幕在顶部。投稿伞隐藏 type/object/state + 补充/细节隐藏文件名。
- **档案 Archive 一页两态**（`.archive-scope-tab` own/contributed）：照片卡网格 + 交叉表/总览。卡片右下角角标（多图/插图/文本/视频）；✎ 开编辑、双击跳地图。About 页 4 数字框在右栏、凡例在其下。
- **编辑器**（仅 localhost，`setupEditor`/`openEditor`/`saveEditor`/`renderFlow`）：两列（字段+内容图文），左预览实时。内容块 text/dialogue/photo：段落&对话双语、都可 ▾ 折叠；对话「对话」标签在顶、每句左侧竖排工具栏(✕/↑/↓)。图片块=缩略图+角色下拉+**左下角裁剪钮**。CSS 坑：`display:flex/grid` 会盖 `[hidden]`，要配 `[hidden]{display:none}`。ESC 逐层退。
- **地址层级**（编辑器，`onLevel2/3Change`/`collectLevelsForSave`/`hydrateLevels`，数据 `data/japan-areas.json`）：都道府县→市→区级联下拉（datalist，焦点清空显全列表、blur 还原）。选府县**不自动填子级**（v119，以前会乱塞第一个市/区）；每级下拉末尾有「unknown」=该级未知（存字面 `"unknown"`，如 `["Tokyo","unknown"]`；lvl1 也有「未知 Unknown」=整条 `["unknown"]`）。存 romaji（`locationLevels`）。
- **图片裁剪**（v117 起，v118 全生效，非破坏性）：`media[].crop={aspect,scale,posX,posY,(ar)}`（null=原图），只影响网站显示不改文件。缩略图左下角 `.editor-media-crop` 钮→`openCropModal`（选比例/缩放/拖动/保存/还原）。内容图用 `cropStyles()`（包 `.media-crop` 盒）；**封面+灯箱共用的 `#focus-image` 用 `applyFocusCrop()`**（frame 变裁剪比例盒 + `object-fit:cover`+`object-position`+`--crop-scale`）。**坑**：`normalizeMedia` 曾漏透传 `crop`（前端读不到，v118 补上）。
- **视频媒介**：`.mp4/.mov/.webm/.m4v`，`isVideoFile()`，视频不当封面；`server.js` 已补 MIME。

## 最近几轮（详见 修改记录.md）
- **v119**：① 地图样式调试面板**拆普通/卫星两套**（`mapCategoryState={roadmap,satellite}`+`activeMapCategoryState()`，面板顶显示在调哪张，两套独立存 localStorage）；② 打开标点详情页**强制回顶显主图**（`openFocusMode` 里 `focusScroll.scrollTo(top:0)`，不沿用旧滚动位置）；③ 地址层级修复：选府县**不再自动乱填子级**、**unknown 能正常存/回填**（`collectLevelsForSave`+`hydrateLevels`）。
- **v118**：① 修「视频放大后无法播放」（`pauseFocusVideos` 误关了灯箱视频的 controls，改成只针对内页 `.focus-video`）；② 裁剪**真正应用到详情页封面 + 灯箱放大**（`normalizeMedia` 之前把 `crop` 丢了→前端一直读不到；补 `crop` 透传 + 给共用 `#focus-image` 加 `applyFocusCrop`，放大框按裁剪比例）。
- **v117**：图片裁剪功能（编辑器内容区，非破坏性，选比例+缩放平移，可还原，全链路透传+网站显示）。
- **v116**：灯箱视频缩略图修复；视频放大不再先错位（`is-media-sizing`）；关联下划线延到 logo；对话加折叠+标签移顶；段落加左竖线。
- **v115**：视频可放大（灯箱）；放大主图投稿隐文件名+去 00:00；关联链接白色；层级 unknown 挪到底；对话单句可上下排序。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
