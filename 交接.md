# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-15，前端 v209**。
> **本次任务完成智能体：codex。以后每次更新本文件都必须写明完成任务的智能体（codex / Claude Code 等）。**
> ⚠️ **存档规则（2026-07-05）**：只有「大改动且牵连多处」才自动 commit；其余每个任务做完**先问一句要不要存档**，别自作主张。见 CLAUDE.md 约定4。
> 🔧 **即时提醒**：改前端只需页面 reload；改 `editor-api.mjs`/`server.js` 才**必须杀 node 重启**（`pkill -f "server.js"` 后再 `preview_start`）。`export-tables.mjs` 里照抄了 app.js 的格式化函数（时间/object/地址回退），改其一要两边同步。
> 📌 **工作区未提交改动分三部分**：①用户之前录入的真实数据（`data/umbrellas.json` + 3 个 `submission(pending)/*/record.json` + `导出表格/contributed.csv`，都是上一轮遗留、**不是本轮改的**）；②**档案页重做 + 标签精简 + 默认语言自动判断 + 展开列表同步 v205→v209**（app.js/index.html/styles.css/sw.js/`data/texts.json`/`scripts/editor-api.mjs`/AGENTS.md/CLAUDE.md/`交接.md`/`修改记录.md`）**未 commit，待用户决定**；③未跟踪的 `翻译流程.md`（上一轮的文档）。**按钮/欢迎/回退那批已 commit（`2fc964f`）。**

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **139 条记录**（`filebox/records/**/record.json`，真实数据，**绝不能乱删**）；其中 34 条是投稿伞（`submissionType:"contributed"`，多为 `submission(pending)` 下、坐标/地址大概或缺失，用户在陆续补），2 条隐藏在 `filebox/hidden`（IMG_1467/IMG_8955）。前端合计显示 137 条。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。**当前 v209**，bump 后顺手 grep `v=` 核对。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，别换随机端口看地图。
- 本地浏览器验证：首屏是欢迎页，`app-shell` 在 `body.is-entered` 前 `visibility:hidden`——预览里先 `document.body.classList.add('is-entered')`（或点标题进入）。编辑器 UI 只在 `localhost/127.0.0.1` 出现（`IS_LOCAL`）。**无头浏览器模拟不了 `pointer:coarse`（触摸设备），触摸相关的 UI 要真机/平板横屏确认。**

## 最近 5 轮
- **2026-07-15（codex，未 commit=待用户决定）v208→v209 展开列表同步 Archive + 写入规则**：地图「展开列表」去掉 Fieldwork/Contributed 两个 scope 页签，标题改成 Archive；按钮顺序同步为 Time / Place / Type。Time 和 Place 显示全部记录（fieldwork+contributed），Type 只显示 Fieldwork，和 Archive 保持同一套范围逻辑。清理旧 listScope 残留。把“改 Archive 必须同步展开列表”的规则写进 `AGENTS.md` 和 `CLAUDE.md`。四处版本 208→209。只改前端和说明文档，**未动真实 record 数据**。
- **2026-07-15（codex，未 commit=待用户决定）v207→v208 首次访问自动选择默认语言**：没有手动选过语言时，`getStoredLang()` 会先看浏览器语言列表：日语环境默认 ja，英语环境默认 en；再看语言地区后缀（如 `*-JP`）和东京时区兜底；其他情况默认 en。用户点过右上角 JA/EN 后仍写入 `localStorage` 的 `fu-lang`，以后永久尊重手动选择，不会被自动判断覆盖。四处版本 207→208。只改前端语言初始化/版本号 + `修改记录.md`/`交接.md`，**未动真实 record 数据**。
- **2026-07-15（codex，未 commit=待用户决定）v206→v207 档案页标签精简 + 手机访问排查**：按用户要求去掉 Archive 的 `submitted` 标签；标签顺序改成 `time / place / type / statsFw / statsContrib`；`type` 后面的 `(Fieldwork)` 去掉，两个 stats 后缀保留。四处版本 206→207。只改前端标签/分发注释/版本号 + `修改记录.md`/`交接.md`，**未动真实 record 数据**。手机同 Wi-Fi 打不开的主因：Mac 当前局域网 IP 已变成 `192.168.3.48`，旧 `192.168.3.39` 不是当前地址；本轮已重新启动 4173 服务，本机 `http://127.0.0.1:4173/` 和手机用的 `http://192.168.3.48:4173/` 都返回 HTTP 200。
- **2026-07-11（本轮，Claude Code）v203→v206 一批前端改动，分两块存档状态不同**：用户一次给 8 条（第1条首访提示不做）。
  - **A. 按钮/欢迎/设备适配/回退（已 commit `2fc964f`，v203→v204，未 push）**：①**任务2 触摸/宽度分离**：新增 `isTouchDevice()`（`pointer:coarse`），跟 `isMobileSheet()`（宽度 820）分开——交互按触摸判、布局按宽度判，修「平板横屏被当纯桌面」。②**任务3 欢迎屏轻触**：底部加日英双语提示（`#welcome-tap-hint`，`I18N.welcomeTapHint`，仅 `@media(pointer:coarse)` 显示，桌面靠准星暗示不显示）；触摸设备整屏可轻触进入（`els.welcome` click→enterSite，`isTouchDevice()` 守卫、桌面不触发）。③**任务4 两个地图按钮**（`#map-fab-row`，中下方，浅色毛玻璃胶囊）：「ランダム」`#random-marker`（map-pin 右下角 `+`→`?` 自绘 icon）+「最寄りへ」`#nearest-marker`（map 每次 `idle` 时 `countMarkersOnScreen`==0 才 `hidden=false`）。**任务8 随机点击直接开详情**：`goToRandomMarker` 走 `selectUmbrella(pick.id,{focus:true})`（带镜头动画+开详情）。**任务7 要动画不要瞬移**：`goToNearestMarkerOnScreen` 先 `setZoom` 再 `panTo`。**关键坑**：`panTo` 紧跟 `setZoom` 会打断平移动画→中心不动（只 zoom 变）；随机改走 focus、最近改「先 zoom 后 panTo」。**任务6(按钮不要黑底)**：地图区域强制深色 color-scheme 导致 `--panel` 取深色玻璃→ `.map-fab{color-scheme:light}` 拿浅色玻璃（和汉堡展开一致）。⑤**任务5 无定位回退**：拒绝/无授权/日本外从「东京站 15 级」改「离东京站最近标点居中、13 级」（`TOKYO_FALLBACK_ZOOM=13`、`nearestMarkerTo`/`getTokyoFallbackView`）。**验证**（本地视口 490，桌面 pointer:fine）：无报错；进入即 z13 最近标点居中✓、随机 z16→开详情✓、最近按钮无标点时浮出并跳回✓、欢迎提示已本地化桌面隐藏✓。**未验证（要真机/平板横屏）**：触摸端整屏轻触+提示显示（无头模拟不了 coarse）。
  - **B. 任务6 档案页重做（v204→v206，未 commit，待用户决定；后续 codex 已精简到 v209）**：把「Archive ( Fieldwork / Contributed ) 大切换 + 各自一排标签」合并成一套标签：`time`(两边合并·按拍摄时间分月·**无拍摄时间落底部 `unknown` 组·恒最底**) / `type`(仅 fieldwork·几乎不变·**all 下加可编辑总说明文**) / `place`(两边合并进 fieldwork 完整地址层级·投稿按自己 `locationLevels` 落位) / `statsFw` / `statsContrib`。**投稿伞卡片左上角**加与详情页 ID 同款信封+勾 icon（`.card-contributed`，`renderPhotoCard` 里，线宽绑 `var(--icon-stroke)`）。**结构**：`renderArchive` 变成统一分发器——`statsContrib` 渲染进 `#contributed-content`（复用旧总览/排序处理器，`renderContributedArchive` 已去掉自绘工具栏），其余进 `#archive-content`；`render()` 不再单独调 `renderContributedArchive`/`syncArchiveScope`（后两者及 `groupByMonth`/`groupContributedByMonth`/`groupContributedByPlace` 现在是死代码、留着没删）。新增 `groupMergedByMonth`/`renderTypeIntro`。**新增可编辑文案 `typeAllIntro`**：`data/texts.json` + 前端 `loadTexts`(`normalizeTypeAllIntro`) + 「文案編集」面板 build/fill/save（`data-texts-typeall`）+ 后端 `editor-api.mjs` `saveTexts` 白名单，全链路加了。**用户后续修正已做**：①恢复「Archive」大标题（只去掉 scope 部分）②标签**桌面一排、窄屏自动换行**③去掉 `submitted` 标签④标签顺序改为 time/place/type/statsFw/statsContrib⑤Type 不再显示 `(Fieldwork)`；Stats 后缀保留⑥首次访问默认语言会按浏览器语言/地区/时区自动判断，手动选择后不覆盖⑦地图展开列表同步成 Time / Place / Type，Time/Place 看全部记录、Type 只看 Fieldwork。四处版本 204→209。**验证**（v206 时本地 1280 桌面 + 490/375）：`node --check` app.js/editor-api.mjs、texts.json JSON 均过、无报错；Time=137 合并·34 角标·月份降序·unknown 垫底；Type=103 fieldwork+说明文、无投稿；Place=137 合并进层级·34 角标；Stats(FW)=透视+总览；Stats(投稿)=投稿总览(contributor/submitted/taken/place)；大标题「Archive」✓、桌面 1 排/手机换行✓。**未验证**：v209 改完后的浏览器视觉、桌面宽屏 **Place 树**、文案編集面板保存 typeAllIntro 往返、真机触摸端欢迎轻触。**只改前端 + data/texts.json + editor-api.mjs + 交接/修改记录，未动任何 record 真实数据；导出表格脚本独立不受影响。** **下一步**：用户点一圈档案页（尤其桌面 Place 树、展开列表、文案編集存 typeAllIntro）+ 真机看触摸端欢迎轻触 → 决定 v209 是否 commit（commit 时只提本轮相关文件，别捎带 📌①的用户数据改动和 `翻译流程.md`）。
- **2026-07-10（Claude Code，未 commit=待用户决定，未 push）v202→v203 地址换行 + 搜索加 title + 表格改版**：用户 6 条。**①详情页地址溢出图片**：`formatAddressBreaks` 改成「只在逗号/of/for/the 前断，词组不拆」+ CSS `.focus-overlay-info .focus-info{max-width:85%}`+`.focus-info-value p{overflow-wrap:anywhere}`。**②展开列表搜索加 title**：`filteredUmbrellas` haystack 补 `item.title.ja/.en`。**③表格改版（重写 `export-tables.mjs`，值全照详情页）**：「文件名」→「文件夹名」（修 IMG_8056 media[0] 是插图的 bug）；时间用详情页格式；「状态」→「object」；空地址回退层级地址；fieldwork「显示名」移最后+`★`。四处版本 202→203。**改前端(app.js/styles.css/index.html/sw.js)+export-tables.mjs，未动真实数据。** 另**追加小修**：清 19 个日文段落 PDF 换行残留空格（只删挨着假名/汉字的半角空格）、rebuild。**遗留**：个别块疑似 PDF 漏字（IMG_6637「植え込み?」、IMG_6774「同じ道?」），待用户确认。

## ⏳ 待办
0. **站内自建投稿表单**：已挪进 `未来可能需要注意的事.md`（推荐 Google Apps Script Web 应用当收件端）。用户想做但暂缓。
1. **投稿全流程只差最后一下**：收件箱/EXIF/只读信息/「导入并编辑」都验过，**只剩用户亲手点一次「导入并编辑」**确认完整链路。
2. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
3. **3 个历史未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664。
4. **12 个投稿伞没抓到天气**：时间只有日期没钟点（如「2024.10」）；补到「年月日 时:分」后编辑器点主图「获取天气」。
5. **Open-Meteo 档案对太近日期有几天延迟**：新标点抓不到多半日期太近，等几天或用 forecast 的 `past_days`。
6. **未来想法**见 `未来可能需要注意的事.md`：上传自动转 HEIC；视频裁剪未做。
7. **本轮档案页的收尾**（见「最近5轮·本轮 B·下一步」）：桌面 Place 树观感、文案編集存 typeAllIntro 往返、真机看欢迎轻触；再定 v209 是否 commit。

## 关键子系统（速查）
- **档案页/展开列表（v209 重做，本轮）**：Archive 单一分发器 `renderArchive()` 按 `state.archiveMode` ∈ `{time,place,type,statsFw,statsContrib}` 渲染。`statsContrib`→`#contributed-content`（`renderContributedArchive`，`state.contributedMode=stats`，工具栏已删）；其余→`#archive-content`。合并数据 = `state.umbrellas`（fieldwork+contributed）；Time 用 `groupMergedByMonth`（`parseLooseDateParts` 通吃 ISO+宽松，无时间进 `unknown` 组恒垫底）；Place 用 `groupByPlace(merged)`（投稿靠 `locationLevels` 落位）。Archive 标签顺序为 Time / Place / Type / Stats(Fieldwork) / Stats(Contributed)；Type 不带 `(Fieldwork)`。地图展开列表同步为 Time / Place / Type：Time/Place 看全部记录，Type 只看 Fieldwork。以后改 Archive 必须同步检查展开列表（规则已写 AGENTS/CLAUDE）。投稿卡角标 `.card-contributed`（`renderPhotoCard`）。Type 的 all 说明文 = 可编辑 `TEXTS.typeAllIntro`（texts.json + 文案編集面板 + 后端 saveTexts 白名单）。
- **默认语言（v208）**：第一次访问、浏览器里还没有 `fu-lang` 时，按 `navigator.languages` / `navigator.language` 判断；日语或 `*-JP` / 东京时区默认 ja，英语默认 en，其余默认 en。用户手动点 JA/EN 后写入 `fu-lang`，之后不再自动覆盖。
- **投稿系统（v145/v146）**：真源是用户 Google 表单。搬运只本机：`scripts/google-auth.mjs`(OAuth)+`submissions-core.mjs`(核心)；入口=`npm run submissions:pull` 或编辑器收件箱（工具栏信封 `#editor-inbox-btn`）。后端 `/api/submissions/list|photo|import`。**改 editor-api/core 后杀 node 重启。**
- **详情面板排布（v127 重写）+手机抽屉（v160 手势）**：`.focus-image-panel` 固定框内 `.focus-scroll`+`.focus-stage`。窄屏(≤820)底部抽屉 `setupSheetGestures`/`applySheetDrag`/`releaseSheetDrag`/`animateSheetExit`，两档 peek↔full。聚焦标点手机在屏幕上方 25%。
- **天气联动（v128 按每张图存）**：`media[].weather`(+`showWeather`)；主图=拍摄前 24h 逐时、补充/细节=当时 1 点。抓取 `/api/fetch-weather`（Open-Meteo 无 key）。图标 11 种（v153 Lucide+手绘小雪）。weather+showWeather 有 4 处重建会丢、都已补保留。
- **图片分级（v139）**：每原图旁 `.thumb.webp`(400)+`.web.webp`(1280)；列表用 thumb、详情用 web、放大先 web 再后台下原图。`record-utils.mjs` 靠 `isDerivativeFile` 排除生成物。存量 `npm run images:build`。
- **可调设定**：聚焦模糊/地图文字 `data/site-settings.json`（`/api/save-site-settings`）；视觉主题 `data/theme.json`（`iconStroke`+详情 4 类字体，`/api/save-theme`，`THEME_CLAMP` 钳制）。都是「本机面板调→写回文件→push 上线」套路。图标想吃线宽就把 CSS `stroke-width` 改 `var(--icon-stroke,默认)`。**改 editor-api 保存前杀 node 重启。**
- **UI 文案 `data/texts.json`**：statsIntro + typeAllIntro(本轮新增) + typeDescriptions + about。前端 `loadTexts` fetch；本机「文案編集」面板（工具栏 `#texts-toggle`）改完 `/api/save-texts` 写回。**不要在 app.js 硬编码这些文案。**
- **显示名 displayId（v141）**：填了就替换页面显示 ID，内部匹配/文件夹仍用真实 `item.id`；显示统一走 `displayUmbrellaId()`。
- **根字体栈 lang 无关（v157，别改回去）**：`:root` `font-family` 必须拉丁字体在前、日文字体兜底、最后 `sans-serif`，否则切语言英文字号会跳。
- **地图标点**：`renderMapMarkers` 原地更新+distinct zIndex+`optimized:true`，不整批重建。再点已聚焦标点走 `selectUmbrella` 的 `reFocusingSame` 分支（不回顶）。**`panTo` 后别紧跟 `setZoom`（会打断平移）——要动画就「先 setZoom 后 panTo」，或用 `selectUmbrella(id,{focus:true})`。**

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结。
4. **存档规则（2026-07-05）**：大改动且牵连多处才自动 commit；其余做完先问一句要不要存档。预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）。
6. **`交接.md` 每次重写只留最近 5 轮，并且必须署名本次任务由哪个智能体完成。**
7. 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
