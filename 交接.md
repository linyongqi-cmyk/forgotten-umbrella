# 交接文件 / Handoff（新窗口先读这个）

> 用于在新会话继续开发。**先读 `CLAUDE.md`（项目说明）**，再读本文件（当前进度+待办），需要细节可查 `修改记录.md`。
> 用户是非技术小白，用大白话中文沟通。截至 2026-06-22。

## 当前状态
- 项目能正常跑：`npm start` → http://127.0.0.1:4173/ 。前端缓存版本 **v61**。
- 本地编辑器功能已经很完整：字段编辑、地址级联下拉（全日本）、每把伞的数量/颜色/种类/状态、标记变色、图片管理、图文段落编排、分类增删改、新增/删除标点、拖动设坐标、左侧实时详情预览。
- 76 条记录里**有用户真实录入的数据**（数量/颜色/地址/状态等）。**绝不能删**。
- **可能有未提交的改动**：先 `git status` 看一眼；上一轮（详情页 INFORMATION 排版、补充图片小字、种类选项+默认、欢迎页字距）若还没 commit，先按规则问用户是否存档。

## ⚠️ 待办任务（用户已提出、还没做）

按用户的话"先易后难"。这些都在**编辑器/档案/欢迎页**：

1. **(item 6) Archive 卡片加编辑按钮**：每张档案卡右下角加一个编辑按钮，点了像编辑模式一样**弹出同一个编辑框**直接编辑，**不要跳回地图页**。
2. **(item 7) Archive 卡片双击 → 跳地图**：双击档案里某张图，跳到地图上该图对应位置。
3. **(item 8) 无时间无地址的图片** —— 已和用户达成方案（不算大工程）：靠 item 6 在 Archive 直接编辑它；并在**编辑器里加一个「放到地图上」按钮**，点一下把坐标设成当前地图中心（出现标记），再拖到准确位置（复用"新增标点"的坐标逻辑）。
4. **(item 9) 时间排序按钮整合**：现在"时间"排序右边有个单独的"调换顺序"按钮——**删掉它**，整合进"时间"按钮本身：在时间排序状态下再点一次"时间"就调换升/降序，并加一个**视觉符号**（如 ↑↓）。**Archive 和地图页左侧弹出侧边栏都要改**。
5. **(item 10) Archive 卡片显示规范化**：不再显示地址/时间/状态/颜色等文字。改成显示**图标 logo**——当该标点有多张图（补充图片+细节）时右下角显示"多图" logo；有"插图"时显示"插图" logo；有 title 就显示在 id 右边。
6. **(item 12) 进入地图缩放动画**：第一次点欢迎页进入地图时，加一个**从"能看到日本主岛"的比例尺缩放到默认视角**的动画。

**语言相关（用户明确说暂缓，先别做）**：
- title 改双语（英文框+日文框）；图文段落也改双语；语言切换去掉中文只留日英。原因：目前默认日语但很多地方仍是英文，文本体系还没理顺。

## 用户定的规则（务必遵守，已写进 memory + CLAUDE.md）
1. 动手前先**确认 + 反思**需求（合理吗？有更好方案吗？）。
2. 挖**隐藏需求**。
3. 任务结束给**小结**。
4. 任务结束固定两问：「**你是否要存档？**」+「**打开预览网址**」（给可点链接）。
5. 改过文件就更新 **`修改记录.md`**。
- 原型期只本地；存档=commit（提交 main）；**只有用户说上线才 push**。
- 解释一切用大白话；数据是用户真实数据，**删前必核对**。
- 编辑系统做得这么细是**为后期做"照片/伞的数量类型状态"统计表格**准备的，别简化掉这些字段。

## 怎么继续（给新窗口的"我"）
- 改前端记得**版本号一起 +1**（见 CLAUDE.md「版本号」）。
- 改了 `editor-api.mjs` 要**杀 node 重启**预览服务器。
- 测试改动用 `preview_*` 工具；测完**把改动的测试数据还原**，别污染用户真实数据。
- 每做完一块：更新 `修改记录.md` → 问用户是否存档 + 给预览链接。
