# 交接 / Handoff（新窗口先读这个）

> 新会话先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。截至 **2026-07-01，前端版本 v114**。
> 🔜 **下一步（用户明确要的，未做，下一轮专项）**：
> 1. **编辑器「内容」图片裁剪**（#8）——非破坏性（不改本地文件、只网站显示裁剪结果、可还原原图）、自由比例或标准比例，裁剪钮放缩略图左下角；需加 record 字段(如 `media[].crop`)+全链路透传(record-utils/build/editor-api)+交互裁剪弹窗+详情页/编辑器/灯箱按 crop 显示。**视频裁剪**：用户问能否做成简单的「限定容器大小」效果——**只在裁剪代码不影响其他功能的前提下才做视频裁剪**，否则只做图片。
> 2. **视频放大(#9)**：用户要**加**——让视频也能点开进灯箱放大播放。现在 `getExpandableMedia` 排除视频、灯箱只有 `#focus-image`(img)；要让灯箱支持 `<video>`（setExpandedImageFrame 用 videoWidth/Height，视频不做平移缩放只放大播放）。
> **T7 模糊本身仍需真机确认。**

## 当前状态
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（Mac：`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 上跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **93 条记录**（71 自拍 + 22 投稿，都是真实数据，**绝不能乱删**）。
- **改前端就把版本号一起 +1**（`index.html` 两处、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。当前 **v114**。
- **本地预览看不到地图**：Google Maps API Key 限域名，127.0.0.1 上地图不渲染（绿色标点只能在你真实浏览器看）。详情页/编辑器/表格用预览 MCP 的 DOM 脚本验证（`state`/`editor` 是模块变量，window 取不到）。

## ⏳ 待办（按优先级）
1. **T7 模糊地址（v107 从根源重做，待真机验证）**：record 有 `blurApprox`/`approxZoom`/`blurLabel`，编辑器「投稿信息」勾「模糊地址」才显示聚焦缩放框+标点下文字框。**核心做法（v107）**：模糊不再是盖在地图上的全屏层（会糊住标点），而是用 **Google Maps OverlayView 把模糊 div 注入到 marker 下面的 pane**（`overlayLayer`，markers 在 z103 的 overlayMouseTarget、下面 pane 空）——地图被糊、**真标点天然在上面、始终清晰**，不藏标点不画假标点。函数 `ensureBlurOverlay`/`showBlurApproxOverlay`/`hideBlurApproxOverlay`，`draw()` 按视野定位、跟随平移缩放。模糊态隐藏全屏 `.focus-blur`，用 `.focus-blur-approx-layer`（`blur(4px)` + **黑白径向**：圆内白 0.5→0.38、圆外黑 0.32，`draw()` 把 `--cx/--cy` 对准标点=`state.blurOverlayCenter`，可调 `--cr`/白黑 alpha）。标点下方 30px 显示 `#focus-approx-label`（=blurLabel 或回退显示地址，纯白字无背景+阴影）。**真机调**：CSS 搜 `.focus-blur-approx-layer`（backdrop blur px、白/黑 alpha、`--cr` 圆大小）。rednote_621639248 已设 blurApprox=true/approxZoom=14 可看。⚠️ **本地地图渲染不稳定（API Key 限域名），blur 视觉+标点清晰需真机确认**；万一 backdrop-filter 在 pane 里不生效，降级成只有白滤镜（无模糊），告诉我再调。
2. **T8 地图调试面板（已实现，编辑模式专用，待真机调样式）**：`#map-layers` 按钮只在编辑模式出现（不面向用户）。13 整类，每类「自動/表示/淡化/隠す」循环 + 缩放阈值框（低于则隐藏），普通图+卫星图都生效，存 localStorage（仅本机；线上用户只看底图）。加/删类别改 `MAP_LAYER_CATEGORIES`，淡化效果在 `categoryStyleRules()` 的 `fade` 分支（lightness/gamma）。**真机进编辑模式→点 `#map-layers` 按钮调；调好后把想要的整套值告诉我，我再写进 `mapStyles`/卫星样式做成正式默认**（目前调试值只存本机 localStorage，不会自动变成线上默认）。真 alpha 透明度仍做不到。
3. **投稿伞坐标/地址层级多为大概或缺失**（很多只有 locationText 没填 locationLevels，所以「場所」分组里大量落到「海外」）；需真机逐个拖坐标+补地址层级。`rednote_kankan(2)`投稿时间仍 2026/06/30（原 TIFF 重存丢建立时间），编辑器手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（表里有行没照片）、`IMG_8583.jpg`（有照片没对应行）。
5. **投稿系统二期（Google 表单自动同步，未做）**：用户 2026-06-27 说先不做但记住。方案见 `~/.claude/plans/claude-md-scalable-riddle.md` 和 memory。

## 投稿伞系统全貌（v92 一期 → v94 二期，已完成）
- **来源字段**：record.json 有 `submissionType`(contributed/own或空)、`submitter`(署名)、`submissionChannel`(渠道,内部)、`submissionTime`(投稿时间,默认原始照片建立时间)、`submitterNote`(原话)、`remarks`(备注占位)、`locationApprox`/`timeApprox`。透传链：`record-utils.mjs`→`build-umbrellas.mjs`→`editor-api.mjs`。
- **编辑器**（app.js setupEditor/openEditor/saveEditor）：
  - **两列**：`.editor-col-left`（各字段）+ `.editor-col-right`（备注+内容图文）。宽度用 CSS 变量 `--editor-w`/`--preview-w`(`clamp()`：宽屏固定 px、窄屏 vw 收缩保证露地图)；档案 `editor-open` 预留宽度、toolbar 顶部居中都用这俩变量。
  - 头部勾选框 `.editor-head-check`（文字左框右）：`#editor-flag-toggle`(待改)、`#editor-linked-toggle`(关联)、`#editor-title-toggle`(标题)。关联/标题勾了才显示对应行。
  - 左栏顺序：来源→类型(仅自拍`.editor-type-row`,`listTypeCategories()`排除submission)→标题→关联→坐标→显示地址(**textarea自动撑高**)+右侧「大概」→地址层级→拍摄时间+「大概」→投稿信息(`.editor-contrib`仅投稿,**已删投稿渠道+原话框**,投稿时间`formatDateOnly`只到年月日)→伞属性。`syncSourceVisibility()` 控制类型/投稿信息显隐。
  - 右栏：**备注 Remarks(仅勾「待改」后显示,`editor.remarksRow`)** + 内容。
  - 内容块 `renderFlow`：text/dialogue/photo。`＋加段落`/`＋对话`/上传。**操作钮竖排**(主图横排);段落&对话双语 textarea(对话块有「对话」标签);图片块=缩略图+**下方下拉(补充/细节/插图)**、顶部显**文件名**、时间框空=用 EXIF(placeholder)填了=覆盖(`item.timeOverride`)。可 ▾ 折叠。
  - **对话块(dialogue)**：blocks `type:"dialogue"`,详情页 `renderDialogueLines` 渲染斜体+左竖线、編者/投稿者左 正文右(`.focus-dialogue`)。原 9 条原话段落已脚本转成 dialogue。editor-api `sanitizeBlocks` 已认 dialogue（**改了 editor-api 要杀 node 重启**）。
  - 没有 ID 编辑框（改 ID 风险高）。保存不带 category(类型走 move-record)。
  - ⚠️ **CSS 坑**：`.editor-row`/`.archive-content` 的 `display:flex/grid` 会盖 `[hidden]`——可隐藏元素要配 `[hidden]{display:none}`、勾选框配 `input[type=checkbox]{width:auto}`。
  - 左预览 `renderEditorPreview()`/`buildPreviewDraft()` **实时**(object/state 仍上次保存值)。dirty=`editor.dirty`,`closeEditor()` 未保存会 confirm。
  - 新增标点：`#editor-add`→`openCreateDialog()`(选来源+类型,投稿隐类型)→`onCreateRecord` 弹窗**可改文件名**(留空=不改,文件名即id)→`centerInEditorGap()` 把新标点落到可见地图区(避开编辑栏,含 EXIF GPS)。
  - 三个圆 logo 钮 `.editor-toolbar`：平时右下角地球上方竖排；**打开标点编辑(editor-open)时移到左预览/右编辑栏中间空隙顶部横排**。
  - **ESC** 逐层退：新增弹窗→当前标点编辑→编辑模式→关左侧列表栏。
  - **右上角 `#scale-readout`** 显当前缩放数字(仅本地,setupEditor 只在 IS_LOCAL 跑)。
- **投稿伞 id**：已重命名——只投稿一次去 `_1`，多次 `_N`→`(N)`(英文半角括号)。只改文件夹名(=id)+ linkedId，媒体文件名没改。脚本在 scratchpad(一次性)。
- **公开地图 4 色标记（v97，`markerCategory`/`MARKER_COLORS`）**：深红 `#8f2310`=有标题自拍 / 红 `#c54f35`=普通自拍 / 深绿 `#1f6b3a`=有故事段落投稿(item.story 非空) / 绿 `#2e9e5b`=普通投稿。`markerIcon(active,flag,category)`。地图筛选 `#map-filter`(展开式)：`state.markerFilter` 4 类显隐，只影响地图标记不影响档案；编辑模式下不过滤。
- **详情页**（renderFocusHeader/renderFocusArticle）：投稿伞徽章/「约」/「投稿者」**一律英文**(`.en`：Contributed / approx. / Contributed by)；**隐藏 type/object/state**；时间用 `formatLooseDate`。**投稿者原话已迁进 blocks 文字段落**(v97，按行渲染=分行)，不再有单独引用块。图片**无阴影**。
- **Archive 一页两态**（不再是导航标签）：大标题「Archive ( Fieldwork / Contributed )」，`.archive-scope-prefix/-sep/-suffix` 死文本，只有两个 `.archive-scope-tab`(`data-archive-scope` own/contributed) 可点、当前页 `is-active` 加下划线；`syncArchiveScope()` 控制显隐（`display` 会盖 `[hidden]`，已补 `[hidden]{display:none}`）。
  - own = フィールド：只自拍（grid + 交叉表统计 + 总览，过滤投稿）。总览 IMG 表头可点复位默认排序。
  - contributed = 投稿（与 Fieldwork 同构）：`renderContributedArchive` 工具栏 **撮影時間(按月分组)/場所(按都道府县分组,非日本→海外,`JAPAN_PREFECTURES`)/統計**（已删投稿时间排序）→ 分组照片卡网格(`renderArchiveGroup`) / `renderContributedOverview` 总览(列 contributor(同投稿者(N))/submitted/taken/place,默认 contributor 字母序,点表头排序 ↑↓,点 contributor 复位,全英文,标题「総覧」)。卡片 ✎ 开编辑、双击跳地图。
  - About 页 4 方框(`#stat-count`/`#stat-fieldwork`/`#stat-submissions`/`#stat-since`) + **4 色图例** `#about-legend`(`renderLegend`)。

## 视频媒介（v93，自拍+投稿通用）
- `record-utils.mjs`：媒体含视频(.mp4/.mov/.webm/.m4v)，`isImageFile`/`isVideoFile`，**视频不当封面**。
- `app.js`：`isVideoFile()`；详情页视频渲染 `<video class="focus-video" controls>`；灯箱排除视频；编辑器 flow 视频显示+上传 accept video/*。
- `server.js`：补 video/webp 等 MIME（本地预览正确播放）。

## 数据导入相关脚本（一次性）
- `scripts/import-collected.mjs`：从 `filebox/collected/*.xlsx`(先 python 导出成 JSON) + iCloud「other people」照片导入投稿伞，含 META(坐标/id 映射)。**投稿时间读 iCloud 原始照片建立时间**（仓库副本的建立时间是导入当天，不可用）。重跑前先删 `submission(pending)`。
- `scripts/split-1164552325.mjs`：拆双伞记录（已执行完）。

## 最近几轮（详见 修改记录.md）
- **v114（2026-07-01）**：档案图片卡当记录含视频时右下角加视频描边 logo（`CARD_ICON_VIDEO`，同风格）。已 DOM 验证。
- **v113（2026-07-01）**：一批 13 项里做了 12 项——① 补充/细节图右上角加放大镜；② 地址级联每级加 unknown；③ 编辑模式也遵守标点筛选；④ 待改：自拍黑/投稿白；⑤ 凡例+筛选日语系统用日语且改序(Fieldwork/Fieldwork·titled/Contributed/Contributed·story)；⑥ 关联链接去箭头改水平链接 logo+蓝色提亮(#5aa2ff)；⑦ 视频中间大播放键+默认静音；⑩ 退出详情/放大别的图时暂停视频；⑬ 视频字幕挪顶部避开进度条；⑪ 对话每句删除键挪左上圆钮；⑫ 投稿伞补充/细节隐藏文件名；⑧ 编辑器视频缩略图禁画中画(去 fig5 遮挡)。**⑧裁剪主功能+⑨视频放大未做（大功能，下一轮专门做）。均已本地验证。**
- **v112（2026-07-01）**：① **修模糊地点标点放大图后点空白回不到详情**（点空白关放大图的处理挂在 `.focus-blur` 上，而 is-blur-approx 把它 `display:none`→放大态没背景接收点击。加 `.is-blur-approx.is-image-expanded .focus-blur{display:block}` 放大态重显当背景，统一逻辑）；② 投稿标记改**信封**描边图标（之前人形不达意，用户选信封）；③ **词条列宽自适应**（`.focus-info` 改 grid `auto minmax(0,1fr)` + row `display:contents`，标签列=当前最宽标签宽；自拍无 submitted→列窄正文左移，去掉固定 84px 的大间距）。**①逻辑验证、②③本地截图验证。**
- **v111（2026-07-01）**：① **模糊内圈 1px 外圈 6px 且无接缝**（底层全屏 blur(1px)+色洗白 0.3/黑 0.24，`::after` 叠 blur(6px) 用渐变蒙版 0.4cr 透明→边缘实，额外模糊渐进加上去=平滑无接缝；可调 `--blur-inner`/`--blur-outer`/`--cr`/alpha）；② **修"模糊标点主图先低位再跳上去"**（`.is-loading` 的 opacity 有 900ms 过渡没真正藏住、慢加载投稿图 frame 撑高回流→给 is-loading 加 `transition:none` 瞬间藏，加载完淡入全尺寸）；③ 浮层描边变浅（0.6/0.45）；④ submitted 挪到 by 前；⑤ 主图右上加放大镜提示 `#focus-zoom-hint`（点它/点图都放大，灯箱隐藏）；⑥ 投稿标记 Contributed 胶囊→小人形描边 logo。**②③④⑤⑥已本地验证；① 需真机看。**
- **v110（2026-07-01）**：模糊接缝一度改回单层匀模糊（v111 又改成 1px/6px 叠加，见上）；详情页主图去阴影（text-shadow 代替渐变垫底）；竖图文字溢出图外修掉（主图不限高、frame=图）；information 重排+加投稿时间；滑动提示 Scroll for more；About 右栏。

## 工作约定（详见 CLAUDE.md + memory）
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——合适检查点直接 commit，预览链接需要时才给。
5. 改过文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main（单人不开分支）；**只有用户说「上线/同步」才 push**；push 前先 `git fetch` 看远端。
