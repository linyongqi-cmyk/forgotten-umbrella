# 交接 / Handoff（新窗口先读这个）

> 新会话先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。截至 **2026-06-29，前端版本 v93**。

## 当前状态
- 项目正常跑：`npm start` → http://127.0.0.1:4173/ 。改了 `record.json` 要 `npm run records:build`；改了 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**预览（Mac：`pkill -f "node server.js"` 后再 preview_start，否则有缓存）。
- **Mac 上 node 是绝对路径** `/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里 `npm`/`node` 不在 PATH，跑脚本用绝对路径）。
- 当前 **92 条记录**（71 自己拍 + 21 投稿），都是真实数据，**绝不能乱删**；改前先核对。
- **改前端就把版本号一起 +1**（`index.html` 两处、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。当前 **v93**。
- **本地预览看不到地图**：Google Maps API Key 限域名，127.0.0.1 上地图不渲染（所以绿色标点只能在你真实浏览器看）。详情页/视频/统计要靠预览 MCP 用 DOM 脚本测（`state`/`editor` 是模块变量，window 上取不到）。

## ⏳ 待办：投稿系统二期（Google 表单自动同步，尚未做）
- 用户 2026-06-27 明确「先不做二期，但要记住还要做」。等编辑系统稳定后做。
- 用户已有现成 Google 表单（带文件上传题→强制 Google 登录，外部读不到题目）。思路：本机脚本 `scripts/pull-submissions.mjs` 从 Google 表格(CSV发布)+Drive(公开链接)零依赖拉新投稿 → 落 `submission(pending)` → 本机审核转正。详版见计划文件 `~/.claude/plans/claude-md-scalable-riddle.md`。

## 投稿伞系统（v92 一期 + v93 数据/视频，已完成）
- **区分来源**：record.json 有 `submissionType`("contributed"/"own"或空)、`submitter`、`submissionChannel`、`submitterNote`、`locationApprox`、`timeApprox`。旧 71 条默认 own。透传链：`record-utils.mjs`(序列化注释)→`build-umbrellas.mjs`→`editor-api.mjs`(消毒)。
- **编辑器**：抽屉「来源」单选→投稿展开专属字段(`.editor-contrib`)；左侧「新增标点」旁来源下拉(`editor.addSource`)，投稿新建默认 approx=true。
- **详情页**：投稿伞标题旁「投稿」徽章(`.focus-badge`)、地点/时间 `*Approx` 加「約/approx.」前缀、`submitter` 显示致谢(`.focus-credit`)、`submitterNote` 作引用块(`.focus-note`)。文案在 `UI_TEXT`(contributedBadge/submitterCredit/approxPrefix)。
- **统计**：两套并列，`renderStats` 顶部「自分で撮影/投稿」切换(`state.statsScope`，点击委托在 archiveContent click)；`statsScopeItems()` 过滤，`buildStatsUnits(items)`/`renderStatsOverview(items)` 收 items 参。About `#stat-submissions`=投稿数、`#stat-count`=总数。
- **公开地图绿点**：`markerIcon(...,isContributed)` 绿色 `#2e9e5b`（`isContributedItem()`），own 仍红。

## 视频媒介（v93，自己伞+投稿伞通用）
- `record-utils.mjs`：`MEDIA_EXTENSIONS`=图片+视频(.mp4/.mov/.webm/.m4v)，`isImageFile`/`isVideoFile`；**视频永不当封面**(primary 只选图片)。
- `app.js`：`isVideoFile()`；详情页 `renderFocusArticle` 视频块渲染 `<video class="focus-video" controls>`；`getExpandableMedia` 排除视频(不放大)；编辑器 flow 视频显示 `<video>`、上传 accept 加 video/*。CSS `.focus-video`。
- `server.js`：补 `.mp4/.webp/.gif/.avif/.mov/.webm` MIME（本地预览正确播放；线上 Pages 本来就对）。

## 已导入的 21 条投稿伞（v93）
- 来源：`filebox/collected/collected_from_others_data.xlsx` + iCloud 照片夹「other people」。脚本 `scripts/import-collected.mjs`（一次性，含 23 行的坐标/id 映射表 META）。重跑要先删已存在文件夹。
- 都在 `filebox/records/submission(pending)/<id>/`，坐标是**大概**(approx，需你在地图上逐个微调)，伞数量/颜色/状态留空（待你审核填）。
- **跳过**：Sliden_1、ygrons_1（表里有行但没照片）；IMG_8583.jpg（有照片但表里无行）。`未得到许可` 子夹按要求未导入。
- LYUCHEN_1 含 2 个视频(.mp4)；kankan_2 原 .tiff、LYUCHEN bmp 已转 jpg。

## 最近几轮（详见 修改记录.md）
- **v93（2026-06-29）**：视频播放 + 投稿伞绿点 + 批量导入 21 条投稿伞 + formatDateTime 原文兜底。
- **v92（2026-06-27）**：投稿系统一期（区分投稿/自己，编辑+详情页+两套统计+投稿数）。
- **2026-06-27 迁移到 Mac**：Node 24、修 launch.json node 路径、更新 CLAUDE.md。
- **2026-06-27 清理数据**：删 22 孤儿缩略图；IMG_8062 补 linkedId。
- **2026-06-27 修线上插图**：加 `.nojekyll`。

## 工作约定（详见 CLAUDE.md + memory）
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——合适检查点直接 `git commit`，预览链接需要时才给。
5. 改过文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main（单人不开分支）；**只有用户说「上线/同步」才 push**；push 前先 `git fetch` 看远端。
