# 交接 / Handoff（新窗口先读这个）

> 新会话先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。截至 **2026-06-29，前端版本 v97**。

## 当前状态
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（Mac：`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 上跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **93 条记录**（71 自拍 + 22 投稿，都是真实数据，**绝不能乱删**）。
- **改前端就把版本号一起 +1**（`index.html` 两处、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。当前 **v97**。
- **本地预览看不到地图**：Google Maps API Key 限域名，127.0.0.1 上地图不渲染（绿色标点只能在你真实浏览器看）。详情页/编辑器/表格用预览 MCP 的 DOM 脚本验证（`state`/`editor` 是模块变量，window 取不到）。

## ⏳ 待办（按优先级）
1. **T5 左侧档案栏同步（未做，待澄清）**：用户要「档案页面的修改同步到左边档案栏」，但左栏是地图的地点列表（own+contributed 都列），不确定要的是：加 Fieldwork/Contributed 切换？还是同款排序/卡片？下次问清再做。
2. **T17 大概位置（用户说先搁置，要我给意见）**：建议点开 locationApprox 的点时，在地图上以坐标为中心画半透明圆（约 150–300m）+「approximate area」提示，比换图标简单、又不误导。
3. **投稿伞坐标多为大概**（locationApprox），需真实浏览器逐个拖准。`rednote_kankan（2）`投稿时间仍 2026/06/29（原 TIFF 重存丢了建立时间），编辑器手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（表里有行没照片）、`IMG_8583.jpg`（有照片没对应行）——要的话补缺的那一半给 AI。
5. **投稿系统二期（Google 表单自动同步，未做）**：用户 2026-06-27 说先不做但记住。方案见 `~/.claude/plans/claude-md-scalable-riddle.md` 和 memory。

## 投稿伞系统全貌（v92 一期 → v94 二期，已完成）
- **来源字段**：record.json 有 `submissionType`(contributed/own或空)、`submitter`(署名)、`submissionChannel`(渠道,内部)、`submissionTime`(投稿时间,默认原始照片建立时间)、`submitterNote`(原话)、`remarks`(备注占位)、`locationApprox`/`timeApprox`。透传链：`record-utils.mjs`→`build-umbrellas.mjs`→`editor-api.mjs`。
- **编辑器**（app.js setupEditor/openEditor/saveEditor，**v95 大改**）：
  - 抽屉宽 880px、`.editor-body` 是**两列**：`.editor-col-left`（各字段）+ `.editor-col-right`（内容图文）。
  - 头部三个勾选框 `.editor-head-check`（文字左、勾选框右）：`#editor-flag-toggle`(待改,立即保存+重绘)、`#editor-linked-toggle`(关联)、`#editor-title-toggle`(标题)。**关联/标题勾了才在左栏显示对应行**（`editor.linkedRow`/`editor.titleRow`，openEditor 按是否已有值自动勾选）。
  - 左栏顺序：来源→类型(`.editor-type-row`)→标题→关联→坐标→显示地址+右侧「大概」(`.editor-field-approx`+`.editor-mini-check`)→地址层级→拍摄时间+右侧「大概」→投稿信息(`.editor-contrib`)→伞属性。`syncSourceVisibility()`：选投稿→隐类型显投稿信息；选自拍→反之。**类型下拉用 `listTypeCategories()` 排除 submission**。
  - **投稿信息已删「投稿渠道」框**；投稿时间只到年月日（存取走 `formatDateOnly`，字段上方有说明）。22 条投稿数据已用一次性脚本去掉时分。
  - 没有 ID 编辑框了（删了，改 ID 风险高）。保存不带 category（类型走 `onCategoryChange`→move-record）。
  - 伞属性折叠区 `editor.umbrellaCheck`：自拍默认勾选+展开。所有勾选框统一文字左、框右。**所有文本框自动高度**（`field-sizing:content`）；内容段落可 ▾ 折叠(`item.collapsed`+`.is-collapsed`)。
  - ⚠️ **CSS 坑**：`.editor-row`/`.archive-content` 等设了 `display:flex/grid` 会盖掉 `[hidden]`——加新可隐藏行/勾选框时记得有对应 `[hidden]{display:none}` 和 `input[type=checkbox]{width:auto}`，否则会出现「不勾也显示」「勾选框拉满宽」。
  - 左侧预览 `renderEditorPreview()` 用 `buildPreviewDraft()` **实时**反映未保存改动（object/state 仍是上次保存值，保存后才重算）。dirty 用 `editor.dirty`（drawer input/change→`onEditorInput`），`closeEditor()` 未保存会 confirm。
  - 新增标点：`#editor-add`→`openCreateDialog()`（`.editor-create-dialog` 弹窗先选来源+类型，投稿隐类型）→选完存 `editor.pendingCreate`→触发文件框→`onCreateRecord` 读它。
  - 三个圆形 logo 钮在 `.editor-toolbar`（右下角地球上方，column-reverse 让编辑钮在最下）：编辑(`#editor-toggle`)/文案/新增；文案+新增 `body:not(.edit-mode)` 时 `display:none`。
  - **ESC** 逐层退：弹窗→当前标点编辑(`closeEditor` 带未保存 confirm)→编辑模式(`toggleEditMode`)。
- **投稿伞 id（v97）**：已重命名——只投稿一次去 `_1`，多次 `_N`→`（N）`(中文括号)。媒体文件名没改（仍 `xxx_2_1.jpg`），只改了文件夹名(=id)，linkedId 已同步。重命名脚本 `scratchpad/rename-ids.mjs`(一次性)。
- **公开地图 4 色标记（v97，`markerCategory`/`MARKER_COLORS`）**：深红 `#8f2310`=有标题自拍 / 红 `#c54f35`=普通自拍 / 深绿 `#1f6b3a`=有故事段落投稿(item.story 非空) / 绿 `#2e9e5b`=普通投稿。`markerIcon(active,flag,category)`。地图筛选 `#map-filter`(展开式)：`state.markerFilter` 4 类显隐，只影响地图标记不影响档案；编辑模式下不过滤。
- **详情页**（renderFocusHeader/renderFocusArticle）：投稿伞徽章/「约」/「投稿者」**一律英文**(`.en`：Contributed / approx. / Contributed by)；**隐藏 type/object/state**；时间用 `formatLooseDate`。**投稿者原话已迁进 blocks 文字段落**(v97，按行渲染=分行)，不再有单独引用块。图片**无阴影**。
- **Archive 一页两态**（不再是导航标签）：大标题「Archive ( Fieldwork / Contributed )」，`.archive-scope-prefix/-sep/-suffix` 死文本，只有两个 `.archive-scope-tab`(`data-archive-scope` own/contributed) 可点、当前页 `is-active` 加下划线；`syncArchiveScope()` 控制显隐（`display` 会盖 `[hidden]`，已补 `[hidden]{display:none}`）。
  - own = フィールド：只自拍（grid + 交叉表统计 + 总览，过滤投稿）。总览 IMG 表头可点复位默认排序。
  - contributed = 投稿（v97 与 Fieldwork 同构）：`renderContributedArchive` 工具栏(投稿/拍摄/地点排序 + 統計) → 照片卡网格 / `renderContributedOverview` 总览(列 contributor(同投稿者(N))/submitted/taken/place，默认 contributor 字母序，点表头排序，点 contributor 复位；全英文)。卡片 ✎ 开编辑、双击跳地图。
  - About 页 4 方框(`#stat-count`/`#stat-fieldwork`/`#stat-submissions`/`#stat-since`) + **4 色图例** `#about-legend`(`renderLegend`)。

## 视频媒介（v93，自拍+投稿通用）
- `record-utils.mjs`：媒体含视频(.mp4/.mov/.webm/.m4v)，`isImageFile`/`isVideoFile`，**视频不当封面**。
- `app.js`：`isVideoFile()`；详情页视频渲染 `<video class="focus-video" controls>`；灯箱排除视频；编辑器 flow 视频显示+上传 accept video/*。
- `server.js`：补 video/webp 等 MIME（本地预览正确播放）。

## 数据导入相关脚本（一次性）
- `scripts/import-collected.mjs`：从 `filebox/collected/*.xlsx`(先 python 导出成 JSON) + iCloud「other people」照片导入投稿伞，含 META(坐标/id 映射)。**投稿时间读 iCloud 原始照片建立时间**（仓库副本的建立时间是导入当天，不可用）。重跑前先删 `submission(pending)`。
- `scripts/split-1164552325.mjs`：拆双伞记录（已执行完）。

## 最近几轮（详见 修改记录.md）
- **v97（2026-06-29）**：投稿伞 id 重命名(_1去/_N→（N）)；原话迁进内容段落(按语言拆 ja/en、分行)；详情页去图片阴影+徽章/约/投稿者全英文；地图四色标记+展开式筛选+About 四色图例；编辑器加备注框、图片块竖排钮+文件名+下拉收窄、新增可改文件名、编辑钮组移顶部空隙；投稿档案与 Fieldwork 同构(网格+排序+总览)。
- **v96（2026-06-29）**：修编辑器/Archive 一堆 bug——根因是 CSS `display` 覆盖 `[hidden]`（补 `[hidden]{display:none}` + 勾选框 `width:auto`）；Archive 标题改「Archive ( Fieldwork / Contributed )」只两词可点+下划线；大概勾选移到输入框右边精简文字；删投稿渠道框；**投稿时间统一只到年月日**（脚本改 22 条数据）；文本框自动高度+可折叠；ESC 先关标点编辑再退编辑模式；About 加 Fieldwork 数。
- **v95（2026-06-29）**：编辑器大改（两列布局/字段重排/关联&标题改右上角勾选/删ID/类型隐submission仅自拍/大概勾选移到字段后/勾选框统一右置/实时预览/三个logo钮移右下/新增弹窗选来源+类型/ESC退出+未保存提示）+ Fieldwork 标签 + Archive 大标题切换 own/contributed（删导航标签）。
- **v94（2026-06-29）**：投稿伞二期——独立 Archive(contributed) + 编辑器重构 + 拆分双伞 + 详情页隐 type/obj/state + 时间格式统一 + Original 标签。
- **v93（2026-06-29）**：视频播放 + 投稿伞绿点 + 批量导入 21 条投稿伞 + formatDateTime 原文兜底。

## 工作约定（详见 CLAUDE.md + memory）
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——合适检查点直接 commit，预览链接需要时才给。
5. 改过文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main（单人不开分支）；**只有用户说「上线/同步」才 push**；push 前先 `git fetch` 看远端。
