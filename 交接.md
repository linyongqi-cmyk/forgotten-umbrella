# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-03，前端 v129**。
> **本次任务完成智能体：Claude Code。以后每次更新本文件都必须写明完成任务的智能体（codex / Claude Code 等）。**

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **95 条记录**（真实数据，**绝不能乱删**）。本轮我把 91 个 record.json 重新序列化（加了天气字段+统一格式，用户原有编辑都原样保留）。仍有一批用户在补的投稿伞 `record.json`。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，4173 被占用时不要换随机端口看地图，先释放 4173。
- 本地浏览器可验证布局：注意**首屏是欢迎页**，`app-shell` 在 `body.is-entered` 前是 `visibility:hidden`——预览里要先 `document.body.classList.add('is-entered')` 才看得到内容；点标点没有全局函数，可临时在 `selectUmbrella` 处挂 `window.__fu` 调试钩子（记得提交前删）。

## ⏳ 待办
1. **v129 真机复查**（本轮改动，真机点一下确认）：
   - ① **放大切换动画**：放大后在不同比例的图之间切换（如 IMG_8622 竖图↔横图），面板应**从中心平滑缩放**、不再「先跳到左上再缩回」。无头浏览器实测过渡全程居中(offset 恒 0)，动画类真机再瞄一眼最稳。
   - ② **天气横轴**：现在放在**图片之上**（覆盖信息块底部、place 那行下面），长度=图片宽度约 65%、左对齐；两端 `-24h`/`now` **各自对齐到首/尾图例正下方**；**放大灯箱里不显示**天气横轴。
   - ③ **补充/细节图天气**：详情页里补充/细节图的图注（右下角标题）左边也会出现「拍摄当时」单个天气图例（勾了「显示天气」才出）。放大灯箱里同样有。
   - ④ **Back to map 位置**：短记录（内容不满一屏，如 IMG_8632）按钮贴在图片正下方；长文章（能滚，如 IMG_8622）按钮固定在面板底部、滚动时不动。
2. **12 个投稿伞没抓到天气**：因为它们的时间**只有日期没有钟点**（如「2024.10」「2025.04.27」），抓逐时天气需要具体到小时。等用户把这些点的时间补到「年月日 时:分」后，在编辑器点它主图的「获取天气」即可。
3. **天气图标风格**：现在是我手画的极简线条 SVG（`weatherIconSvg`，晴/多云/云/雾/雨/雪/雷）。若想换统一风格图标可后续替换。可考虑给档案卡/统计页也用天气。
4. **Open-Meteo 档案对太近日期有几天延迟**：真机若某新标点抓不到，多半是日期太近，等几天或改用 forecast 接口的 `past_days`。
5. **地图筛选默认值可固化**（`#map-layers`）：普通/卫星1/卫星2 各一套；真机调好后把值写进默认。
6. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
7. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664（remarks 有记）。
8. **投稿二期（Google 表单自动同步）未做**；**视频裁剪未做**。

## 关键子系统（速查）
- **详情面板排布（v127 推倒重写，v129 微调）**：`.focus-image-panel` 是**固定高宽的框**（`height:calc(100vh - --focus-top - --focus-bottom)`，`overflow:hidden`）；里面 `.focus-scroll`(flex 纵向滚动)+`.focus-stage`。**v129：`.focus-scroll` 改 `flex:0 1 auto`**——短内容时滚动区只占内容高、footer(Back to map)贴内容正下方；长文章时压到剩余高、内部滚、footer 固定面板底部不滚。放大(灯箱)`.is-expanded` 用 `inset:0+margin:auto` 居中；**v129：切换图片的 FLIP 缩放动画 `flipExpandedPanel` 去掉了旧的 `translate(-50%,-50%)`、改纯 `scale()`**（修「切换不同比例图先跳左上再缩回」的怪动画）。窄屏(≤820px)仍是底部抽屉。
- **天气联动（v128 按每张图存，v129 横轴挪回图上）**：数据在 `media[].weather`（+`media[].showWeather`）。主图=拍摄前 24h 逐时（画横轴）；补充/细节=只拍摄当时 1 点（单个图例）。**v129 展示位置**：主图横轴由 `renderFocusInfo` 直接拼在**覆盖信息块 `.focus-overlay-info` 底部**（图片之上，place 行下面），不再有独立 `#focus-weather` 元素——因此放大灯箱里随整块覆盖信息 `display:none` 一起隐藏（=放大页不显示天气）。补充/细节图的单个图例现在**详情页图注**(`renderFocusBlockHtml`)和**放大灯箱角标题**里都显示。抓取：编辑器每张图「获取天气」→`/api/fetch-weather`（带 `mediaId`，`editor-api.mjs::fetchWeather`）。**weather+showWeather 有 4 处会被重建丢掉**，都已补保留：`normalizeMedia`/`applyMediaMetadata`/`mergeRecordMediaWithFolder`/`build-umbrellas.mjs`。批量脚本 `scripts/fetch-all-weather.mjs`。`weather.mjs::fetchWeatherData(lat,lon,time,{hoursBefore})`，Open-Meteo 免费档案无 key。
- **横轴渲染**：`renderFocusWeatherAxis(weather)`（`weatherChangePoints` 取变化点+头尾→贪心强制最小间距 13% 合并太近的→线段只画在图例空隙实现遮挡）。**v129：横轴宽度=文字栏 65% 左对齐；两端标签 `-24h`/`now` 用 `left:pos[0]%`/`pos[末]%`+`translateX(-50%)` 各自对齐首/尾图例正下方**（不再 space-between 贴容器两端）。`singleWeatherIconFor(media)`=补充/细节单图例。CSS `.focus-weather`(width:65%)、`.fw-track/.fw-seg/.fw-mark/.fw-icon/.fw-axis`、`.focus-photo figcaption .fw-single`+`.focus-expanded-caption .fw-single`。
- **聚焦模糊 / 模糊度调整（v123）**：`#blur-adjust` 打开即实时预览；参数存 localStorage `fu-blur-settings`，CSS 变量 `:root --fb-*-n/-a`。
- **地图标点**（`renderMapMarkers`/`markerZIndex`）：原地更新 + distinct zIndex + `optimized:true`，不要整批销毁重建。
- **编辑器三栏 / 修改记录**：预览栏+修改记录栏+编辑栏绑定，有标点打开才一起出现；三条 `.panel-resizer` 可拖大小，存 `fu-panel-sizes`。
- **图片裁剪**：`media[].crop` 非破坏性；封面+灯箱共用 `#focus-image`。

## 最近 5 轮
- **v129（本轮，Claude Code）**：详情页 6 项。① 放大切换怪动画修复=`flipExpandedPanel` 去掉多余 `translate(-50%,-50%)`、改纯 `scale()`（v127 已把居中改 inset+margin，那 translate 就多余了）。② 天气横轴从「主图正下方独立 `#focus-weather`」挪回**图片之上**（覆盖信息块底部，`renderFocusInfo` 拼），宽 65% 左对齐。③ 放大灯箱不再显示天气横轴（②带来的，随覆盖信息块隐藏）。④ 两端标签 `-24`→`-24h`、各自对齐首/尾图例正下方。⑤ 补充/细节图天气图例进详情页图注（之前只放大时有）。⑥ `.focus-scroll` 改 `flex:0 1 auto`=短内容 Back to map 贴图片下、长文章固定底部不滚。本地无头浏览器实测全部通过（FLIP 动画真机再瞄）。
- **v128（Claude Code）**：每张图加「☁获取天气」按钮+「显示天气」勾选框（按 mediaId 抓、主图24h/补充细节1点）；主图天气横轴重画=去温度、两端只 -24/now、中间只变化点图例、强制最小间距合并太频繁的变化、连线在图例处断开(遮挡)；补充/细节放大时角标题放单个当时图例；修 4 处会丢天气字段的地方。（v129 又把横轴挪回图上、标签改 -24h）
- **v128 数据（Claude Code）**：天气从 `record.weather` 改成**按 media 存**；`weather.mjs` 加 `hoursBefore`、时间解析放宽；`scripts/fetch-all-weather.mjs` 一次性迁移+抓所有主图（83 个，12 投稿伞只有日期跳过）；`createRecord` 自动抓主图天气。批量重写 91 个 record.json。
- **v127（Claude Code）**：详情页图片排布推倒重写=固定高宽容器（矮则居中、高则顶端滚动）；放大居中改 `inset:0+margin:auto`（修放大动画奇怪）。本地 1280×860 验证通过。
- **v126（Claude Code）**：① 详情面板改**固定顶端**（`--focus-top`，后 v127 又改成固定高框居中/滚动）。② 天气联动第一版（记录级 `record.weather`+编辑器底栏按钮+详情页横轴，后 v128 全部改成按每张图）。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结。
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）。
6. **`交接.md` 每次重写只留最近 5 轮，并且必须署名本次任务由哪个智能体完成。**
7. 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
