# 交接 / Handoff（新窗口先读这个）

> 新会话先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，用大白话中文。截至 **2026-06-23，前端版本 v68**。

## 当前状态
- 项目正常跑：`npm start` → http://127.0.0.1:4173/ 。改了 `record.json` 要 `npm run records:build`；改了 `scripts/editor-api.mjs` 要**杀 node 重启**预览服务。
- **用户提的 16 项大改 + 后续一堆修正全部做完**（第六、七轮）。已提交到 main：`9d3fcfb`（前半）、`d361481`（C–F+修正）、本轮 docs/数据回填。
- 76 条记录是用户真实数据，**绝不能乱删**；改前先核对。

## 这两轮做了什么（概览）
- **双语**：日/英真切换，编辑器 title 和段落各两个框，`title`/`blocks[].text` = `{ja,en}`，旧字符串自动当日文且兼容。
- **地图**：定位兜底东京+境内才跳；首屏缩放动画；普通↔卫星+卫星文字开关；隐藏卫星道路白线；POI 放大(zoom≥17)才显示并淡化。
- **EXIF**：`scripts/exif.mjs` 解析 GPS+时间；新增/上传图自动读；`backfill-exif.mjs` 已回填旧图时间。
- **左栏/Archive/详情页/图片放大**：见 `修改记录.md` 第七轮条目。

## 可能的后续（用户暂未要求，先别擅自做）
- **双语只覆盖**了 About、Archive 标题、详情页 title/段落。导航 tab、排序按钮(時間/種類/場所)等其余界面**还没双语**，要不要扩展看用户。
- **POI 显示规则**：阈值在 `app.js` 的 `POI_REVEAL_ZOOM`(=17)、方向在 `POI_SHOW_WHEN_ZOOMED_IN`(=true)，一改即变。用户目前认可"放大才显示"。
- 卫星图的文字/道路样式受 Google 限制（道路线隐藏、POI 淡化都是近似），别期待像普通地图那样完全可控。

## 工作约定（已更新，详见 CLAUDE.md + memory）
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问"是否存档/开预览"**——合适检查点直接 `git commit`，预览链接需要时才给。
5. 改过文件就更新 `修改记录.md`（最上、绝对日期、简短）。
- 原型期只本地；存档=commit 到 main；**只有用户说"上线/同步"才 push**。
