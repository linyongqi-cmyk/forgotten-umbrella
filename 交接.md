# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-10，前端 v203**。
> **本次任务完成智能体：Claude Code。以后每次更新本文件都必须写明完成任务的智能体（codex / Claude Code 等）。**
> ⚠️ **存档规则已改（2026-07-05）**：只有「大改动且牵连多处」才自动 commit；其余每个任务做完**先问一句要不要存档**，别自作主张。见 CLAUDE.md 约定4。
> 🔧 **即时提醒**：`scripts/build-umbrellas.mjs`/`export-tables.mjs` 是 `records:build` 每次 spawn 的新进程，改了**不用杀 node**（前端改动只需页面 reload）；只有改 `editor-api.mjs`/`server.js` 才**必须杀 node 重启**（`pkill -f "server.js"` 后再 `preview_start`）。**表格展示逻辑在 `export-tables.mjs` 里是照抄 app.js 的格式化函数（时间/object/地址回退），改其一要两边同步。**
> 📌 **工作区未提交改动分两部分**：①用户自己录入的真实数据（editFlag/displayId、删 IMG_0975→其实还在 unknown、搬 IMG_8939 进 submission、几张新图）；②**近三轮我改的**：隐藏功能(前后端)+ 45 条 record.json 翻译 + 详情页地址换行/搜索(app.js/styles.css)+ 新增/重写 `scripts/export-tables.mjs` + `导出表格/*.csv` + build/package.json/文档。**这几轮是否 commit 待用户决定。**

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`；或先 `export PATH` 加上它那个 bin 目录）。
- 当前 **139 条记录**（`filebox/records/**/record.json`，真实数据，**绝不能乱删**）。其中一批是 `submission(pending)` 投稿伞，坐标/地址多为大概或缺失，用户在陆续补。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，4173 被占用时不要换随机端口看地图，先释放 4173（`pkill -f server.js`）。
- 本地浏览器可验证布局：注意**首屏是欢迎页**，`app-shell` 在 `body.is-entered` 前是 `visibility:hidden`——预览里要先 `document.body.classList.add('is-entered')` 才看得到内容。**编辑器 UI（含投稿收件箱）只在 `localhost/127.0.0.1` 出现**（`IS_LOCAL`），要先点编辑按钮或 `document.body.classList.add('edit-mode')` 才见到工具栏那几个按钮。

## 最近 5 轮
- **2026-07-10（本轮·下半场，Claude Code，未 commit=待用户决定，未 push）v202→v203 地址换行 + 搜索加 title + 表格改版**：用户给 6 条。**①详情页地址溢出图片**：根因 `formatAddressBreaks` 把空格全换不断行空格→只能逗号断行。用户选「只在逗号/of/for/the 前断，词组不拆」→改函数（占位标记 MARK/nbsp）+ CSS `.focus-overlay-info .focus-info{max-width:85%}`+`.focus-info-value p{overflow-wrap:anywhere}` 兜底。真实渲染验证：地址=容器85%、4 行、无横溢。**②展开列表搜索加 title**：`filteredUmbrellas` haystack 补 `item.title.ja/.en`（原漏）。验证 en"Abandonment"/ja"四重奏"都筛出 IMG_8056。**③表格改版（重写 `export-tables.mjs`，值全部照详情页）**：「文件名」→**「文件夹名」**（顺带修 bug：IMG_8056 的 media[0] 是插图 `________.png` 不是主图，旧脚本抓成 `________`）；**时间**用详情页格式（`formatDateTime` ja-JP `2026/07/02 13:08` / 投稿 `formatLooseDate` / approx 前缀）；「状态」→**「object」**（`buildObjectText`：数量+颜色+种类）；**显示地址空则回退 `formatLocationLevels` 层级地址**（两表）；fieldwork「显示名」移**最后**+有则 `★` 标记。把 app.js 的展示函数原样搬进脚本（改要两边一起）。四处版本 202→203。**验证**：两 CSV 12 列无错行；IMG_8056 修复、`★ IMG_8664`、空地址回退 `Kyoto, Kyoto City, Minami Ward`；node --check 全过、4173 HTTP 200。**改前端(app.js/styles.css/index.html/sw.js)+export-tables.mjs，未动真实数据。** **下一步**：用户看详情页地址换行观感（真机/正常浏览器打开标点详情，无头环境封面依赖 Google 地图没法完整截图）+ 表格；决定这几轮要不要 commit。
  - **追加小修**：清理了 19 个日文段落/对话块里 PDF 换行残留的半角空格（规则：只删「至少一边挨着假名/汉字/日文标点」的空格，英文/数字间空格+换行保留；英文正文无异常没动）。改了段落的记录同步重算 story、rebuild 了 umbrellas.json+表格。纯数据。**遗留**：个别块疑似 PDF 漏了字（不是空格，如 IMG_6637「植え込み?」、IMG_6774「同じ道?」），未擅自补，待用户确认。
- **2026-07-10（本轮·上半场，Claude Code，未 commit=待用户决定，未 push）批量翻译 + 总览表格导出**：接着上一轮隐藏功能，用户要①把手填的中文/日文（显示地址/标题/段落/对话）翻成英/日；②整理成 fieldwork+contributed 两张可同步表格。**翻译（47 处，已写回记录、@@ 清零）**：靠 `@@` 标记 + 「疑似中文放 ja 栏」检测出待翻内容——37 条中文**显示地址**→英文（按现有 `…, Ward, Kyoto` 格式 + 每条 `locationLevels` 层级；专有名词联网核对：Fushimi Inari Taisha / Kamitobaguchi Sta. / 御土居街名 Doi 读法等），9 个**标题**（中文 ja→正规日语+英语，或只补 en），2 段**段落/对话**（中→日+英）。⚠️3 个全汉字实为正规日语的标题（閉店清掃/初回記録/判断不能）是**误报**、没动——以后别把全汉字日语当中文覆盖。改段落的记录按编辑器同逻辑重算 `story`。写回用项目序列化器 `stringifyRecordWithComments`（先验证无损往返再批量）；rebuild 后网站地址栏中文→英文一并修好。**表格（新增 `scripts/export-tables.mjs`）**：生成 `导出表格/fieldwork.csv`(105) + `contributed.csv`(34)，列按用户指定、不载图、补充照片只列文件名；**隐藏标点也纳入并打「✓ 已隐藏」**；UTF-8 BOM+CRLF。挂到 `build-umbrellas.mjs` 末尾 → 编辑器每次保存自动重建时**表格自动同步**（真源仍是 record.json）；另加 `npm run records:export`。**决策**：日语「显示地点」用户要求暂不做，已写进 `未来可能需要注意的事.md`（要做需把 locationText 升级成双语字段，牵连结构/构建/前端/编辑器）；表格形式选了「自动同步 CSV」。**验证**：全库 `@@`=0；CSV 严格解析器校验 12 列无错行、翻译/多行段落/隐藏标记都正确；rebuild 137 条通过。**动了真实数据（45 条 record.json）+ 新增脚本，未动前端版本号（纯数据/脚本）。** **注意**：`filebox/hidden` 现有 2 条用户隐藏的真实记录(IMG_1467/IMG_8955)；`导出表格/*.csv` 未 gitignore、会随每次 build 变动(和 umbrellas.json 一样)。**下一步**：用户看表格是否满意 → 决定这两轮(隐藏功能+翻译+表格)要不要一起 commit。
- **v201→v202（Claude Code，未 commit=待用户决定，未 push）新增「隐藏标点」功能**：用户要在编辑模式加隐藏标点——但**不是只藏地图标点**，而是相当于删除+搬进隐藏文件夹、保留全部数据（因为标点牵连档案图片卡/统计表/展开列表/about 数据统计等很多地方）。跟用户确认后：恢复方式=**编辑器加「已隐藏」面板**、隐藏后**地图上完全不显示**（两个都选推荐项）。**实现（复用现有软删除那套 rename+原位恢复的思路）**：隐藏=把整条记录文件夹搬进 `filebox/hidden/<原分类>/<id>/`（保留分类子路径，恢复原位放回）；`build-umbrellas` 只扫 `filebox/records`→隐藏后自动从 umbrellas.json 消失→地图/档案/统计/列表/about 全部同步不显示，数据完整躺 hidden。**①编辑面板右上角**（× 左边）`eye-off` 隐藏按钮（`.editor-hide-record`，线宽绑 `var(--icon-stroke)`）→ 确认弹窗 → `hideCurrentRecord` → `/api/hide-record` → 关编辑器+重载 umbrellas+重绘。**②工具栏「已隐藏」面板按钮**（`.editor-hidden-btn`，eye-off，仅编辑模式）→ `openHiddenPanel` 弹窗（复用收件箱遮罩样式，单栏列表）→ `/api/hidden-list` 列出（缩略图+id+标题+分类·地址+「恢复显示」）→ 点恢复 `/api/unhide-record`+重载重绘。**③后端 `editor-api.mjs`**：`hiddenRoot` 常量 + `hideRecord`/`hiddenList`/`unhideRecord` 三函数 + 三路由；缩略图优先 legacyThumb(独立路径不搬)、否则同目录 `.thumb.webp`(存在才用)否则原图；恢复后 `removeEmptyDirsUpTo` 清 hidden 空分类夹。`filebox/hidden` **进仓库**（未 gitignore，数据要备份；线上静态站不引用它=对外不可见）。四处版本 201→202。**验证**（本地 4173，改后端后杀 node 重启）：curl 全链路 hide(139→138+搬走)→list(返回该条)→unhide(→139+原位+hidden清空)；浏览器实测两按钮渲染✓、`eye-off` 线宽吃 `--icon-stroke`(1.2px)✓、面板打开显示卡片+缩略图 HTTP 200✓、点「恢复显示」→列表清空+条数回 139✓。**只改前端(app.js/index.html/styles.css/sw.js)+后端(editor-api.mjs)，没动用户真实数据。** **下一步**：用户若满意→决定要不要 commit（只提代码文件，别捎带工作区里用户自己的数据编辑）。
- **v200→v201（codex，已 commit，未 push）项目清理/过期说明同步**：用户先要求“存档”，已把清理前状态提交为 `479d312 Archive current prototype state`。本轮只做低风险清理：删掉 `app.js` 里无人调用的旧函数 `renderItemText` / `formatListMeta`；删掉 `styles.css` 里无人引用的 `--detail-body-*` 旧变量；把天气图标、Archive 卡片角标、编辑器删除按钮、本地编辑工具栏这些还写死 `1.7` 的 SVG 线宽改成 `var(--icon-stroke, 1.7)`，继续跟“视觉设定”同步；同步更新 `AGENTS.md` / `CLAUDE.md` 里的过期版本号、记录数、依赖说明。四处版本 200→201。**验证**：旧函数/旧变量/写死线宽搜索为空，`node --check app.js`、`node --check scripts/editor-api.mjs`、`git diff --check`、v201 四处一致通过；本地服务器 `http://127.0.0.1:4173/` 返回 HTTP 200。**没碰真实数据/图片**。
- **v160→v161（本轮，Claude Code，已 commit，未 push）手机详情抽屉 5 小改 + 手势调节面板（任务6）**：用户一次给 7 条，提问澄清后：任务5 澄清为「地图+清晰圈整体缩小」、任务2 选方案A、任务6 开工做面板、任务7 震动放弃(iOS Safari 不支持 navigator.vibrate、用户是 iPhone)。**①删底部「Back to map / Scroll for more」（手机）**：`.focus-image-panel:not(.is-expanded) .focus-footer` 手机 `display:none`（退出靠下滑、看更多靠上滑；桌面/放大灯箱不变）。**②只有一张横屏/短内容详情页（方案A）**：新增 `sheetCanExpand()`=`!is-focus-short`；peek 已能看全时点把手/上拖都不展开到 full（只留下拖退出），避免拖出空白——改了把手 click 守卫 + `setupSheetGestures` 半开分支(短内容上拖 mode=native 不接管)。**③+④换提示**：去掉抽屉顶部灰色小横条 `.focus-sheet-grip`，改成向上小箭头 `.focus-sheet-hint`(svg)+`focus-sheet-hint-bob` 上浮动效(提示可上滑)，全屏档(is-sheet-full)/短内容(is-focus-short)时隐藏。**⑤清晰圈占比缩小（手机，圆+地图整体缩小）**：聚焦时地图再拉远约 1.2 倍（新增 `FOCUS_MAP_ZOOM_MOBILE=17.74`，`focusMapZoom()` 只手机用、桌面仍 18），清晰圈半径同比缩 0.83（手机 media query 把 `.focus-blur` 的 `--fb-radius` 改成 `calc(var(--fb-radius-n)*0.83)`，approx 同理）→ 圈里显示的真实内容不变、整体缩小、圆面积≈现在的 69%。**⑥手势调节面板（任务6，重点）**：**复用现成「视觉设定/theme.json」那套**（不另起炉灶），同一面板加一组「手势（手机详情抽屉）」7 滑块，桌面调→保存进 `data/theme.json`→手机打开线上就吃这套（正是用户要的跨端）。7 参数：`sheetFollow` 跟手系数、`sheetSnapRatio` 换档吸附阈值、`sheetExitRatio` 退出下滑距离(屏高比)、`sheetSnapMs` 吸附/回弹时长、`sheetExitMs` 退出滑出时长、`sheetInertia` 惯性(松手速度折算成多少 ms 额外位移,0=关)、`sheetExitFade` 退出最低透明度。实现：THEME_DEFAULTS/RANGES/GROUPS + 后端 THEME_CLAMP 各加 7 键；`applyTheme` 把数值存进模块级 `sheetTuning`(面板拖动预览也刷新它→桌面实时改手感)+写 CSS 变量 `--sheet-snap-ms`/`--sheet-exit-ms`(手机抽屉 height 吸附/退出过渡时长引用)；手势代码 `applySheetDrag`(乘跟手系数)/`releaseSheetDrag`(可调阈值+**新增惯性** effDy=(dy+vy×inertia)×follow)/`sheetExitDistance`/`animateSheetExit`(setTimeout 用 sheetExitMs) 全改读 `sheetTuning`；touchstart/touchmove 加瞬时速度采样(指数平滑)。四处版本 160→161。**验证**（本地 preview 375×812，地图端口未授权=正常不出图）：footer computed display=none✓、小横条已删、上滑箭头在✓、手机 `--fb-radius`=136×0.83≈113/138×0.83≈114.54(随 radiusN)✓、面板出现「手势」组 7 滑块✓、拖 sheetSnapMs→520 时 `--sheet-snap-ms` 实时变 520ms 读数「520ms/300ms」✓、theme.json 已含 7 键、applyTheme 从 theme.json 读到并写变量✓、node --check app.js/editor-api.mjs 过、无我引起报错。**改了前端(app.js/index.html/styles.css/sw.js 版本)+后端(editor-api.mjs THEME_CLAMP)+data/theme.json(加 7 键)。** **待真机（关键）**：任务 2/3/5/6 的实际观感全靠真机——无头浏览器打不开 Google 标点、驱动不了 touch，所以上滑箭头动效、短内容不展开、清晰圈+地图缩小比例、以及**手势跟手/惯性/回弹的手感**都要在真 iPhone 上看。任务5 的 1.2 倍/0.83、任务6 七个参数默认值都是估算，**大概率要用户在桌面面板拖着调、存了到手机看**。**下一步**：用户真机试→用「手势」面板调到满意→保存(记得先杀 node 重启)。
## ⏳ 待办
0. **站内自建投稿表单**：已挪进 `未来可能需要注意的事.md`（推荐 Google Apps Script Web 应用当收件端、保留现有管线只换前门）。用户想做但暂缓，等点头。
1. **投稿全流程只差最后一下**：表单现有 1 条真实测试投稿。收件箱列表(月份组/id 标题)/照片 EXIF/只读信息/「导入并编辑」都已验；**只剩用户亲手点一次「导入并编辑」**确认：生成 `submission(pending)` 草稿→自动打开编辑器→在编辑器里补地址级联/伞属性/段落/四勾选框/拖动定坐标；且非 HEIC 图会自动生成三档尺寸(已单测证明)。（这条测试稿一旦导入会标记已导入、不再出现在收件箱；若之后删掉标点，收件箱会自动重新显示可导入。）
2. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
3. **3 个历史未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664（remarks 有记）。
4. **12 个投稿伞没抓到天气**：因为它们的时间**只有日期没有钟点**（如「2024.10」）。等用户把时间补到「年月日 时:分」后，在编辑器点主图「获取天气」即可。
5. **Open-Meteo 档案对太近日期有几天延迟**：真机若某新标点抓不到天气，多半是日期太近，等几天或改用 forecast 接口的 `past_days`。
6. **未来想法**见 `未来可能需要注意的事.md`：上传自动转 HEIC。（天气图标 v153 已换 Lucide + 用户手绘小雪，此项已做。）
7. **视频裁剪未做**。

## 关键子系统（速查）
- **投稿系统（v145/v146，本轮新增）**：真源是用户的 **Google 表单**（含必填上传照片题→Google 强制投稿者登录）。① 前端入口：导航 `⬆ Submit` 标签 → `#submit-view` 内嵌表单 iframe（`app.js` tab 处理里懒加载 `data-src`）。② 数据搬运（方法 B，只读连 Google，**只本机**）：`scripts/google-auth.mjs`（OAuth 桌面授权，`scripts/google-credentials.json` 密钥 + `.google-token.json` 令牌，都 gitignore）+ `scripts/submissions-core.mjs`（**唯一一份核心逻辑**：认列关键词、抽 Drive 文件 id、下载照片、`buildBlocks`、生成 record、主图 id 去重存 `.submissions-state.json`）。③ 两个入口共用核心：命令行 `npm run submissions:pull`（`pull-submissions.mjs` 薄壳，支持 `--dry-run`）；编辑器**收件箱**（工具栏信封按钮 `#editor-inbox-btn` → `.editor-inbox-modal`，`app.js` 的 `openInbox/loadInbox/openInboxDetail/doInboxImport`）。④ 后端 3 接口（`editor-api.mjs`：`/api/submissions/list|photo|import`，photo 返回 base64/HEIC 标记，import 生成 `submission(pending)` 标点并 rebuild）。⑤ 表格 ID 存 `scripts/submissions.config.json`。配置文档 `投稿导入-使用说明.md`。**改 editor-api/core 后要杀 node 重启。**
- **详情面板排布（v127 推倒重写，v129 微调）**：`.focus-image-panel` 固定高宽框（`overflow:hidden`）；里面 `.focus-scroll`(flex 纵向滚动，`flex:0 1 auto`)+`.focus-stage`。短内容 footer(Back to map)贴内容下、长文章 footer 固定面板底。放大灯箱 `.is-expanded` 用 `inset:0+margin:auto`；切图 FLIP 用纯 `scale()`（`flipExpandedPanel`）。**窄屏(≤820px)底部抽屉（v160 手势重写）**：手势逻辑全在 `setupSheetGestures`+`applySheetDrag`/`releaseSheetDrag`/`animateSheetExit`+`sheetMetrics`/`updateSheetMetrics`/`setSheetState`。两档 peek(50svh)↔full；peek 上拖升 full、full 顶部下拖回 peek、peek 大幅下拖退出(closeFocusMode)；拖动 `is-sheet-dragging` 关过渡跟手；拖/缩地图 `is-focus-map-active` 收抽屉+露「展开」胶囊 `#focus-expand`。聚焦标点手机在屏幕上方 25%（`FOCUS_MARKER_SCREEN.yMobile=0.25`）。
- **天气联动（v128 按每张图存）**：数据在 `media[].weather`(+`showWeather`)。主图=拍摄前 24h 逐时（画横轴，`renderFocusWeatherAxis`）；补充/细节=拍摄当时 1 点（单图例 `singleWeatherIconFor`）。主图横轴拼在 `.focus-overlay-info` 底部（放大页随覆盖信息一起隐藏）。抓取：编辑器每图「获取天气」→`/api/fetch-weather`。**weather+showWeather 有 4 处重建会丢，都已补保留**（`normalizeMedia`/`applyMediaMetadata`/`mergeRecordMediaWithFolder`/`build-umbrellas.mjs`）。`weather.mjs::fetchWeatherData`，Open-Meteo 免费无 key。图标 **11 种**（v153 全换 Lucide 线条 + 手绘小雪；`weatherCategory`/`WEATHER_ICON_INNER`/`weatherIconSvg`）：晴/夜/多云间晴昼夜/阴 + **雨四级**(小雨 cloud-hail / 中雨 cloud-rain / 大雨 cloud-rain-wind / 暴雨 cloud-lightning，按 WMO code 分) + 小雪(手绘7点)/大雪(snowflake)。
- **图片分级提速（v139）**：每原图旁生成 `.thumb.webp`(400)+`.web.webp`(1280)，`build-umbrellas.mjs` 产 thumb/src/original 三路径；列表小图用 thumb、详情用 web、放大先 web 再后台下原图替换。`scripts/image-derivatives.mjs`。`record-utils.mjs` 扫描靠 `isDerivativeFile` 排除生成物。存量补齐 `npm run images:build`。
- **可调设定真源 `data/site-settings.json`（v138）**：聚焦模糊 9 参数 + 三张地图文字筛选。本机在面板调 → 防抖 POST `/api/save-site-settings` 自动写回文件 → push 即上线。**「本地调完自动上线」的通用套路，以后类似可调参数照此接入。**
- **视觉主题 `data/theme.json`（v153 起，v154 拆四类，v157 加两 gap）**：`iconStroke` + 详情正文 **4 类**（overlay 主图浮字 / dialogue 对话 / para 段落 / id ID）。段落间距(gap)按类分开：overlay/dialogue/para 各有一个 `*Gap`（overlayGap→INFORMATION 行 row-gap、dialogueGap→对话行间距、paraGap→**换行后段落之间**即 `.item-story` 下边距），id 类无 gap。共 15 个字段。前端 `loadTheme`+`applyTheme` 写 `:root` 变量（`--icon-stroke` + `--detail-{overlay,dialogue,para,id}-{size,line,weight}` + `--detail-{overlay,dialogue,para}-gap`，线上也吃）。本机专用「视觉设定」面板(工具栏 sliders 图标 `#theme-toggle`→`.theme-editor-overlay`)：按 5 组渲染滑块（`THEME_GROUPS`），每行「当前/默认」读数 + `↺` 单项恢复默认，拖动实时预览、保存 POST `/api/save-theme`（`THEME_CLAMP` 现 15 键范围钳制）。想让某图标也吃线宽，把它 CSS `stroke-width` 改成 `var(--icon-stroke, 默认)`（已接 nav/map-layer/panel-toggle/search/card-badge + 详情天气/放大镜/关联/信封/back/scroll）。想给某类文字改用哪套变量：见 `.focus-info-*`(overlay)/`.focus-dialogue`(dialogue)/`.item-story·.focus-text-block·.focus-note`(para)/`.focus-header .focus-title`(id)。旧 `--detail-title-*` 已删、`--detail-body-*` 保留但无引用。**改 editor-api 保存前要杀 node 重启。**
- **显示名 displayId（v141）**：record 字段，填了就替换页面上一切显示的 ID，内部匹配/文件夹/linkedId 仍用真实 `item.id`。显示统一走 `displayUmbrellaId()`；搜索 haystack 含 id+displayId（v144）。
- **根字体栈 lang 无关（v157，别改回去）**：`styles.css` `:root` 的 `font-family` 必须**拉丁字体在前、日文字体(Hiragino/Yu Gothic/Noto Sans JP)兜底、最后才 `sans-serif`**。原因：`sans-serif` 这个通用关键字用哪套字体由 `<html lang>` 决定，切语言(ja/en)时英文字号会忽变；把拉丁字体显式提前=英文永远同一套字体渲染，从根上关掉 lang 依赖。以后加字体或调栈，务必保持「拉丁在前」。
- **地图标点**（`renderMapMarkers`/`markerZIndex`）：原地更新 + distinct zIndex + `optimized:true`，不整批销毁重建。再点已聚焦标点走 `selectUmbrella` 的 `reFocusingSame` 分支（不回顶）。
- **编辑器三栏**：预览栏+修改记录栏(软删除可撤回，`filebox/.trash`)+编辑栏，`.panel-resizer` 可拖、存 `fu-panel-sizes`。**图片裁剪** `media[].crop` 非破坏性。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结。
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）。
6. **`交接.md` 每次重写只留最近 5 轮，并且必须署名本次任务由哪个智能体完成。**
7. 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
