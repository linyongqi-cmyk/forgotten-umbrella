# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-02，前端 v128**。
> **本次任务完成智能体：Claude Code。以后每次更新本文件都必须写明完成任务的智能体（codex / Claude Code 等）。**

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **95 条记录**（真实数据，**绝不能乱删**）。本轮我把 91 个 record.json 重新序列化（加了天气字段+统一格式，用户原有编辑都原样保留）。仍有一批用户在补的投稿伞 `record.json`。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，4173 被占用时不要换随机端口看地图，先释放 4173。
- 本地浏览器可验证布局：注意**首屏是欢迎页**，`app-shell` 在 `body.is-entered` 前是 `visibility:hidden`——预览里要先 `document.body.classList.add('is-entered')` 才看得到内容；点标点没有全局函数，可临时在 `selectUmbrella` 处挂 `window.__fu` 调试钩子（记得提交前删）。

## ⏳ 待办
1. **v128 真机复查**（本轮改动，真机点一下确认）：
   - ① **详情页图片排布**：短内容（如 IMG_6859）应上下**居中**在固定高的框里；长图（IMG_8755）应**顶端对齐可往下滚**；**点图放大**应干净地淡入到屏幕正中、**不再从停靠位滑过去**（问题B/C/D）。
   - ② **主图天气横轴**：在**主图正下方**（不再盖在图上），中间只有天气图例、两端只有 `-24`/`now`、**没有温度**；图例之间的连线在图例处**断开**（镂空处不露线）；变化太频繁时图例被合并、**不重叠**。
   - ③ **编辑器每张图**（编辑模式打开标点）：主图/补充/细节行都有「☁ 获取天气」按钮+「显示天气」勾选框；点补充/细节的「获取天气」会**先保存再抓**（抓当时 1 点）；放大补充/细节图时，右下角标题左边出现单个天气图例（要勾了「显示天气」）。
2. **12 个投稿伞没抓到天气**：因为它们的时间**只有日期没有钟点**（如「2024.10」「2025.04.27」），抓逐时天气需要具体到小时。等用户把这些点的时间补到「年月日 时:分」后，在编辑器点它主图的「获取天气」即可。
3. **天气图标风格**：现在是我手画的极简线条 SVG（`weatherIconSvg`，晴/多云/云/雾/雨/雪/雷）。若想换统一风格图标可后续替换。可考虑给档案卡/统计页也用天气。
4. **Open-Meteo 档案对太近日期有几天延迟**：真机若某新标点抓不到，多半是日期太近，等几天或改用 forecast 接口的 `past_days`。
5. **地图筛选默认值可固化**（`#map-layers`）：普通/卫星1/卫星2 各一套；真机调好后把值写进默认。
6. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
7. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664（remarks 有记）。
8. **投稿二期（Google 表单自动同步）未做**；**视频裁剪未做**。

## 关键子系统（速查）
- **详情面板排布（v127 推倒重写）**：`.focus-image-panel` 是**固定高宽的框**（`height:calc(100vh - --focus-top - --focus-bottom)`，`overflow:hidden`）；里面 `.focus-scroll`(flex 纵向滚动)+`.focus-stage`(`margin:auto 0`)——内容矮则**居中**、高则**顶端+内部滚动**。放大(灯箱)`.is-expanded` 用 `inset:0+margin:auto` 居中（不再 translate(-50%)），`transform` 只留给入场滑动，放大只做 200ms 淡入(`focus-expand-pop`)。窄屏(≤820px)仍是底部抽屉(`height:auto`)。
- **天气联动（v128，按每张图存）**：数据在 `media[].weather`（+`media[].showWeather`）。主图=拍摄前 24h 逐时（画横轴，放主图下方 `#focus-weather`）；补充/细节=只拍摄当时 1 点（放大时角标题旁单个图例）。抓取：编辑器每张图的「获取天气」→`/api/fetch-weather`（带 `mediaId`，坐标用记录级、时间用该图 photoTime→记录时间、按 role 定 24h/1点，`editor-api.mjs::fetchWeather`）。**注意 weather+showWeather 有 4 处会被重建丢掉**，都已补保留：前端 `normalizeMedia`、后端 `applyMediaMetadata`/`mergeRecordMediaWithFolder`(record-utils)、`build-umbrellas.mjs`。批量脚本 `scripts/fetch-all-weather.mjs`（迁移旧 record.weather→主图 + 抓所有主图 24h）。`weather.mjs::fetchWeatherData(lat,lon,time,{hoursBefore})`，Open-Meteo 免费档案无 key。
- **横轴渲染**：`renderFocusWeatherAxis(weather)`（`weatherChangePoints` 取变化点+头尾→贪心强制最小间距 13% 合并太近的→线段只画在图例空隙 `calc(...+13px)/(...-26px)` 实现遮挡）。`singleWeatherIconFor(media)`=补充图单图例。CSS `.focus-weather .fw-track/.fw-seg/.fw-mark/.fw-icon/.fw-axis`、`.focus-expanded-caption .fw-single`。
- **聚焦模糊 / 模糊度调整（v123）**：`#blur-adjust` 打开即实时预览；参数存 localStorage `fu-blur-settings`，CSS 变量 `:root --fb-*-n/-a`。
- **地图标点**（`renderMapMarkers`/`markerZIndex`）：原地更新 + distinct zIndex + `optimized:true`，不要整批销毁重建。
- **编辑器三栏 / 修改记录**：预览栏+修改记录栏+编辑栏绑定，有标点打开才一起出现；三条 `.panel-resizer` 可拖大小，存 `fu-panel-sizes`。
- **图片裁剪**：`media[].crop` 非破坏性；封面+灯箱共用 `#focus-image`。

## 最近 5 轮
- **v128（本轮，Claude Code）**：每张图加「☁获取天气」按钮+「显示天气」勾选框（图1/图3，按 mediaId 抓、主图24h/补充细节1点、删记录级按钮）；主图天气横轴重画=移到主图正下方、去温度、两端只 -24/now、中间只变化点图例、**强制最小间距合并太频繁的变化(2.3)**、连线和图例同粗且在图例处断开(遮挡 2.6)；补充/细节放大时角标题左边放单个当时图例(2.4)；修 4 处会丢天气字段的地方(normalizeMedia/applyMediaMetadata/mergeRecordMediaWithFolder/photoItem)。本地全部验证过。
- **v128 数据（本轮，Claude Code）**：天气从 `record.weather` 改成**按 media 存**；`weather.mjs` 加 `hoursBefore`、时间解析放宽认点格式；`scripts/fetch-all-weather.mjs` 一次性迁移+抓所有主图（抓到 83 个，12 个投稿伞只有日期没钟点跳过）；`createRecord` 自动抓主图天气。批量重写 91 个 record.json（加字段+统一格式，用户编辑都保留）。
- **v127（本轮，Claude Code）**：详情页图片排布推倒重写=固定高宽容器（矮则居中、高则顶端滚动，修问题B/C）；放大居中改 `inset:0+margin:auto`（修问题D放大动画奇怪）。本地 1280×860 验证：居中/顶端对齐/放大居中/关闭都正确。
- **v126（Claude Code）**：① 详情面板改**固定顶端**（`--focus-top`，后 v127 又改成固定高框居中/滚动）。② 天气联动第一版（记录级 `record.weather`+编辑器底栏按钮+详情页横轴，后 v128 全部改成按每张图）。
- **v125（Claude Code）**：① 详情面板改回垂直居中（后被 v127 取代）。② 地图页右上角导航加自适应文字阴影（卫星/深底深阴影、浅底浅光）。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结。
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）。
6. **`交接.md` 每次重写只留最近 5 轮，并且必须署名本次任务由哪个智能体完成。**
7. 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
