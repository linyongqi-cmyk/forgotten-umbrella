# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-02，前端 v121**。

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **95 条记录**（真实数据，**绝不能乱删**）。新增了 `IMG_8755`、`IMG_8766`（placement，未 commit）。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，所以 **preview_start 用默认 4173 端口时地图正常显示**。⚠️ 若 4173 被占用自动换了随机端口（如 62497）就会 RefererNotAllowed 看不到——那时先 `pkill -f "node server.js"` 释放 4173 再 preview_start。验证地图相关时可临时在 `init()` 末尾加 `window.__t={state,els,...}` 调试钩子（用完删掉、别提交）。

## ⏳ 待办
1. **地图筛选默认值可固化**（`#map-layers`，仅编辑模式）：现在**普通/卫星1/卫星2各一套**，13 类每类一排单选「自動/表示/淡化/隠す」+**两个缩放阈值(から/まで)**（v121 新增上限），存 localStorage。真机调好后把想要的值告诉我写进代码做正式默认。
2. **模糊标点视觉微调**（v121 重做：模糊层在 floatPane 把地图+所有标点一起柔化，选中的画一个清晰蓝复刻标点浮在上面）：模糊强度在 `.focus-blur-approx-layer` 的 `--blur-inner/--blur-outer` 调；复刻标点几何在 `FOCUS_PIN_SVG` + `.focus-blur-approx-pin`（若以后改 `markerIcon` 尺寸/锚点，这两处要同步）。真机看糊的程度是否合适。
3. **投稿伞坐标/地址层级多为大概或缺失**：需逐个补。`rednote_kankan(2)` 投稿时间要手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。另 `IMG_8665` 主图文件名应是 8664，`remarks` 里记了要改文件重传。
5. **投稿二期（Google 表单自动同步）未做**：用户说先记住。方案见 memory。
6. **视频裁剪未做**（图片裁剪已含自由比例，内容图+封面+灯箱都生效）。
7. **修改记录撤回的边角**：撤回「修改」是把改前 record.json 原样写回（走和保存同一条 merge 管线），对老格式记录会顺带把 record.json 规范化成新模板（字段补全/媒体按盘对齐）——数据字段能正确还原，属可接受行为。删除是软删除到 `filebox/.trash`（已 gitignore），撤回=移回原位。

## 关键子系统（速查，细节看代码/修改记录）
- **投稿 vs 自拍**：record 有 `submissionType`(contributed/own)、`submitter`、`submissionTime`、`locationApprox/timeApprox` 等。透传链 `record-utils.mjs`→`build-umbrellas.mjs`→`editor-api.mjs`（改后端要杀 node）。
- **地图 4 色标记**（`markerCategory`/`MARKER_COLORS`）：深红=有标题自拍/红=普通自拍/深绿=有故事投稿/绿=普通投稿。标点 `optimized:false`（各自DOM）。**v121：`renderMapMarkers` 改成原地更新**（复用已存在 marker，只删真正消失的），并按纬度给固定 `markerZIndex`（南在前、聚焦点最上）——解决重叠点缩放闪烁 + 打开标点下跳（不再整批销毁重建导致空一帧）。筛选 `#map-filter`（`state.markerFilter`，编辑模式也生效）。
- **模糊地址标点**（v121 重做）：模糊层 `.focus-blur-approx-layer` 注入地图 **`floatPane`（标点上方）**，backdrop-filter 把地图+所有标点一起柔化；选中的清晰=在层上再画像素级复刻蓝标点（`FOCUS_PIN_SVG`+`.focus-blur-approx-pin`，几何对齐 markerIcon）。移动/缩放→`is-focus-map-active` 淡出模糊层→下面真标点恢复正常。`updateMarkerIcons` 只负责把聚焦点变蓝+置顶。
- **地图相机动画**（`animateMarkerToFocus`/`zoomToDefaultAroundMarker`/`recenterFocusedMarker`）：起点一律用投影 `getLatLngScreenPoint`（和 `getCenterForMarkerScreenPoint` 同一视觉中心偏移约定），**不要用 DOM rect**。
- **详情页**（单栏）：A1(ID/标题)浮主图左上、A2+C(地点/时间/信息)浮左下；下面 D1+D2 按 blocks 原序单流。图片放大镜→灯箱（图可平移缩放、视频原生播放）。投稿伞隐藏 type/object/state。
- **图片裁剪**（v117 起，v120 加「自由」，非破坏性）：`media[].crop={aspect,scale,posX,posY,(ar)}`（null=原图）。内容图用 `cropStyles()`；封面+灯箱共用 `#focus-image` 用 `applyFocusCrop()`。**v121 修裁剪预览≠详情**：定比例裁剪的 `#focus-image` 加 `position:absolute;inset:0` 强制填满定高 frame，`object-fit:cover` 才按裁剪弹窗一样裁（之前 grid+固有比例 img 忽略 height:100% 退回自然高→不裁只截顶）。`.is-crop-free`(原图)不套此规则（frame 靠 img 撑高）。
- **修改记录 / 撤回**（v121 新，仅本机编辑模式）：`.editor-history` 面板在预览栏（收到 40vh）下方；`state.editHistory`（localStorage `fu-edit-history`）以标点为单位存最近 50 条新增/修改/删除，每条可**撤回**或**点行再编辑**。记录挂在 `saveEditor`(modify)/`createRecord` 流程/`deleteCurrentRecord`。撤回：modify→`/api/restore-record-data`(写回基线)、create→`/api/delete-record`、delete→`/api/restore-trashed`。后端 `save-record` 返回 `previous` 快照；`delete-record` 软删除到 `filebox/.trash/<id>__<ts>`(+`.meta.json` 存原相对路径，已 gitignore)。相关函数：`recordEditHistory`/`renderEditHistory`/`undoHistoryEntry`/`reeditHistoryEntry`。
- **编辑器**（仅 localhost，`setupEditor`/`openEditor`/`saveEditor`/`renderFlow`）：两列（字段+内容图文），左预览实时。CSS 坑：`display:flex/grid` 会盖 `[hidden]`，要配 `[hidden]{display:none}`。
- **地图信息筛选**（`#map-layers`，仅编辑模式，v120 重构 + v121 双阈值）：三套独立 `state.mapCategoryState={roadmap,sat1,sat2}`，`activeMapBaseKey()` 按 mapBase+mapLabels 选。卫星统一 hybrid+`SATELLITE_BASE_STYLES` 关全部文字，`categoryStyleRules()` 按当前套叠加。每类=一排 4 单选 + 两个缩放框(zoom 下限/zoomMax 上限，`data-map-zoom`/`data-map-zoom-max`)。存 localStorage `fu-map-layers`（旧格式自动迁移）。

## 最近几轮（详见 修改记录.md）
- **v121**（本轮）：① 模糊标点重做(floatPane 整体柔化+清晰复刻标点，不再丑；移动恢复正常)；② 重叠点闪烁+打开下跳→`renderMapMarkers` 原地更新+固定 zIndex；③ 筛选加上限阈值(から/まで)；④ 语言菜单只加粗去✓；⑤ 裁剪预览≠详情修复(img absolute 填满定高 frame)；⑥ 新增「修改记录」撤回/再编辑功能(预览栏收 40vh+字号调小；后端软删除+两个恢复接口)。
- **v120**（一批11项）：T1 打开下跳→投影起点；T2 模糊只选中清晰(本轮 v121 又整体重做)；T3 缩放闪烁→optimized:false；T4 裁剪加自由比例；T5/6/7 卫星地图信息重构(hybrid+base关全部文字，卫星1/2各一套)；T9 折叠钮纤细 chevron；T10 档案选中框白；T11 语言加✓(本轮 v121 去掉✓)。
- **v119**：① 地图筛选拆普通/卫星；② 打开详情强制回顶显主图；③ 地址层级不自动填子级、unknown 可存/回填。
- **v118**：① 修视频放大无法播放；② 裁剪应用到封面+灯箱。
- **v117**：图片裁剪功能（编辑器内容区，非破坏性，全链路透传）。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
