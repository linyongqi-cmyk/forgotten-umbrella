# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-02，前端 v125**。
> **本次任务完成智能体：Claude Code。以后每次更新本文件都必须写明完成任务的智能体（codex / Claude Code 等）。**

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **95 条记录**（真实数据，**绝不能乱删**）。有未 commit 的新记录 `IMG_8755/8766/0070` 等 + 一批用户在补的投稿伞 `record.json`（这些数据改动不是本轮 codex 做的，别误回滚）。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，4173 被占用时不要换随机端口看地图，先释放 4173。
- 本地浏览器可验证布局；以前的“无头页 RAF 不跑”限制仍要记住：动画逐帧类问题最终最好真机再看。

## ⏳ 待办
1. **T3 真机复查**：v125 详情面板已从「固定顶端」改回**垂直居中**（短横图不再贴顶）。本地预览端口非 4173、Google 地图没加载，没能点真实标点看效果；请真机点 8665/8755/8766 等横向主图，确认：短内容居中、长内容顶端稳定不下跳、切换记录不闪。
   - 若真机发现横图加载瞬间仍有下跳：根因多半是 `finalizeFocusImageLoad()`（app.js ~2365）去掉 `is-loading` 的时机早于图片高度落定；可考虑等 `#focus-image` load 后再多等一帧，或给未裁剪封面预留更准的比例（`reserveFocusCoverFrame` 目前无缓存时兜底 4/3）。
   - **天气联动（用户 T3 只是提问，未要求做）**：用户问能否用照片 EXIF 的时间+地点查当天/前几天天气显示在详情页。可行——record 已有 `photoTime`+`photoCoordinates`/`locationCoordinates`。方案见文末「天气联动可行性」。要做再做。
2. **地图筛选默认值可固化**（`#map-layers`）：普通/卫星1/卫星2 各一套。v124 已撤回无效的国名/都道府县/市町村细分，行政文字只保留一个整体控制；真机调好后把值写进默认。
3. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664（remarks 有记）。
5. **投稿二期（Google 表单自动同步）未做**；**视频裁剪未做**（图片裁剪已含自由比例，内容图+封面+灯箱都生效）。
6. **修改记录撤回边角**：撤回「修改」会把改前 `record.json` 走保存管线写回，老格式记录会顺带规范化；删除=软删到 `filebox/.trash`（gitignore），撤回=移回原位。

## 关键子系统（速查）
- **聚焦模糊 / 模糊度调整（v123）**：`#blur-adjust` 不再只限编辑模式。打开面板就实时预览；如果已经点开某个标点，面板调的是当前真实效果，不进入假预览。参数存在 localStorage `fu-blur-settings`，CSS 变量在 `:root --fb-*-n/-a`。
- **模糊标点圆环文字（v124）**：`#focus-approx-label` 是 SVG 圆形 textPath，文字方向已改成向外。调节面板有 `文字距离中心`（-600px~600px）和 `文字旋转角度`；负距离会翻到圆心另一侧。
- **地图文字控制（v124）**：`MAP_LAYER_CATEGORIES` 只保留可靠生效的大类。Google Maps 虽列出 administrative 的子类，但日本/当前底图实际不能稳定单独控制国名/都道府县/市町村，所以已撤回到「行政文字」整体控制。
- **下跳补强（v124）**：详情面板从垂直居中改成固定顶端位置，避免横图加载/高度变短时面板顶端往下挪；`finalizeFocusImageLoad()` 等两帧排版稳定后再显示面板。聚焦动画里的 marker hover 放大已恢复。
- **地图标点**（`renderMapMarkers`/`markerZIndex`）：仍是原地更新 + distinct zIndex + `optimized:true`，不要整批销毁重建。
- **编辑器三栏 / 修改记录**：预览栏+修改记录栏+编辑栏绑定，有标点打开才一起出现；三条 `.panel-resizer` 可拖大小，存 `fu-panel-sizes`。
- **图片裁剪**：`media[].crop` 非破坏性；封面+灯箱共用 `#focus-image`，定比例裁剪通过 `.is-cropped` 强制填满 frame。

## 最近 5 轮
- **v125（本轮，Claude Code）**：① 详情面板改回**垂直居中**（`.focus-image-panel` `top:50%`+`translate(…,-50%)`，`max-height` 收到 `calc(100vh - clamp(96px,12vh,140px))`），修「一张横图无段落时贴到最顶」；长内容被 max-height 卡住后居中=上下等边距，顶端稳定不下跳。② 地图页右上角导航加**自适应文字阴影**（卫星/深底=半透明深色 drop-shadow，明底显、深底隐；`is-roadmap` 浅底=浅色发光），解决未选中浅灰字糊在背景/模糊标点上。桌面 1280×800 已核对面板 centerY=视口中心、非选中 tab 带 text-shadow；地图未加载（预览端口非 4173），真机再确认。
- **v124（codex）**：① 圆环文字路径反向，模糊标点文字改为向外。② 横图详情下跳再修：详情面板固定顶端，不再随自身高度变化垂直居中；主图加载后等排版稳定再淡入。③ 恢复聚焦动画中的 marker hover 放大。④ 撤回不可靠的行政文字细分按钮，保留原本有效的「行政文字」整体控制。已验证 `node --check app.js`；浏览器 v124 实测 IMG_8766/IMG_8755 聚焦后面板 top 稳定 72px，地图文字面板只剩「行政文字」，圆环 SVG 路径已反向。
- **v123**：① 模糊度调整不再需要编辑模式，打开即实时预览；点开标点后也可直接调当前效果。② 模糊标点文字改成围绕圆心的圆环；新增文字距离中心(-600~600px)和旋转角度。③ 地图行政文字细分到国名/都道府县/市町村/町名・地区/街区地番（v124 已撤回，因为实际不可靠）。④ 8665/8755/8766 下跳补强：横向主图先锁比例框，图片加载后再显示；聚焦动画中禁 hover 放大。⑤ `交接.md` 署名规则写入 `CLAUDE.md` + 本文件。已验证 `node --check app.js`/`server.js`，浏览器 v123 中圆环文字滑块实时生效，IMG_8766 焦点模式可直接调。
- **v122**：聚焦模糊两套合一（共用 `.focus-blur` overlay + CSS 变量 + @property），删复刻标点；模糊度面板；标点 `optimized:true`；修改记录绑定编辑栏；三栏可拖拽；档案卡待改红点。
- **v121**：模糊标点整体柔化（后来 v122 重做）；重叠点不闪；筛选加上限阈值；语言只加粗；裁剪预览与详情一致；新增修改记录撤回/再编辑。

## 天气联动可行性（用户 T3 提问，未做）
- **可行**。record 里已有：`photoTime`（EXIF 拍摄时间）、`photoCoordinates`/`locationCoordinates`（经纬度）。有这两样就能查历史天气。
- 免费历史天气 API 推荐 **Open-Meteo**（`archive-api.open-meteo.com/v1/archive`，无需 key、免费、给经纬度+日期返回逐日/逐时的气温/降水/天气代码）。
- 两种做法：**A. 构建期抓一次**——在 `scripts/build-umbrellas.mjs` 里对每条记录调一次 API，把天气写进 `data/umbrellas.json`，前端只读缓存（线上是纯静态站，推荐这种，避免每次访问都打外部 API、也不受浏览器跨域限制）。**B. 前端实时 fetch**——详情页打开时才查（简单但每次都联网、且要处理失败/加载态）。
- 显示位置：详情页 INFORMATION 网格里加一行「weather / 当天☔/☀ + 气温」即可。
- 注意：投稿伞很多坐标/时间是大概或缺失（见待办 3），这些查不到或不准，要做好「无数据」兜底。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结。
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）。
6. **`交接.md` 每次重写只留最近 5 轮，并且必须署名本次任务由哪个智能体完成。**
7. 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
