# 交接 / Handoff（新窗口先读这个）

> 先读 `CLAUDE.md`（项目说明），再读本文件（现状+待办），细节查 `修改记录.md`。
> 用户是非技术小白，**一律用大白话中文**。当前 **2026-07-02，前端 v122**。

## 当前状态 / 怎么跑
- 跑：`npm start` → http://127.0.0.1:4173/ 。改 `record.json` 要 `npm run records:build`；改 `scripts/editor-api.mjs` 或 `server.js` 要**杀 node 重启**（`pkill -f "node server.js"` 后再 preview_start）。
- **Mac 跑脚本用绝对路径 node**：`/Users/eiki/.local/node-v24.18.0-darwin-arm64/bin/node`（shell 里没有 `npm`/`node`）。
- 当前 **95 条记录**（真实数据，**绝不能乱删**）。有未 commit 的新记录 `IMG_8755/8766/0070` 等 + 一批用户在补的投稿伞 record.json（都不是我改的，未提交）。
- **改前端就把版本号 +1**（`index.html` 两处 `?v=`、`app.js` 的 `sw.js?v=`、`sw.js` 的 CACHE_NAME，四处一致）。
- **地图能在本地渲染**：Google Key 授权了 `127.0.0.1:4173`，preview_start 用默认 4173 时地图正常。⚠️ 4173 被占用换了随机端口就 RefererNotAllowed 看不到——先 `pkill -f "node server.js"` 释放 4173 再 preview_start。
- ⚠️ **本地预览是无头页面（`document.visibilityState==="hidden"`）→ `requestAnimationFrame` 不跑**。所以**相机/标点动画类**（打开标点的飞入、模糊淡入）在这里**逐帧截不到**（截图能让页面短暂前台但 RAF 不持续）。要逐帧调试可临时 monkeypatch RAF 成 setTimeout。动画类改动最终以**真机确认**为准。验证时可临时在 `init()` 末尾加 `window.__t={state,els,selectUmbrella,openEditor,focusUmbrellaOnMap,...}` 钩子（用完删）。

## ⏳ 待办
1. **地图筛选默认值可固化**（`#map-layers`）：普通/卫星1/卫星2 各一套，13 类每类一排单选+两个缩放阈值(から/まで)，存 localStorage。真机调好后把值告诉我写进代码做正式默认。
2. **聚焦模糊参数微调**（v122 可视化了）：编辑模式左侧「聚焦模糊度」(`#blur-adjust`) 面板拖滑块调，实时预览。调满意后把数值告诉我写进 `:root` 的 `--fb-*-n`/`--fb-*-a` 做正式默认（现在默认：普通 6px/138px/34px；模糊 7px/200px/60px/veil0.3）。
3. **投稿伞坐标/地址层级多为大概或缺失**：逐个补。`rednote_kankan(2)` 投稿时间要手改。
4. **3 个未处理**：`Sliden_1`、`ygrons_1`（有行没照片）、`IMG_8583.jpg`（有照片没行）。`IMG_8665` 主图名应是 8664（remarks 有记）。
5. **投稿二期（Google 表单自动同步）未做**：先记住，方案见 memory。
6. **视频裁剪未做**（图片裁剪已含自由比例，内容图+封面+灯箱都生效）。
7. **T3 标点下跳真机复查**：已从根因改成 `optimized:true`（标点画在地图 canvas 上、和瓦片锁帧移动=不掉），本地无头页 RAF 不跑没法逐帧看，真机确认一下打开 8665/8755/8766 是否还掉、以及重叠点(aaa(1)/8680)缩放会不会又闪（理论上 distinct zIndex 已防）。
8. **修改记录撤回边角**：撤回「修改」把改前 record.json 走保存管线写回，对老格式记录会顺带规范化（字段补全/媒体按盘对齐）——数据字段能正确还原，可接受。删除=软删到 `filebox/.trash`（gitignore），撤回=移回原位。

## 关键子系统（速查，细节看代码/修改记录）
- **地图标点**（`renderMapMarkers`/`markerZIndex`）：**v122 起 `optimized:true`**（画在地图 canvas 上、和瓦片锁帧同步移动，修「打开标点下跳」）；**按纬度算 distinct `markerZIndex`**（南在前、聚焦点最上，防重叠点缩放 z 序打架闪烁）。**原地更新**（复用已存在 marker、只删真正消失的），别再整批销毁重建。4 色 `MARKER_COLORS`；聚焦点走 `updateMarkerIcons` 变蓝(isActive)。筛选 `#map-filter`（编辑模式也生效）。
- **聚焦模糊（v122 两套合一，重点！）**：普通标点和模糊标点**共用同一个 `.focus-blur` 全屏 overlay**（`position:fixed;inset:0;z-30`，在地图上方，`backdrop-filter:blur()` 柔化地图+其他标点，径向 `mask` 留出清晰/白雾圈让选中的标点透出）。**没有** floatPane 层、**没有**复刻标点（删了 → 没有 hover 穿透 bug）。差异只在 CSS 变量：`.focus-blur` 默认用 `--fb-*-n`（普通：blur/radius/feather + veil 0 全清）；`.is-blur-approx .focus-blur` 换成 `--fb-*-a`（模糊：更大 radius + 白 tint + `--fb-veil-a` 中心白雾）。`--fb-radius/veil/blur` 用 **`@property`** 注册 → 模式互切平滑。移动/缩放地图 `is-focus-map-active` 让 overlay 淡出→全恢复清晰。`focusUmbrellaOnMap` 只 toggle `is-blur-approx` 类 + `updateMarkerIcons`。
- **模糊度调整器（v122 T1，`#blur-adjust`，仅编辑模式）**：左侧按钮开面板，7 滑块（普通/模糊各 模糊强度/半径/羽化 + 模糊的中心白雾浓度）→ `applyBlurSettings()` 写 `:root` 的 CSS 变量、存 localStorage `fu-blur-settings`。「预览」`startBlurPreview(kind)` 加 `is-blur-preview` 类直接显示 overlay（不进详情页，圈心挪 58vw 避开面板；`.is-blur-preview .map-layer-tools{z-index:40}` 让面板浮在 overlay 上可拖）。默认值在 `:root --fb-*-n/-a` + `BLUR_PARAMS` 的 def。
- **相机动画**（`animateMarkerToFocus` 等）：起点用投影 `getLatLngScreenPoint`（和 `getCenterForMarkerScreenPoint` 同一视觉中心偏移约定），**不要用 DOM rect**。轨迹是 lerp(起点→焦点目标 23vw/50vh)，数学上单调无跳；跳是渲染层的事（已用 optimized 修）。
- **编辑器三栏 + 可拖拽（v122 T4）**：预览栏(`.editor-preview` 左上，`--preview-split` 高)+修改记录栏(`.editor-history` 左下)+编辑栏(`.editor-drawer` 右，`--editor-w` 宽)。**绑定**：`syncEditHistory` 只在 `editMode && editingId` 显示，`openEditor`/`closeEditor` 各调一次 → 三栏一起显隐。**拖条** `.panel-resizer`（`setupPanelResizers`）：resizer-col(左列宽 `--preview-w`)/resizer-split(预览记录分界 `--preview-split`)/resizer-editor(编辑栏宽 `--editor-w`)，只在 `body.editor-open` 出现，存 localStorage `fu-panel-sizes`。预览内容字号已整体调小（`.editor-preview` 里覆写 `--detail-*-size` + `.item-story`/`.focus-dialogue`）。
- **修改记录 / 撤回**（v122 起绑定编辑栏）：`state.editHistory`（localStorage `fu-edit-history`）以标点为单位存最近 50 条新增/修改/删除，每条可**撤回**或**点行再编辑**。撤回：modify→`/api/restore-record-data`、create→`/api/delete-record`、delete→`/api/restore-trashed`。后端 `save-record` 返回 `previous` 快照；`delete-record` 软删到 `filebox/.trash/<id>__<ts>`(+`.meta.json`)。
- **档案卡待改红点（v122 T5）**：`renderPhotoCard` 对有 `editFlag` 的记录左上角加 `.card-flag-dot`（红点），**只在编辑模式显示**（和地图标点待改变色同规则）。
- **图片裁剪**（v117~，非破坏性）：`media[].crop`。封面+灯箱共用 `#focus-image`，定比例裁剪的 img 用 `position:absolute;inset:0` 填满定高 frame 才裁对（v121 修）。
- **地图信息筛选**（`#map-layers`，仅编辑模式）：三套 `mapCategoryState={roadmap,sat1,sat2}`；卫星统一 hybrid+`SATELLITE_BASE_STYLES` 关全部文字；每类一排 4 单选 + 两个缩放框(zoom/zoomMax，v121)。

## 最近几轮（详见 修改记录.md）
- **v122**（本轮）：① 聚焦模糊两套合一(共用 `.focus-blur` overlay+CSS 变量+@property 平滑，删复刻标点=修 hover bug)；② 模糊度可视化调整面板(`#blur-adjust`,滑块+预览+localStorage)；③ 标点下跳根因修=`optimized:true`+distinct zIndex；④ 修改记录绑定编辑栏(一起显隐)+三栏可拖拽改大小；⑤ 档案卡待改红点。
- **v121**：① 模糊标点整体柔化(floatPane+复刻标点，本轮 v122 又整个换掉)；② 重叠闪烁+打开下跳→原地更新+固定 zIndex(本轮 optimized 进一步)；③ 筛选加上限阈值；④ 语言只加粗去✓；⑤ 裁剪预览≠详情修复；⑥ 新增修改记录撤回/再编辑(后端软删除+restore 接口)。
- **v120**（一批11项）：T1 下跳→投影起点；T2 模糊只选中清晰；T3 缩放闪烁→optimized:false(本轮 v122 又改回 true)；T4 裁剪自由比例；T5/6/7 卫星地图信息重构；T9/10/11 档案+语言。
- **v119**：地图筛选拆普通/卫星；打开详情回顶；地址层级修复。
- **v118**：修视频放大无法播放；裁剪应用到封面+灯箱。

## 工作约定
1. 动手前先确认+反思；2. 挖隐藏需求；3. 任务后给小结；
4. **不用每次问「是否存档/开预览」**——检查点直接 commit，预览链接需要时才给。
5. 改文件就更新 `修改记录.md`（最上、绝对日期、简短）；`交接.md` 每次重写只留最近 5 轮。
- 原型期只本地；存档=commit 到 main；**只有用户说「上线/同步」才 push**（push 前先 `git fetch`）。
