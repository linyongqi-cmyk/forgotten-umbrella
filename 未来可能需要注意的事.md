# 未来可能需要注意的事 / Future Maybe

> 这里记「现在不用做、但以后可能需要」的想法和坑。想到就往下加一条，绝对日期，写清「是什么 / 为什么以后可能要 / 真要做时大概怎么做」。做掉了就把那条删掉或标「已做」。

---

## 站内自建投稿表单（不跳转、完全在本站投稿）

- **记录日期**：2026-07-04（Claude Code）
- **是什么**：现在投稿走 Google 表单——点导航 Submit 会新标签打开 Google 表单填写。用户希望以后能**完全在本站填完投稿、不跳转**。
- **为什么现在没做**：Google 表单有「必填上传照片」题 → Google 强制投稿人登录、且禁止在别人网站的 iframe 里登录，所以内嵌表单没法 100% 站内完成（之前 v145 内嵌过、体验不好，已在本轮改回新标签跳转）。
- **真要做时大概怎么做（推荐路线，改动最小）**：**保留现有「读 Google 表格 + Drive → 编辑器收件箱」这条管线不动**，只换「前门」：
  - 自己做一个站内投稿表单（HTML）；照片上传交给一个 **Google Apps Script Web 应用**当收件端（Apps Script 以你的身份运行，投稿人**无需登录**，照片仍写进你的 Google Drive、答案仍写进同一张表格）。这样编辑器收件箱照常读、后端一行不用改。
  - 线上（GitHub Pages 纯静态）也能用，因为表单只是往 Apps Script 发一个 POST。
  - **要注意**：Apps Script 的部署/配额；无登录要防垃圾投稿（蜜罐字段 / 简单验证码）；照片大小上限（base64 传给 Apps Script 约几十 MB）。先拿一条小样验证「站内填→进 Drive→收件箱看得到」再放开。
  - 备选：Formspree / Netlify Forms（有照片大小限制）或自建 serverless + 对象存储（要额外基建）。

---

## 重新手绘天气图标（用户自己画好上传，AI 替换）

- **记录日期**：2026-07-04（Claude Code）
- **是什么**：天气图例（详情页主图那条 24h 横轴、以及补充/细节图的小图标）现在用的是代码里手写的极简线条 SVG。用户打算**自己用别的方式手绘一套**，画好后上传给 AI，AI 再把代码里的图标替换掉。
- **怎么交接（画好后走这个流程）**：
  - 图标要求：正方形，画布（viewBox）**24×24**；只用线条、不要填充；颜色别写死，用 `currentColor`（这样它会跟着文字颜色变，深色/浅色底都能用）。用户可以直接给 SVG 文件，或给清晰的图片让 AI 描成 SVG。
  - 替换位置：`app.js` 里 `weatherIconSvg(category)` 这个函数（约第 2740 行），以及它上面共用的几个常量 `WEATHER_CLOUD / WEATHER_SUN / WEATHER_MOON / WEATHER_SUN_SMALL / WEATHER_MOON_SMALL`（约 2733–2739 行）。改完记得前端四处版本号 +1。
- **一共要画哪些图标（10 个）**——括号里是它代表的天气 + 对应的气象代码（WMO code，用户不用管代码，知道是什么天气即可）：

  **白天 8 个：**
  1. **晴 clear**（大晴天，太阳）— code 0
  2. **多云间晴 partly**（有太阳也有云）— code 1、2
  3. **阴 cloudy**（全阴，也用于雾）— code 3、45、48
  4. **中雨 rain**（普通下雨；「小雨」已并到这里）— code 51、53、55、61、63、80、81
  5. **大雨 heavy-rain**（雨很大）— code 65、82
  6. **雷雨 thunder**（打雷 / 雷阵雨）— code 95、96、99
  7. **小雪 light-snow**（小雪 / 雨夹雪 / 中雪都归这个）— code 56、57、66、67、71、73、77、85
  8. **大雪 snow**（雪很大）— code 75、86

  **夜晚 2 个（只在「现在 now」这一刻会用到，把太阳换成月亮）：**
  9. **晴·夜 clear-night**（晴天的夜里，一弯月亮）
  10. **多云间晴·夜 partly-night**（有月亮也有云）

- **注意**：图标的「粗细分级」是和判断逻辑一一对应的（比如中雨/大雨/雷雨是三档、小雪/大雪是两档）。如果用户想合并或拆分某些图标（例如不再区分中雨大雨），要顺便改 `weatherCategory(code)`（约 2696 行）的分类映射，否则会出现「有这个天气但没有对应图标」的情况。真要动时先跟用户确认分几档。

---

## 自动转 HEIC（图片格式转换）

- **记录日期**：2026-07-04（Claude Code）
- **是什么**：用户用 iPhone 拍的照片常是 `.HEIC` 格式，浏览器打不开。目前（v143）的做法是**直接禁止上传 HEIC**，提示用户「请先转成 jpg」。以后可以改成**上传时后端自动把 HEIC 转成 jpg**，用户就不用自己转了。
- **为什么以后可能要**：如果用户或投稿者经常传 HEIC、每次手动转很麻烦，自动转会顺手很多。
- **目前为什么不做**：需要额外的转换库、增加后端复杂度；现在「禁止 + 提示转 jpg」最省事够用。
- **真要做时大概怎么做**：
  - 后端 `scripts/editor-api.mjs` 的 `uploadImage`/`createRecord` 里，检测到 `.heic/.heif` 就先转成 jpg 再存盘（文件名扩展名也改成 `.jpg`）。
  - 转换库候选：`heic-convert`（纯 JS，无系统依赖，简单）或 `sharp` + libheif（`sharp` 已经装了，但默认不一定带 HEIC 解码，需确认构建是否支持）。优先试 `heic-convert`。
  - 记得把前端 `app.js` 的格式白名单（`ALLOWED_IMAGE_EXTS`、`mediaFormatError`、两处 `<input accept>`）和后端 `assertImageFilename`/`assertMediaFilename` 一起放开 `.heic/.heif`，否则会在进转换之前就被拦下。
  - 转换后 ID = 文件名（去扩展名），注意别和已有记录重名。
