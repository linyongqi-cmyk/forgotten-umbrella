# 修改记录 / Change Log

> 这个文件记录每一次对项目做过的修改。**规则:每完成一个任务(改动过文件)后,就在最上面追加一条。**
> 时间用绝对日期。给非技术的自己看的,用大白话。

---

## 2026-06-22 · 订正（第3、7项理解有误）

- **第3项订正**：之前误删了"显示地址 Location"手填框；已**加回**该字段（数据保持清空）。
- **第7项订正**："坐标"被打成了"主标"——本意是把**坐标**移到**显示地址**前面；已调整字段顺序为：ID → 标题 → 时间 → **坐标 → 显示地址** → 地址层级 → 类型 …

## 2026-06-22 · 编辑器交互优化（9 项）

- **详情页改成整面板一起滚动**：封面图和文字一起随滚轮滑动，不再是只有下面一小块能滑；并隐藏了滚动条。
- **地址大改**：删掉"显示地址"手填框；清空全部记录的地址（回到 japan 默认）；都道府县下拉把「京都/东京/千叶/大阪」排到最前；选了都道府县后，市/区、区会自动选第一个。
- **数量**：去掉"未填"，默认 1；详情页里如果没选颜色/种类，object 文本就不显示。
- **状态**：选项排版改整齐（两列对齐）。
- **合并栏目**：原来的"图片"和"正文编排"两栏合并成**一个「内容」列表**——封面、其他照片、文字段落都在同一个列表里一起排序（★ 设为封面）。
- **标题**移到地址前面。
- **进入编辑模式时左边显示该标点的详情预览**，方便边改边看。
- **新功能**：看一个标点的详情页时点「编辑模式」，直接进入这个标点的编辑。

## 2026-06-22 · 接手项目 + 搭建本地编辑器

### 1. 环境与基础修复（提交 `eaec27d`）
- **安装 Node.js 24**(之前没装,项目跑不起来);确认无需 `npm install`。
- `.gitignore`:忽略约 400MB 的旧图备份 `filebox/choice/`,避免提交时撑爆仓库。
- `package.json` + `.nvmrc`:声明需要 Node 20+。
- `index.html`:去掉硬编码的"14 item"假数字。
- `.github/workflows/deploy.yml`:新增 GitHub Pages 自动部署(push 到 main 才触发,目前未启用)。
- `README.md`、`开发与上线流程.md`:补充说明。

### 2. 本地编辑器 · 基础（提交在 `eaec27d` 之后逐步加入）
- **后端**`scripts/editor-api.mjs` + `server.js`:新增只对本机生效的保存接口,改完写回 `record.json` 并自动重建数据库。
- **前端**`app.js` + `styles.css`:右上角「编辑模式」按钮(**只在本机出现,线上访客看不到**);点标记/卡片弹出编辑抽屉;地图标记可拖动改坐标;一键保存。
- `.claude/launch.json`:本地预览配置。

### 3. 图片管理 + 新增/删除标点（提交 `199ef76`）
- 图片:上传 / 删除 / 设为主图 / 上下排序;每张非主图可选角色(补充图片/细节/插图)并按角色显隐 ID/标题/时间。
- 新增标点(选图后落在地图中心,可拖动)、删除整条标点(二次确认)。
- 底层:`record.json` 的图片顺序与主图选择变为权威;media 新增 `title` 字段。

### 4. 结构化字段:数量 / 颜色种类 / 状态 + 智能默认（提交 `9b7ef26`）
- **智能默认/占位**:ID(只读)、标题、时间(占位显示照片时间)、显示地址(占位显示地址层级,空则 unknown)、类型(占位显示文件夹类别)——空白时真的走默认逻辑。
- **数量**下拉(1–5/unknown);**颜色和种类**按数量生成行(每行颜色+种类下拉,colored/patterned/other 弹说明框,"只填第一行其余继承");**状态**多选(other 互斥+自填)。
- **公开展示联动**:object 文本(如 `two blue long umbrella`)、状态按列表显示。
- 76 条记录统一升级到新数据结构。

### 5. 地址级联下拉（用你的 KEN_ALL_ROME Excel）
- 把你提供的 Excel 转成 `data/japan-areas.json`(全日本 47 都道府县、1192 市/区、1088 政令市区,日英双语)。
- 编辑器地址改为级联下拉:层级1(日本/其他/未知,**japan 默认不展示**)→ 都道府县 → 市/区 → 区,**可输入关键词筛选**,选项日英双语。
- 公开显示地址 = **全部选中层级拼接**(罗马音首字母大写,如 `Chiba Ken, Chiba Shi, Wakaba Ku`);留空时自动用层级,层级也空则 unknown。
- 旧数据(如 ["Tokyo","Shibuya"])匹配不上新罗马音时,自动归到「其他」并保留原文,不丢数据。
- 新增「维护本修改记录」的工作规则。

### 6. 故事多段落 + 图文穿插 + 详情页改造（Phase 4）
- 故事改为「正文编排」:可点「＋ 加段落」加多个段落;段落和**非主图照片**放在同一个列表里**一起排序**(主图只当封面)。
- **详情页重做**(点开标点的聚焦页):从"主图大、其余小缩略图"改成**可滚轮顺畅滑动的图文文章流**——封面图 → 元信息 → 段落与照片依次排列。
- 数据新增 `blocks` 字段(图文编排顺序);兼容旧记录(无 blocks 时自动用 story + 非主图照片生成)。
- 76 条记录统一加上 blocks 字段。
