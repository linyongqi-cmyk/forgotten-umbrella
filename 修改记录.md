# 修改记录 / Change Log

> 每完成一个任务在最上面加一行。绝对日期，尽量短。查历史用；平时不用读。

- **2026-07-07 地图列表选中态和手机点选落点 v178→v179（codex）**：按浏览器注释修地图左侧/底部列表。①列表项选中态取消白色大边框，改成条目右侧中间的小白点，桌面和手机共用。②地图列表的排序按钮去掉圆形胶囊背景，改成极简文字 + 当前项细下划线。③手机端从展开列表点单个标点时，不再把标点移动到屏幕正中，而是放到和详情页半开时一样的屏幕位置，避免被底部列表遮住；桌面端保持居中。四处版本号 178→179。**验证**：`node --check app.js`、`git diff --check` 通过，v179 四处一致。

- **2026-07-07 投稿 Place 细分 unknown/overseas + 手机列表动效 v177→v178（codex）**：按用户反馈继续修 Archive。①Contributed 的 Place 仍只按第一层分组，但现在保留原始地址层级用于判断：编辑器选 `unknown` 显示 `unknown`，非日本/other 显示 `overseas`；排序固定为正常地点在前、`overseas` 倒数第二、`unknown` 最后。②手机端 Contributed 的 Place 视觉重新接回 Fieldwork 同款层级行/缩略图/箭头，但数据只到第一层。③手机“展开列表”下拉关闭改成像详情抽屉：拖动时跟手下移并渐隐，松手后按距离/速度决定关闭或回弹。四处版本号 177→178。**验证**：`node --check app.js`、`git diff --check` 通过，v178 四处一致；排序脚本确认 `overseas` 在 `unknown` 前一位。

- **2026-07-07 投稿 Archive 地点回退/提示动画重置 v176→v177（codex）**：修正上一轮理解错误。Fieldwork 的 Place 继续保留 Kyoto→Kyoto City→Ward 层级；Contributed 的 Place 回退为只按第一层地点分组（如 Kyoto），不再展开到 city/ward。投稿伞编辑地址选 other 或非日本第一层时，Archive 统一显示为 `overseas`，并固定排在最底部。手机首次捏合提示不再写 `localStorage`，刷新页面后会重新出现，方便反复检查。四处版本号 176→177。**验证**：`node --check app.js`、`git diff --check` 通过，v177 四处一致；浏览器视觉待确认。

- **2026-07-06 手机首次标点缩放提示试版 v175→v176（codex）**：按用户想法尝试增加手机端提示动画。手机端第一次打开标点、地图聚焦动画完成后，在标点下方短暂显示 `Pinch to zoom map` 胶囊提示，带两个圆点的捏合动画；约 2.8 秒后自动消失，关闭详情也会消失。用 `localStorage` 记录每个浏览器只出现一次，避免反复打扰；桌面端不显示。四处版本号 175→176。**验证**：`node --check app.js`、`git diff --check` 通过，v176 四处一致；手机视觉待真机确认。

- **2026-07-06 ARCHIVE place 细化/定位点头动画/Back 文案 v174→v175（codex）**：按用户 1-5 做细节优化。①Fieldwork 的 ARCHIVE place 模式去掉顶部地址筛选行（`all / Kyoto...`），place 不再被 prefecture 子筛选过滤；type 模式筛选保留。②投稿伞 Archive 的 `Location` 按钮文案改为 `Place`，并在 location/place 模式复用同一套地点层级展示；手机端也用层级行 + 小预览。③地点层级缩略图改成稳定随机 3 张；父层会避开子层已经使用的预览图，减少 Kyoto 与 Kyoto City 同时展开时缩略图重复。④右下定位动画从持续旋转改成 0.42s 顺时针点头再回正；反馈 token 仍保留，重复点击会重新播。⑤手机 full 档 `Back to map` 加回文字，文字作为图标右侧标签显示，不再参与左上图标位置计算。四处版本号 174→175。**验证**：`node --check app.js`、`git diff --check` 通过，v175 四处一致。

- **2026-07-06 手机 ARCHIVE place 试版 v173→v174（codex）**：按讨论尝试改手机端 ARCHIVE 的 place 排列，桌面端保持原样。手机 place 不再默认把所有层级和图片卡全摊开，改成层级行：名称 + 条数 + 3 张小预览 + 箭头；点击层级行才展开下一层，最底层才显示 3 列图片网格。新增 `archiveOpenPlaceGroups` 单独记录手机 place 展开状态，不复用桌面折叠状态；place 分组 key 改成带父层路径，避免同名城市/区展开状态串层。四处版本号 173→174。**验证**：`node --check app.js`、`git diff --check` 通过，v174 四处一致；手机视觉待真机确认。

- **2026-07-06 定位按钮动画重启 v172→v173（codex）**：修正右下角“回到当前位置”按钮动画只触发一次的问题。定位反馈改成每次点击先移除 `.is-locating`、强制 reflow、再重新加回，确保 CSS 旋转动画从头播放；并用 token 防止上一次点击的结束定时器把下一次动画提前关掉。动画至少保留一轮 900ms，桌面/手机共用。四处版本号 172→173。**验证**：`node --check app.js`、`git diff --check` 通过，v173 四处一致。

- **2026-07-06 手机地图 full 锁定/列表下拉/ABOUT 文案 v171→v172（codex）**：按用户手机端反馈修 3 点，`Back to map` 文字与 ARCHIVE 排列先讨论不改。①详情页进入 full 档时，手机端背景 Google Map 锁住手势（`gestureHandling:none`、`draggable:false`），同时地图 drag/zoom 的“收起详情看地图”逻辑在 full 档直接返回，避免误触背景收起详情；回到 peek/退出详情会恢复。②手机地图列表面板展开后，在列表滚动到顶部时继续向下拖超过约 70px 会收起列表；列表中间正常滚动不触发。③ABOUT 英文统计方块 `Recorded locations` 改短为 `Locations`，减少手机端和汉堡按钮重叠。四处版本号 171→172。**验证**：`node --check app.js`、`git diff --check` 通过，v172 四处一致。

- **2026-07-06 手机详情 full 档取消清晰圈 v170→v171（codex）**：修正上一轮清晰圈取消条件。原因是 v170 改成“主图顶部盖过整个圆顶”后，full 档虽然详情页到顶，但参与判断的主图框顶部可能因为把手/布局距离够不到圆顶，导致清晰圈不取消。现在手机端只要详情页进入 `.is-sheet-full` full 档，就直接取消清晰圈；非 full 拖动过程仍保留“主图盖住整圆”判断。四处版本号 170→171。**验证**：`node --check app.js`、`git diff --check` 通过，v171 四处一致。

- **2026-07-06 手机详情清晰圈和底部蒙版 v169→v170（codex）**：按用户确认只做 2-4，回弹字体闪烁先搁置不动。①底部透明蒙版改成固定节点：按“从底部往上”的透明度理解，底部 0%=100% 透明、5%=100% 透明、50%=60% 透明、100%=0% 透明；用 `--sheet-bottom-mask/-50/-5` 三个长度变量实现，继续可过渡。②手机端标点/清晰圈位置从屏幕 25% 下移到 30%。③详情上滑取消清晰圈的条件改晚：必须主图顶部盖过整个清晰圆的最上边，整圆被详情盖住后才取消清晰区。四处版本号 169→170。**验证**：`node --check app.js`、`git diff --check` 通过，v170 四处一致。

- **2026-07-06 手机详情返回按钮再修 v168→v169（codex）**：按用户真机图修 4 点。①`Back to map` 不再是文字大按钮，改成和左上「地图切换」同位置同大小的 48px 图标按钮，full 时淡入、原顶部按钮同步淡出。②底部透明蒙版重新出现的过渡从 180ms 拉长到 340ms，减少突然跳出的感觉。③半开下滑后松手回弹时，保持“取消 opacity 过渡”的保护到整个回弹结束，避免主图浮字闪一下。④手机地图页「展开列表」按钮补到和其它顶部按钮同一层级，避免被模糊层盖住。四处版本号 168→169。**验证**：`node --check app.js`、`git diff --check` 通过，v169 四处一致；本地 4173 返回 200 且 index 已引用 v169。**未完整验证**：内置浏览器自动刷新时卡住，手机视觉仍需真机确认。

- **2026-07-06 手机详情返回位置/菜单/淡出修正 v167→v168（codex）**：按用户真机图修 5 点。①`Back to map` 改成手机专用面板外按钮，不再被详情面板 transform 困住压在主图上；位置固定在图片外左上地图区域，随详情进入 full 淡入。②底部透明淡出改用可动画长度变量，滑到底部后消失、再向上滑重新出现时有过渡，不再突然跳出。③半开下滑退出又回弹时，临时取消面板 opacity 过渡，避免主图浮字跟着消失约 0.5s。④半开时地图切换/标点筛选/展开列表/汉堡仍显示，随着详情上滑按进度逐渐淡出，到 full 后不可点击，并显示 `Back to map`。⑤汉堡菜单和标点筛选互斥；打开后点击地图外、其他地图按钮、移动/缩放地图会自动收起。四处版本号 167→168。**验证**：`node --check app.js`、`git diff --check` 通过，v168 四处一致。**待真机看**：红框位置是否正好、淡出重现是否够顺、菜单自动收起手感。

- **2026-07-06 手机详情 X/返回/底部淡出/清晰圈 v166→v167（codex）**：按用户真机图继续修 4 点。①手机端加回右上角关闭按钮，使用 lucide X 路径，按钮固定在主图右上角，不再固定在详情面板外。②`Back to map` 不再半开时压在主图上，改成只有详情抽屉到 full 档时才固定显示在屏幕左上角。③底部透明淡出不只 full 生效：半开时范围约为抽屉高度 30%，拖到 full 时跟随当前高度收窄到约 1.45 行文字高度。④按“圈和地图一起缩小”的逻辑再缩小清晰圈：手机半径系数 0.83→0.664（现在的 80%），手机聚焦缩放 17.74→17.42（约等于地图比桌面拉远 1.5 倍）。四处版本号 166→167。**验证**：`node --check app.js`、`git diff --check` 通过，v167 四处一致；内置浏览器刷新后确认 `app.js?v=167`、`styles.css?v=167` 加载，X 按钮在 `.focus-image-frame` 内。**待真机看**：X 的位置、full 档 Back to map 出现时机、半开淡出范围是否刚好。

- **2026-07-06 手机详情按钮/渐变/双击修正 v165→v166（codex）**：按用户真机反馈先修明确问题。①手机 full 档不再生成顶部渐变/模糊蒙版，避免主图上方出现灰色重叠条；底部也不再用 `backdrop-filter` 灰条，改成滚动内容自身在底部约 1.45em 内透明淡出，减少边缘感。②手机端补 `touch-action: manipulation` + JS 双击按钮拦截，针对汉堡等按钮双击触发整页放大的问题。③去掉手机详情右上角 `×` 关闭按钮，复用桌面端同款 `Back to map`，在手机详情中固定到屏幕左上角；聚焦态隐藏左上地图工具，避免和返回按钮叠在一起。四处版本号 165→166。**验证**：`node --check app.js`、`git diff --check` 通过，v166 四处一致。**待真机看**：iPhone Safari 双击按钮是否完全不再整页放大、底部淡出是否还需要更长。

- **2026-07-06 手机详情讨论后修正 v164→v165（codex）**：按用户确认的方案修正 5 点。①清晰圈取消改成看“主图顶部是否已经盖过清晰圈底部”，不是抽屉一进入 full 就立刻取消，过渡会更晚、更顺。②全局防下拉/边缘手势只在详情抽屉内拦截，不再挡住地图左侧约 20% 的拖动。③手机 full 档顶部从约 84-120px 改成 48px，详情可读区域更大；抽屉升起遮住清晰圈后，顶部汉堡/图层/列表按钮一起隐藏。④上下滚动提示蒙版改成透明 `backdrop-filter` 模糊，不再叠黑色背景，边缘更容易和底图融合。⑤手机端补充/细节图右上角放大镜强制隐藏，配合手机放大功能取消。四处版本号 164→165。**验证**：`node --check app.js`、`git diff --check` 通过，v165 四处一致。**待真机看**：清晰圈取消时机、透明模糊蒙版是否达到预期、Safari 左边缘手势是否还会被系统拦截。

- **2026-07-06 手机详情关闭备用按钮 v163→v164（codex）**：按上次任务 9 的建议实现一个可回退的小改动：手机端详情抽屉右上角新增很淡的 `×` 关闭按钮（桌面不显示、放大灯箱不显示），点击等同下滑关闭/Back to map，会退出详情并恢复地图缩放；原来的下滑关闭和上滑提示都保留不变。四处版本号 163→164。**验证**：`node --check app.js`、`git diff --check` 通过，v164 四处一致。**待真机看**：按钮位置和透明度是否顺眼，若不喜欢可单独回退本次提交。

- **2026-07-06 手机详情二次修正 v162→v163（codex）**：按用户新反馈修 1-8。①普通地点手机抽屉上滑时不再一动就取消清晰圈，改成抽屉高度实际盖住清晰圈底部后才给 `is-focus-sheet-raised`、mask=none，过渡更顺。②手机端“模糊地点”打开详情后锁住地图交互（Google Map `gestureHandling:none` + `draggable:false`），不允许拖/缩地图，退出详情后恢复。③手机直线地址文字下移，离标点更远；桌面圆环文字/距离/旋转滑块继续保留。④手机详情面板内双指捏合直接 `preventDefault`，避免主图区域触发 Safari 整页缩放。⑥⑦重做滚动渐变：不再是纯黑渐变，而是带 `backdrop-filter: blur()` 的渐隐层；离开顶部时上方出现较高的模糊渐隐层，未到底部时底部出现低高度模糊渐隐层。⑦短内容详情（不能展开的横图等）修正“先下滑退出到一半再上滑会展开空白”的 bug：这类内容只允许下滑退出/回弹，不允许中途反向展开。⑧手机端直接禁用放大灯箱入口：主图、放大镜、正文图片在手机端都不再打开灯箱，手机放大镜按钮隐藏；桌面灯箱不变。四处版本号 162→163。**验证**：`node --check app.js`、`git diff --check` 通过，v163 四处一致。**待真机看**：Safari 双指缩放是否完全压住、模糊渐变观感、模糊地点锁地图手感。

- **2026-07-06 手机详情/模糊地点/灯箱一批修正 v161→v162（codex）**：按用户 11 条里的 1-8、10-11 先改（第 9 条关闭方案另讨论）。①手机详情抽屉上滑到 full 时，普通地点的清晰圈取消，背景整张地图保持模糊（新增 `is-focus-sheet-raised`，mask=none）；半开恢复清晰圈。②模糊地点在手机端不再触发“移动/缩放地图→收起详情→show umbrella”逻辑，避免为看地图而去掉模糊；普通地点仍保留此逻辑。③模糊地点文字只在手机端改成标点下方直线文字；桌面端保留圆环文字，模糊调节面板里的“文字距离/旋转”滑块也保留给桌面圆环用。④加 `overscroll-behavior:none`，并用 JS 尝试拦截左边缘下拉刷新/边缘横滑（Safari 系统手势不能保证 100% 禁掉）。⑤“Show detail”改成透明背景纯文字 `show umbrella`。⑥⑦手机详情 full 档滚动时加上下渐变蒙版：离开顶部显示上方较高渐变，未到底部显示底部低高度渐变，到底隐藏。⑧普通地点地图收起详情时隐藏右下定位/计数工具组。⑩手机放大灯箱缩略图从右侧竖排改到底部横排，过多可横向滑动。⑪放大灯箱图片补手机双指缩放（1~4 倍）。四处版本号 161→162。**验证**：`node --check app.js`、`git diff --check` 过；本地 375×812 预览验证普通详情打开、点箭头 full 后 blur mask=none、底部渐变出现、滚动后顶部渐变出现/底部到尾隐藏、普通地图移动收起时 `show umbrella` 透明背景且右下工具 opacity=0。**未完整验证**：模糊地点真机移动/缩放与双指缩放手感需 iPhone/Safari 看；浏览器验证中途被 127.0.0.1 策略拦住，后半靠代码检查。

- **2026-07-06 手机详情抽屉 5 小改 + 手势调节面板 v160→v161（Claude Code）**：用户 7 条。1-5 见下，**任务6 手势调节面板**：复用现成「视觉设定/theme.json」那套（不另起炉灶），在同一个面板加一组「手势（手机详情抽屉）」7 个滑块，桌面调、保存进 `data/theme.json`、手机打开线上就吃这套（正是用户要的跨端）。参数：`sheetFollow` 跟手系数(位移倍率)、`sheetSnapRatio` 换档吸附阈值、`sheetExitRatio` 退出下滑距离(屏高比)、`sheetSnapMs` 吸附/回弹时长、`sheetExitMs` 退出滑出时长、`sheetInertia` 惯性(把松手速度折算成多少 ms 额外位移,0=关)、`sheetExitFade` 退出最低透明度。实现：THEME_DEFAULTS/RANGES/GROUPS + 后端 THEME_CLAMP 各加这 7 键；`applyTheme` 里把数值存进模块级 `sheetTuning`（面板每次拖动预览也会刷新它→桌面拖滑块实时改手感）+ 写 CSS 变量 `--sheet-snap-ms`/`--sheet-exit-ms`（手机抽屉 height 吸附/退出过渡时长引用）；手势代码 `applySheetDrag`(乘跟手系数)/`releaseSheetDrag`(用可调阈值 + **新增惯性**:effDy=(dy+vy×inertia)×follow)/`sheetExitDistance`/`animateSheetExit`(setTimeout 用 sheetExitMs) 全改读 `sheetTuning`；touchstart/touchmove 加瞬时速度采样(指数平滑)。**任务7 震动放弃**（用户拍板：iOS Safari 不支持 navigator.vibrate，用户是 iPhone）。**验证**（本地 preview 375）：面板出现「手势」组 7 滑块✓、拖 sheetSnapMs→520 时 `--sheet-snap-ms` 实时变 520ms、读数「520ms/300ms」✓、sheetInertia 可拨 0✓、theme.json 已含 7 键、applyTheme 从 theme.json 读到并写 `--sheet-snap-ms:300ms`/`--sheet-exit-ms:260ms`✓、node --check app.js/editor-api.mjs 过、无我引起报错。**待真机**：惯性/跟手/回弹的实际手感、各滑块调完在真 iPhone 上的观感（无头浏览器驱动不了 touch、打不开 Google 标点）。**注意（给 codex/用户）**：改了 `scripts/editor-api.mjs`（THEME_CLAMP 加 7 键），**保存手势设定前要杀 node 重启**（动态 import 有缓存）：`pkill -f "node server.js"`。

- **2026-07-06 手机详情抽屉 4 小改 + 清晰圈缩小 v160→v161（Claude Code，属上面同一版号）**：用户 7 条里的 1/2/3/4/5。**①删底部「Back to map / Scroll for more」（手机）**：`.focus-image-panel:not(.is-expanded) .focus-footer` 手机端 `display:none`（退出靠下滑、看更多靠上滑，桌面/放大灯箱不受影响）。**③+④换提示（手机）**：去掉抽屉顶部灰色小横条 `.focus-sheet-grip`，改成一个向上小箭头 `.focus-sheet-hint`（svg）+ `focus-sheet-hint-bob` 上下浮动动效，提示「可向上滑看更多」；全屏档(is-sheet-full)或短内容(is-focus-short)时隐藏。**②只有一张横屏/短内容详情页（方案A）**：新增 `sheetCanExpand()`=`!is-focus-short`；内容在 peek 已能看全时，点把手/上拖都不展开到 full（只保留下拖退出），避免拖出一片空白——改了把手 click 守卫 + `setupSheetGestures` 半开分支（短内容上拖 mode=native 不接管）。**⑤清晰圈占比缩小（手机，圆+地图整体缩小）**：聚焦时地图再拉远约 1.2 倍（新增 `FOCUS_MAP_ZOOM_MOBILE=17.74`=18-log2(1.2)，`focusMapZoom()` 手机才用，桌面仍 18），同时清晰圈半径按同比例缩到 0.83（手机 media query 把 `.focus-blur` 的 `--fb-radius` 改成 `calc(var(--fb-radius-n)*0.83)`，approx 同理）；两者相乘=圈里显示的真实内容不变、整体缩小、能看到完整清晰圈，圆面积≈现在的 69%。四处版本号 160→161。**验证**（本地 preview 375×812，地图因端口未授权=正常不出图，无我引起的报错）：抽屉底部 footer computed display=none✓、小横条 grip 已删除、`.focus-sheet-hint` 箭头在（path M4 10l8-6 8 6）✓、手机 `--fb-radius` 实测 112.88px=136×0.83（面积≈69%）✓、app.js 加载无语法错。**待真机**：手势里 2/3 的实际观感（上滑箭头动效、短内容不展开、清晰圈+地图缩小后的比例）——无头浏览器打不开 Google 标点、驱动不了 touch，需真机确认。

- **2026-07-06 手机详情抽屉手势（推倒重写）v159→v160（Claude Code，未 commit=待用户确认）**：按用户上传的排版图重写手机端聚焦/详情交互。①**聚焦位置上移**：`FOCUS_MARKER_SCREEN.yMobile` 0.42→0.25——点标点后标点/清晰圆升到屏幕上方 25% 处，上方地图约占 50%、下方抽屉约占 50%（本地 375×812 实测 focusY=203px、抽屉 top≈47%/高 50%）；mobile CSS `--focus-y` 42vh→25vh。②**抽屉手势推倒重写**（`setupSheetGestures`/`applySheetDrag`/`releaseSheetDrag`/`animateSheetExit` + `sheetMetrics`/`updateSheetMetrics`/`setSheetState`）：两吸附档 peek(半开,50svh)↔full(顶到导航下)。peek 上拖→升 full；full 顶部(scrollTop≤0)下拖→回 peek；peek 下拖超过「较大」阈值(max(150px,0.22屏高)=179px)→退出聚焦(=Back to map 的 closeFocusMode{resetZoom})，不够则回弹。拖动时 `is-sheet-dragging` 关过渡 1:1 跟手、内联写 height/transform/opacity，松手清内联让 CSS 吸附。`--sheet-peek/--sheet-full` 由 JS 按视口写成 px（和拖动数值对齐），resize 时刷新。peek 档 `.focus-scroll` overflow:hidden，保证上拖不被内容滚动抢走。把手改「单击(没拖)才切档」。③**地图交互收抽屉+展开胶囊**：拖/缩地图触发 `is-focus-map-active`（原有逻辑）时，mobile 抽屉整体滑出屏底+淡出、露出新增「展开」胶囊 `#focus-expand`；点它 `setFocusBlurSuppressed(false)`+`recenterFocusedMarker()`+回 peek。胶囊全局 `display:none`、仅手机 map-active 显示（桌面 1280 实测 none）。四处版本号 159→160。**验证**（本地 preview 375×812，记录 IMG_0981，合成 touch 事件）：上拖→is-sheet-full=true✓、full 下拖→false✓、peek 远距下拖→触发 animateSheetExit(is-sheet-exiting)并调 closeFocusMode✓、map-active→胶囊 display:flex/panel opacity:0(translateY 115% 关过渡实测=466.9px)✓、点胶囊→map-active 移除+回 peek✓、桌面无影响✓、无报错。**待真机**：手势跟手感/惯性、退出与地图 resetZoom 动画在真浏览器的顺滑度（无头浏览器不推进离屏 CSS 过渡，动画类环节只验证了终态值正确）。

- **2026-07-06 3 个 UI 小修 + 手机详情抽屉第1阶段 v158→v159（Claude Code，未 commit=用户说先不存）**：①**展开列表按钮去圆底（桌面）**：`.panel-toggle`（地图侧栏「展开栏」按钮，52px 圆）之前只在手机端去了毛玻璃圆底，桌面还留着；桌面也改成透明无边框无阴影、白图标+drop-shadow（跟 map-layer-btn 一致），`.panel-toggle-left span svg` 加投影。②**Archive 卡片角标去黑方块底**：`.card-badge` 去掉 `rgba(17,21,20,.62)` 方块底+圆角+固定 26px 盒，只留白图标（16→18px）+双层 drop-shadow，深浅图都可读。③**「58 item」移到地址右边**：`.archive-group-header > div` 改 baseline 横排（原来标题下方一行），各层级分组标题通用。④**手机详情页改可拖动底部抽屉（第1阶段：结构/CSS/点击切档）**：详情面板(`.focus-image-panel`)手机(≤820)从「随内容撑高」改成两档吸附——默认低档 `--sheet-peek:56svh`（露封面+主要信息、地图焦点圆还看得见），加 `.is-sheet-full` 升到 `100svh-focus-top-28`；height 加 300ms 过渡；`.focus-scroll` 手机改 `flex:1 1 auto` 填满抽屉（footer 钉底）；新增顶部拖动把手 `.focus-sheet-handle`（灰色小横条，仅手机显示，放大灯箱时隐藏）；`openFocusMode` 每次开标点复位到低档，点把手 toggle `is-sheet-full`。**拖动手势(上滑全屏/下滑关闭/松手吸附)留第2阶段（需真机验证）；低档浮字可读性/焦点圆露出比例留第3阶段。** 五处版本号(含 favicon)158→159。**验证**（本地 preview）：①panel-toggle 背景透明/无边框圆角阴影/白图标+投影✓；②card-badge 背景 rgba(0,0,0,0)、只剩 18px 白图标✓（Archive 截图）；③「2026/07 7 item」横排、item 在标题右✓（截图）；④手机 375×812：低档高 455(56svh)、全屏 695、底部锚定 bottom 28、把手在顶(26px)可点、点一下 is-sheet-full false→true 切换、scroll flex 1/1/auto 填满✓；桌面 1280 无影响（把手 display:none、加不加 is-sheet-full 高度都 731、scroll flex 回 0/1/auto）✓。**待真机**：④手机上抽屉观感、后续拖动手势。

- **2026-07-05 手机端 6 条（间距/卡片/缩放/深色/对齐）v157→v158（Claude Code）**：①**Ja / En 间距 14.6→8px**：`.lang-sep` margin `-1px`→`-4.3px`（抵消 `.language-toggle` 的 flex gap 6px×2）。②**手机投稿/档案卡片文字重叠**：卡片一行 3 张、格子窄，长 id 不换行会溢出叠到相邻卡。手机(≤820)把 `.card-bar` 改竖排（id 上、title 下），`.card-id` 12px+`overflow-wrap:anywhere`、`.card-title` 11px+允许折行（去掉单行省略号）。③**折叠箭头被挤到月份下方**：手机(≤560)`.archive-group-header` 之前 `flex-direction:column`→改回 `align-items:center` 横排，箭头回到右边。④**禁止整页缩放**：`index.html` viewport 加 `maximum-scale=1, user-scalable=no`，手机双指捏合只缩地图不再误缩整页（Google 地图自有手势不受影响）。⑤**汉堡下拉栏适配深色**：手机 `.view-tabs` 及 `body.view-map .view-tabs` 的 `color-scheme:light`→`light dark`，浅色=白玻璃深字、深色=暗玻璃浅字（标点筛选面板本来就已自适应，无需改）。⑥**ABOUT 四方块顶部对齐汉堡**：手机 `.about-view` padding-top 112→27px（=topbar top14+图标居中13），方块上边线与右上汉堡图标顶齐平。四处版本号 157→158。**验证**（本地 preview 375×812 明/暗）：①量得 Ja→En=8.0px✓；②注入长 id `rednote_1290965abcdef` 折 2 行、宽 91<卡宽 95 不溢出✓；③档案头 flex row、箭头在右(x≈308)✓；⑤暗色下拉栏暗玻璃+浅字（截图✓）、浅色白玻璃✓；⑥方块 top=汉堡图标 top=27、无横向重叠(stats right320<汉堡 left328)✓。**待真机确认**：④双指捏合禁缩放需真 iPhone/Safari 验证（无头浏览器测不了手势）。

- **2026-07-05 又一批 8 条（字体根治/地图图标/间距等）v156→v157（Claude Code）**：用户报 8 条。**①全站 ja/en 字号不一致（根治）**：根因=根字体栈 `styles.css` 以 `sans-serif` 收尾，`sans-serif` 用哪套字体由 `<html lang>` 决定→lang=ja 时英文被日文字体的拉丁字形接管、显得更大更宽，切换语言全站字号忽变（上轮只给导航标签单独锁了拉丁字体，是局部补丁）。改法=把 `:root` 字体栈改成**拉丁字体在前、日文字体兜底**（`-apple-system…Arial, "Hiragino Sans"…"Noto Sans JP"…sans-serif`）——浏览器逐字形回退，英文永远命中拉丁字体（不管 lang 都同一套），只有假名/汉字落到日文字体。**②地图左下角圆形按钮去圆底**（桌面端，手机端上轮已去）：`.map-layer-btn`（切底图/筛选/图层/模糊调整）去掉毛玻璃底+边框+阴影，只留图标，改白描边+投影（跟右上定位按钮一致），选中态改主题色描边。**③Ja / En 间隔太大**：`.lang-opt` min-width 1.7em→1.5em，`.lang-sep` 左右 margin 1px→-1px。**④手机 ABOUT 四个方块移顶部**：手机 `.about-view` 改 flex 竖排（含 `.is-active` 同优先级覆盖 display:grid）、`.about-side` 用 `display:contents` 摊平、`about-stats` order:-1 置顶、copy 居中、legend 最下。**⑤手机 ARCHIVE 一行 3 张**：`.photo-grid` 手机断点 2 列/最窄屏 1 列→都改 3 列。**⑥段落间距理解纠正**：用户指的是「换行后段落之间」（如 8622 的 Day1→Day2），原来 `--detail-para-gap` 只作用在块外边距、段间写死 0.12em；改成 `.item-story` 下边距=该变量（末段不留），编辑器预览同步。**⑦对话/主图浮字也加「段落间距」**：新增 `overlayGap`(默认5)/`dialogueGap`(默认5) 两字段（THEME_DEFAULTS/RANGES/applyTheme 写 `--detail-overlay-gap`/`--detail-dialogue-gap`/THEME_GROUPS 两组各加一项/theme.json），对话行 `.focus-dialogue-line` 与 INFORMATION 行 `.focus-info` row-gap 改用对应变量。**⑧投稿伞排序加「投稿时间」**：新增 `groupContributedBySubmission`（按 submissionTime 月份分组）+ 工具栏「投稿日時/Submitted」按钮 + 模式接线（默认最新在前，asc 翻转）。四处版本号 156→157。**验证**（本地 preview，地图因端口未授权=正常）：①同段英文在 lang=ja/en 下宽度都 256.31px（根治✓）；②map-layer-btn 背景透明/无阴影（✓，截图左上角无圆底）；③Ja→En 间距 14.6px（原约 20px）；④手机 stats top112<copy323<legend1266 置顶✓；⑤手机 375 photo-grid=repeat(3)✓；⑥真实结构测试 para 间距随变量 4→24px、⑦对话 4→20px 变化✓，面板「主图浮字/对话」都出现「段落间距」（截图✓）；⑧点「投稿日時」按投稿月份分组 2026/07→06→05…最新在前✓、无 app 报错。**注**：后端 `editor-api.mjs` 的 `THEME_CLAMP` 白名单已补 `overlayGap`/`dialogueGap`（否则保存视觉设定会把这两项抹掉）——**保存前需杀 node 重启**（动态 import 有缓存）：`pkill -f "node server.js"`。**待真机确认**：①真 iPhone 上切换语言观感；②白图标在浅底地图可读性；⑥⑦需开某标点详情页拖滑块看实际观感。

- **2026-07-05 修上一轮 8 个 bug v155→v156（Claude Code）**：用户报 8 条。**①手机 Ja/En 右对齐**：根因=手机菜单里 `.language-switcher`(外层)和 `.language-toggle`(内层)都被加了 `padding:12px`，嵌套叠加→「En」比上面「Submit」多缩进 12px。改成外层 switcher `padding:0`+flex 撑满，内边距只交给里层 toggle。**②桌面切换抖动**：当前语言那段 `font-weight:400→700` 变粗使整段变宽、导航栏跳动。给 `.lang-opt` 固定 `min-width:1.7em`+居中，并去掉 `font-weight` 过渡。**③手机汉堡 ja/en 字号不一致**：根因=切换语言改 `<html lang>`，`lang=ja` 时导航里的固定英文被日文字体的拉丁字形接管→看起来更大。给 `.view-tabs .tab/.language-toggle` 锁纯拉丁字体栈（`-apple-system…`，无 CJK）。**④navigation 定位按钮移位（非新增）**：把 `#locate-me`(navigation 箭头)从左上横幅移到右下「139 item」上方；删掉上轮加的装饰性 `.reset-map-ico`；`.map-toolbar` 从单个按钮改成容器 div，内含 `.map-locate-btn`(定位)+`.map-reset-btn`(计数)两个透明按钮；定位图标样式改名 `.locate-ico`、转圈动画挂到 `.map-locate-btn.is-locating`。**⑤About 手机撤销上移**：`.about-view` 顶部留白 22px→112px（单列/2×2 统计等其余保留）。**⑥视觉设定加段落间距+折叠**：新增 `paraGap` 字段（THEME_DEFAULTS/RANGES/applyTheme 写 `--detail-para-gap`/THEME_GROUPS「段落」组/后端 THEME_CLAMP/theme.json 各处，默认 8px≈原 0.6em，范围 0-30），详情三个正文块 `.focus-note/.focus-text-block/.focus-dialogue` 的 margin 改用该变量；面板每个大项标题改成可点折叠头（chevron+`.theme-group-rows`+`is-collapsed`）。**⑦Ja/En**：index.html 里 JA→Ja、EN→En。**⑧标签页 favicon**：新建 `assets/icons/favicon.svg`(白底黑线 lucide umbrella)，`<link rel=icon>` 指向它（PWA 的 icon.svg 不动）。四处版本号 155→156。**验证**（本地 preview 55713，地图因换端口未授权=正常，无 app 报错、node --check 全过）：桌面切换 toggle 宽度 166.00px 不变/每段固定 27.2px（②✓）；手机「En」与「Submit」文字右边缘都在距面板右 21px（①✓）；导航 font-family 锁 `-apple-system…`、ja/en 下 font-size 均 16px（③✓）；`#locate-me` 已出横幅、在 reset 上方 top735<767、计数仍「139 item」（④✓）；About 手机 padding-top=112px（⑤✓）；面板 5 组、段落间距滑块拖到 20px 使 `--detail-para-gap` 8px→20px、点组标题 rows display grid→none 折叠（⑥截图✓）；Ja/En 文本（⑦✓）；favicon.svg 200+含 umbrella 路径（⑧✓）。**待真机确认**：③字号差异根因(日文字体接管)在真 iPhone 上消除需真机看；④右下白色定位图标在浅底地图可读性。**注**：改了 `editor-api.mjs`（加 paraGap 钳制），用户若有正在跑的 node 服务、保存视觉设定前要杀 node 重启。

- **2026-07-05 导航/毛玻璃/图标/统计/视觉设定/About 一批 14 条 v154→v155（Claude Code）**：用户一次给 14 条，分 4 阶段做完。**①存档规则改**：只有大改动(牵连多处)才自动 commit，其余每次做完问；已更新 CLAUDE.md 约定4 + memory。**②定位(item2)**：手机点定位没反应=用 http 局域网 IP 打开时 iOS Safari 直接拒绝且不弹权限(仅 https/localhost 可用)；`goToMyLocation` 加 `isSecureContext` 判断 + 失败轻提示(新 `flashMapToast` 底部浮出毛玻璃小条 `.map-toast`)。**③天气轴(item3)**：`renderFocusWeatherAxis` 线段两端让开 20→18px、图例最短距离 `MIN_GAP` 13→19%(改成常量 LET_OFF/MIN_GAP)，让窄轨上「晴↔多云间晴」中间线也看得见。**④语言(item4/14)**：把「Language」下拉换成「JA / EN」直接点击循环切换(只两语=toggle)、当前语言那段加粗+变实(`.lang-opt.is-active`)；删掉 `.language-menu` DOM/CSS/JS，`syncLanguageMenu` 改成更新两段加粗；手机菜单里 JA/EN 仍 flex-end 右对齐。**⑤描边阴影(item5)**：手机白毛玻璃菜单里连选中/hover 的 tab 也去掉 text-shadow(之前「Umbrella map」选中残留一层)。**⑥⑦毛玻璃(item6/7)**：`--panel` 透明度 0.72/0.68→0.55/0.5(更透明)，新增 `--glass-blur:26px`+`--glass-sat:1.4` 统一变量，全站弹窗 backdrop-filter 换成该变量并补 `-webkit-` 前缀(Safari)。**⑧统计表(item8)**：`html,body` 加 `text-size-adjust:100%` 关掉 iOS Safari 宽表自动放大导致的字号忽大忽小(图3)。**⑩展开图标(item10)**：`#toggle-list` 收起=open 图标、展开(aria-expanded=true)=close 图标(桌面 panel-left-open/close、手机 panel-bottom-open/close)，加两个 close svg + CSS 按屏宽和 aria 切换。**⑪计数(item11)**：右下 `#reset-map` 改纵向排——回到定位图标(locate-fixed)在上、「139 item」计数在下且用最轻字重(font-weight 200)。**⑨视觉设定(item9,编辑模式)**：`.theme-editor-overlay` 从覆盖式模态改成左侧贴边滑出面板(遮罩透明+pointer-events:none 让点击穿透到详情页，面板本身 pointer-events:auto、贴左全高 340px、毛玻璃)，能一边调一边看详情；只覆盖 .theme-editor，文案編集面板不变。**⑫⑬About 手机(item12/13)**：`.about-view` 与 `.archive-view` 拆开，手机版顶部留白 112→22px(第一屏标题和右上角汉堡同高)、单列、正文 17→15px/行距 1.9→1.75、标题 22→19px、四个统计块改 2×2、字号收紧。四处版本号 154→155。**验证**(本地 preview 1280 桌面 + 375 手机，无 console 报错、node --check 过)：语言点击 ja↔en 循环+localStorage 更新+当前段 weight700 另段 400(截图)；展开图标桌面/手机各自 open↔close 正确切换；计数 weight200+图标在上+纵排；About 手机 padTop22/单列/2×2 统计(截图确认标题与汉堡同高不重叠)；视觉设定面板左贴边 340×全高+blur26+遮罩透明穿透(截图)；手机菜单 JA/EN 右对齐+Umbrella map 无描边阴影+白毛玻璃 blur26(截图)。**待真机/真数据确认**：③天气轴需开某标点详情页看线段；②定位轻提示需真机 http 触发；⑧统计表字号需 iPhone Safari 打开确认；⑫⑬长英文标题是否与汉堡重叠需真机看。

- **2026-07-05 图标/布局细修一批 + 视觉设定面板拆四类 v153→v154（Claude Code）**：用户一次给 11 条。**图标（手机桌面通用）**：①展开栏按钮桌面用 panel-left-open、手机用 panel-bottom-open（HTML 放两个 svg，CSS ≤820 切换）。②Language 左边加 globe 图标（`.language-globe`，按钮改 inline-flex）。④天气轴图标与连接线间隙加大（`renderFocusWeatherAxis` 线段两端让开 13px→20px）。⑥关联点图标换 Lucide `link-2`。⑦让「图标线宽」也管到之前漏的：详情天气(主轴+图注单图)、封面/图片放大镜、关联点、投稿信封徽标、back to map、scroll for more——把这些硬编码 `stroke-width:2/1.7` 改成 `var(--icon-stroke,…)`。⑧模糊地址弧形标签文字前加「◌」(U+25CC 点线圆，等效 circle-dashed，能跟着弧线走)。**手机端**：③汉堡菜单 Language 右对齐、框收窄(min 220→184px、66vw→52vw)、改白色毛玻璃（对 `.view-tabs` 强制 `color-scheme:light` 让 `--panel` 取浅色版 + 去掉深色描边阴影）。⑪筛选按钮去掉红色圆底(is-active 背景/边框透明)、弹窗从「向右弹」改「向下弹」+ 加 `map-filter-drop` 下滑动画。**详情图注**：⑩视频时长文字从右上(`top:12px`)改回右下(`bottom:12px`)，不再压住放大镜。**编辑模式·视觉设定面板**：⑨每项数值显示「当前/默认」(如 1.2/1.8)、每行加「↺」单项恢复默认按钮。⑨.1 把「详情正文字号/行距」拆成 **4 类**分别可调（主图浮字/对话/段落/ID），每类可调 **字号·行距·字重(font-weight)**；ID 是新增项。CSS 新增 `--detail-{overlay,dialogue,para,id}-{size,line,weight}` 变量，各类元素改用对应变量；旧 `--detail-title-size/line` 删除（唯一使用者=`.focus-header .focus-title` 改读 id 变量）；`--detail-body-*` 保留但已无引用。app.js THEME 结构从 3 字段扩到 13、`applyTheme` 写 13 个 :root 变量、面板按组渲染；后端 `/api/save-theme` 的 THEME_CLAMP 同步扩到 13 键（范围钳制）。四处版本号 153→154。**验证**（本地 preview 127.0.0.1，1280 桌面 + 375 手机）：无 console 报错、node --check 全过；桌面展开图标=左展开版、手机=底展开版；globe 渲染 15px；手机菜单量得 width 184 / color-scheme light / bg rgba(255,255,255,0.72) / Language flex-end 右对齐（截图确认白毛玻璃+globe）；筛选按钮点击后 active 背景/边框透明(无红圆)、面板在按钮正下方且 left 对齐、animationName=map-filter-drop；视觉面板 13 行 5 组、13 个 ↺、数值显示「1.2/1.8」「13px/13px」「1.45/1.45」(截图确认布局)、:root 已注入分类变量。**待真机/真数据确认**：④⑥⑦⑧⑩需打开某标点详情页/视频/模糊地址点才能看观感（无头浏览器 Google Maps 标点难点开，本轮靠代码+变量已生效确认，未逐一开图）。⑨.1 各类拆分对详情页文字的实际效果同样建议开一个详情页拖滑块确认。**注**：改了 `editor-api.mjs`，保存视觉设定前需**杀 node 重启**（动态 import 有缓存）。
- **2026-07-05 图标大改 v152→v153（Claude Code）**：跟用户多轮敲定后一次性替换全站图标。①**导航/地图/详情/编辑器图标换 Lucide 线条图标**（ISC 许可，直接内嵌不走 CDN 以保离线）：menu / upload / panel-bottom-open(展开列表，手机从下展开) / map-pin-search(筛选) / tags(标注) / focus(模糊调节) / search / zoom-in(放大) / circle-play(视频) / external-link(关联链接) / trash-2(删除) / images(多图) / pencil-sparkles(插图) / letter-text(有文本) / video(有视频) / mail-check(投稿信封) / inbox(收件箱) / clock+map-pin(EXIF 时间/坐标)；返回地图保持原样。②**切换地图按钮**改用三张用户手绘 map 图标（一张地图折三格、斜线画在左/中/右格表示当前地图），去掉数字角标（`MAP_SWITCH_OUTLINE`+`MAP_SWITCH_HATCH`，`syncMapTypeButton` 注入 `.map-type-ico`）。③**新增定位按钮** `#locate-me`(navigation 图标)：点按用 geolocation 跳到用户当前位置(`goToMyLocation`，panTo+至少 zoom14，取位时按钮转圈)。④**天气全换 Lucide + 雨按 WMO 重分四级**：`weatherCategory` 新增 `light-rain`——小雨(51/53/55/61/80)=cloud-hail、中雨(63/81)=cloud-rain、大雨(65/82)=cloud-rain-wind、暴雨(95/96/99)=cloud-lightning；晴 sun/夜 moon/多云间晴 cloud-sun·cloud-moon/阴 cloud/小雪=用户手绘7点/大雪 snowflake（`WEATHER_ICON_INNER` 表驱动）。四处版本号 152→153。**验证**（本地 preview localhost）：无控制台报错、node --check 通过；地图页工具图标+定位键+三格地图图标渲染正常；档案卡片角标(images)渲染；`--icon-stroke`/详情字号变量已注入 :root。天气详情轴、放大灯箱因需 Google Maps 出图，**天气图标/放大按钮实际观感待真机确认**。
- **2026-07-05 视觉设定面板 v153（同上，Claude Code）**：新增本机专用「视觉设定」面板（图标线宽 / 详情正文字号 / 详情正文行距三个滑块，拖动实时预览，保存写回 `data/theme.json`；后端 `/api/save-theme` 带范围钳制）。前端启动 `loadTheme`+`applyTheme` 写 `:root` 变量（线上也吃）；主要图标线宽改读 `var(--icon-stroke)`（nav/map-layer/panel-toggle/search/card-badge）。**验证**：面板 3 滑块可拖动实时改 `--icon-stroke`（截图确认工具栏图标变粗）；保存后 `data/theme.json` 正确写入且面板关闭；无报错。

- **2026-07-04 地图页改造(阶段二·手机布局) v151→v152（Claude Code）**：手机地图页把四个按钮排成**顶部横幅**（用户）。只改 `styles.css` 的 ≤820px 断点（桌面不动）：①`.map-layer-tools` 从左下竖排改成**左上横排**（`flex-direction:row`，DOM 顺序=地图切换(4) 在前、筛选(2) 在后）；②`.panel-toggle-left`（档案键1）改到**顶部居中**（`left:50%;translateX(-50%)`，用 `.workspace(.is-list-collapsed)` 前缀压过 line 570/576 桌面定位）；③汉堡(6) 本就在右上=横幅右端；④横幅所有按钮 + 各页汉堡**去掉圆形背景**（border/background/box-shadow/backdrop 清零）；⑤地图页按钮浮在底图上→白图标+深色 drop-shadow（卫星/普通都可读），非地图页汉堡用深色 `--ink`。最终布局：{4 2}靠左 · {1}居中 · {6}靠右。四处版本号 151→152。**验证**（本地 preview 375）：横幅位置量得 tools.left=14、档案 left=164(中心≈188=屏幕中点)、汉堡 left=315(靠右)；截图确认无圆底白图标、右下「139 item」白字、Archive 页汉堡深色无圆底且横幅按钮不出现。**注**：预览是本机(local)，横幅里多出一个编辑器「模糊调节」◉ 与右上「z 15」缩放读数——线上手机(非 local)不会有。白图标在**普通地图(浅底)**的可读性需真机确认。
- **2026-07-04 地图页改造(阶段一·通用逻辑) v150→v151（Claude Code）**：①**三地图循环**：删掉「卫星标签」按钮（`#map-labels-toggle`）与 `toggleMapLabels`，`#map-type-toggle` 改成一个按钮循环三态——卫星1(无字)→卫星2(有字)→普通→…（新函数 `cycleMapType`，复用 `state.mapBase`+`state.mapLabels`）。②**图标带编号**：地图切换按钮重绘为双层图例，中间叠 `.map-type-num`，`syncMapTypeButton` 据 `mapTypeNumber()` 写 1/2/3（1=卫星1、2=卫星2、3=普通），tooltip 提示下一张。③**删「查看所有」圆按钮**：`#reset-map` 去掉 svg 图标，只留 `#result-count`「139 item」文字，改**白字**（加深色描边光晕，卫星/普通底图都可读），点它=显示全部标点（原 reset 逻辑不变）。只改 index.html/app.js/styles.css。四处版本号 150→151。**验证**（本地 preview 1280，地图因 Key 域名限制未出图）：控制台无我引起的 JS 报错（仅 RefererNotAllowedMapError）；右下角变白色「139 item」无圆底；地图切换按钮显示双层图标+居中数字「1」；卫星标签按钮已消失。**三态循环切换本身需真机/线上验证**（依赖 googleReady）。
- **2026-07-04 手机端适配(2)：统计页窄屏修复 v149→v150（Claude Code）**：修手机上统计页崩坏。**根因**：`.archive-content` 是 `display:grid` 但没设 `grid-template-columns`，隐式列 `auto` 被最宽子元素（统计宽表 1531px）撑开→整页横向溢出、说明段落跟着变宽跑出屏幕、`.stats-table-wrap` 的 `overflow-x:auto` 形同虚设。**修法**：`.archive-content` 加 `grid-template-columns: minmax(0,1fr)`，列不再被内容撑；宽表回到各自 `.stats-table-wrap` 内部横向滚。**另在 ≤560px**：统计表字号 13→12、内边距 6/10→5/7、长行标签（如 "transparent long umbrella"）与左上角 `.stats-corner` 改 `white-space:normal` 且 `min-width:92px`，让第一列换行变窄→一屏能看到 4 列数据（原来只 2 列）。只改 `styles.css`。四处版本号 149→150。**验证**（本地 preview）：手机 375——修前 body.scrollWidth=1550、intro 宽 681、wrap 宽 1532（全溢出）；修后 body/intro/content 都=375/339、wrap 宽 339 内部 scrollWidth 958（改为内部滚），透视表一屏见 hookable/drop/disposal/placement、总览表 IMG/date/type 一屏可见其余内滚；桌面 1280——body.scrollWidth=1280 不溢出、透视表全列显示、说明两端对齐、导航横排，`minmax` 未影响桌面。
- **2026-07-04 手机端适配启动：本地服务对局域网开放 + 顶栏改汉堡菜单 v148→v149（Claude Code）**：开始做手机/平板适配。①`server.js` 监听地址从 `127.0.0.1` 改成 `0.0.0.0`，让同 Wi-Fi 的手机能用 `http://<Mac局域网IP>:4173` 打开真机测试；编辑器 `/api/*` 的本机 IP 关卡（isLocalRequest）不动，手机只能看访客版、改不了数据。②**顶栏手机导航改汉堡菜单**（用户选的方案）：`index.html` 顶栏加 `#nav-toggle` 汉堡按钮 + 给 `.view-tabs` 加 id；`styles.css` 桌面隐藏汉堡、≤820px 显示汉堡并把 `.view-tabs` 变成右上弹出的竖排面板（默认 opacity/pointer-events 收起，`.topbar.is-nav-open` 展开），覆盖地图页「导航字变浅」规则保证面板内可读、语言子菜单改 `position:static` 内嵌展开；`app.js` 加汉堡开关逻辑（点汉堡切 `is-nav-open`、选 tab/点外面/Esc 收起），复用原 tab 与语言切换逻辑没动。桌面完全不受影响。四处版本号 148→149。**验证**（本地 preview，模拟 375×812 手机 + 1280 桌面）：手机——汉堡在右上角、点开弹出竖排 Umbrella map/Archive/About/Submit/Language、当前项加粗高亮、地图页深底浅字 / Archive 页白底深字随页面配色、选 Archive 后自动收起、点外面收起（is-nav-open=false）、语言子菜单内嵌展开日本語/English；桌面 1280——汉堡 display:none、导航仍横排。地图本身因 API Key 域名限制在预览端口不出图（**预览环境限制非 bug**）。**未在真机验证触摸手感**。
- **2026-07-04 收件箱 UI 大改 + Submit 直跳 + ABOUT 换位等一批 v147→v148（Claude Code）**：用户 5 组需求。**收件箱（1）**：① 左列改成**按投稿月份分组、可折叠**（`inboxSubmissionDate` 正则抽「四位年+月+日」，因 Google 时间戳含「下午」new Date 认不了；`inboxState.collapsed` 记折叠的月份）；每条**标题=record 文件夹 id**（后端新给 `folderId`：已导入的用真实 id、未导入的用 `nextFolderId` 算预览 id），下面一行显示投稿者+投稿日期（年月日），去掉地点/拍摄时间/照片数。② 右侧**彻底改成只读展示**（`<dl>` 信息行：文件夹 ID/投稿者/投稿时间/发现时间/地点 + 内容段落），删掉所有文本框，只留照片(带 EXIF)+「导入并编辑」按钮。③ 主图有 GPS 时自动当导入初始坐标（不再手填，去掉「用此坐标」「用地图中心」）。④ **修 bug（1.4）**：导入后又删了标点，收件箱仍显示「已导入」且无法重导——后端 `reconcileImportedState` 每次列表时对账：已导入但 record 文件夹已不存在的，撤销「已导入」标记（靠去重日志 `state.log` 的 key→id 映射查文件夹）。⑤ **导入的标点默认勾选「显示名」（1.5）**：`importSubmission` 里 `displayId` 默认填成文件夹 id → 编辑器「显示名」勾选框据「displayId 非空」自动勾上。**投稿者留空默认 Anonymous（2）**：详情页 contributed 的「by」行改成 `submitter || "Anonymous"`（原来留空不显示），收件箱列表/详情同样用 Anonymous。**Submit 直跳（4）**：点导航 Submit 不再进内嵌页，直接 `window.open` 新标签打开 Google 表单（viewform）；删掉 `#submit-view` 内嵌 iframe 那一段 HTML + 懒加载 JS + 死 CSS；Submit 标签加 hover 效果（上传箭头上抬）+ 双语 title 提示（ja「Googleフォームから投稿」/ en「Submit via Google Form」，随语言切换在 applyLanguage 里设）。**ABOUT 换位（5）**：`renderAbout` 的 sections 顺序改成 `[section2, section1]`，即「記録の作法」排到「観察のきっかけ」前面（不动 texts.json）。**站内自建表单（3）**：写进 `未来可能需要注意的事.md`（推荐 Google Apps Script Web 应用当收件端、保留现有管线只换前门）。四处版本号 147→148。改了 editor-api/core 已**杀 node 重启**。**验证**（本地 preview 4173，表单有 1 条真实测试投稿）：list 返回 folderId=form_20260704_1；UI——月份组「2026-07(1)」可折叠、条目标题=文件夹 id+「Anonymous · 2026-07-04」、右侧只读无任何 input/textarea、信息行整齐、照片带 EXIF；Submit 点击 stub 到 window.open(表单 url,_blank,noopener) 且当前视图不变、submit 不激活；tooltip ja/en 双语正确；ABOUT 首段 ja=記録の作法/en=Methods of Recording（已换位）；无 console 报错。**未真跑「导入并编辑」**（会生成正式待审记录，留给用户点）。**item 2 的详情页 Anonymous 未单独聚焦验证**（改动是一行、逻辑直观）。**另：验证「三组尺寸」规则对投稿图走通**——写临时脚本用导入时真正会调的 `fetchDrivePhoto`+`generateDerivatives` 把真实投稿主图跑一遍：产出 `.thumb.webp` 400×300 23KB + `.web.webp` 1280×960 394KB + 原图 4032×3024 jpeg 保留；`importSubmission` 对非 HEIC 图确实调用 `generateDerivatives`（HEIC 先被 `isHeicExt` 拦截、只存原图+占位，需手动转）。脚本跑完即删、未落任何真实记录。

- **2026-07-04 收件箱：照片自动读 EXIF + 改成「导入并编辑」直通编辑器 v146→v147（Claude Code）**：功能 2/3/4。做法定为**复用现成编辑器**（用户拍板）：不在收件箱右侧重做地址级联/伞属性/段落/四勾选框，而是收件箱只做「导入草稿→打开完整编辑器」，所有细编辑在常规编辑器里做（那里本就有地址级联、伞属性、段落图文、待改/关联/标题/显示名四勾选框、拖动地图定坐标）。改动：**后端** `editor-api.mjs` 的 `submissionsPhoto` 对非 HEIC 照片顺手 `parseExif(buffer)`，返回 `exif:{dateTime,coordinates}`（HEIC 读不了照旧只回占位）。**前端** app.js：① `renderInboxPhotos` 每张照片下显示 EXIF（🕑拍摄时间 · 📍GPS），主图有 GPS 就自动填进坐标框、其它图给「用此坐标」按钮；无 EXIF 显「无 EXIF」。② 坐标框改「可留空」，`doInboxImport` 坐标可空（进编辑器再拖动定），导入成功后**直接** loadUmbrellaData→render→关收件箱→openEditor→switchToMapView（有坐标才 centerInEditorGap），toast 提示。③ 按钮「增加为标点」→「导入并编辑」，去掉「必须先填坐标才可点」的禁用。④ styles.css 收件箱照片框改纵向（图 120×120 + 下方 EXIF 说明），加 `.editor-inbox-photo-exif`/`.ib-photo-gps` 样式。四处版本号 146→147。改了 editor-api.mjs 已**杀 node 重启**。**验证**（本地 preview 4173，表单此时有 1 条真实测试投稿）：`/api/submissions/list` 读到 1 条、10 列全认；`/api/submissions/photo` 对测试照片读出 EXIF（拍摄时间 2026-07-01T18:25:06+09:00、GPS 34.976086,135.761689）；UI 打开收件箱→选中→照片显示 EXIF+「用此坐标」、主图 GPS 已自动填坐标框、按钮「导入并编辑」可点；截图确认布局。**未真跑「导入并编辑」**（会用这条真实测试投稿生成正式待审记录+标记已导入，留给用户点；openEditor 那段是沿用旧代码）。**Q1 自建站内投稿表单**：与用户讨论，暂未做（见交接）。

- **2026-07-04 编辑器加「投稿收件箱」：拉 Google 表单投稿→审核→增加为标点 v145→v146（Claude Code）**：功能 2。**后端**：① 抽出共享核心 `scripts/submissions-core.mjs`（连 Google、解析投稿、下载照片、生成 record、去重），命令行 `pull-submissions.mjs` 改成它的薄壳（一份逻辑，CLI 和收件箱共用）。② `editor-api.mjs` 加 3 个只对本机的接口：`/api/submissions/list`（读表格返回投稿列表+imported 标记）、`/api/submissions/photo`（Drive 照片转 base64 dataUrl 给预览；HEIC 只返回标记不返回图）、`/api/submissions/import`（把某条投稿+用户改过的信息+坐标生成成 `submission(pending)` 标点，去重、重建）。**前端**（app.js，仅 IS_LOCAL）：③ 工具栏「新增」正下方加「收件箱」按钮（信封图标，仅编辑模式显示，column-reverse 里 insertBefore add）。④ 收件箱弹窗：左列投稿列表（署名/时间/地点/照片数/已导入徽标）、右侧详情（照片预览含 HEIC 占位、可改署名/时间/地点/内容段落、坐标 lat/lng 输入+「用地图中心」按钮、「增加为标点」需坐标才可点）。导入成功后台 `loadUmbrellaData`+`render` 刷新地图，状态栏给「打开编辑」跳到常规编辑器微调。⑤ styles.css 加收件箱按钮圆钮样式+弹窗全套样式。四处版本号 145→146。改了 editor-api.mjs 已**杀 node 重启**。**验证**（本地 preview 4173）：3 接口 node 语法过；`/api/submissions/list` 实连 Google 读空表返回 0 条；CLI dry-run 重构后仍报 0 条；前端——编辑模式收件箱按钮出现在「新增」下方、点开弹窗、列表加载空表显示「还没有投稿」、无 console 报错、截图确认布局。**未验证（表单现在没有投稿，需用户投一条测试才能验）**：真有投稿时的列表渲染、照片/HEIC 预览、导入生成标点全流程。

- **2026-07-04 导航栏加「投稿」入口（嵌入式 Google 表单） v144→v145（Claude Code）**：功能 1。① index.html 导航 ABOUT 右边加 `.tab tab-submit`（data-view=submit，上传图标 inline SVG + 文字 Submit）。② 新增 `#submit-view`（class `submit-view view`），里面一个懒加载 iframe 指向表单 embed 地址（viewform?embedded=true），下面加兜底链接「新标签打开表单」。③ 切换复用现有通用逻辑（data-view→`${view}-view`），只在 app.js tab 处理里加一段：点到 submit 时才把 `data-src` 赋给 iframe.src（懒加载，不点不拉外部内容）。④ styles.css：submit-view 沿用 archive/about 顶部留白 112px，`.submit-frame-wrap` 居中 760、`.submit-form-frame` 高 `100vh-172`、tab 图标并排、兜底链接小灰字。四处版本号 144→145（index×2、app.js 的 sw、sw.js CACHE_NAME）。**验证**（本地 preview desktop）：标签在 ABOUT 右带图标+Submit、点击 submit-view 激活+map-view 关、iframe 懒加载后 src 正确设上、兜底链接文案/href/target=_blank 正确、无嵌入拦截报错（只有无关的地图 referer 报错，因端口非 4173）。**要告诉用户**：表单一进来先要求登录 Google——因为表单有必填「上传照片」题，Google 对文件上传题强制登录；且框内跳 accounts.google.com 登录多半被禁止 iframe，所以配了「新标签打开」兜底。

- **2026-07-04 投稿导入系统（方法 B：Google 表单→本地记录）后端部分（Claude Code）**：新造「投稿收纳」链路的后端。用户已有 Google 表单、投稿者自己传照片+填信息、`submission(pending)` 是正式分类（非暂存）、投稿者写的文字进「内容段落 blocks」（不进 submitterNote，因编辑器无此框）。① 装 `googleapis`（本机后端依赖，不影响线上静态站）。② `scripts/google-auth.mjs`：OAuth 桌面应用授权，127.0.0.1:4179 回环接 code，令牌存 `scripts/.google-token.json`，只读权限（sheets.readonly + drive.readonly）。③ `scripts/pull-submissions.mjs`：读表格新回答→按关键词认列（表头=表单问题文字，中英日都认）→抽 Drive 文件 id→下载照片到 `submission(pending)/form_日期_序号/`（主图 `<id>.<ext>`、其余 `_2/_3`）→能显示的图生成缩略图/网页版、HEIC 照下不生成→生成 record.json（submitter/time/locationText/blocks 预填，submissionType=contributed、submissionChannel="Google 表单"、locationApprox/timeApprox=true、无坐标故 weather=null）→用主图 Drive id 去重（存 `scripts/.submissions-state.json`）→重建。支持 `--dry-run` 试跑、`--sheet=` 覆盖。④ npm 脚本 `submissions:auth`/`submissions:pull`。⑤ `.gitignore` 加 4 个私密文件（credentials/token/state/config）。⑥ 新 `投稿导入-使用说明.md`（阶段 0 Google Cloud 逐步操作 + 平时用法）。**验证**：两脚本 `node --check` 通过；纯逻辑单测——Drive 链接单张/多张/`/d/` 三种格式都能抽 id、用表单真实问题文字当表头 10 列全部认对。**未验证（需用户做完阶段 0 授权才能实测）**：真正连 Google 读表格/下照片。**待办**：阶段 2 前端 HEIC 占位提醒、阶段 3 网站加「投稿」入口。

- **2026-07-04 修 bug：左侧搜索不认「显示名 displayId」 v143→v144（Claude Code）**：现象=文件名 IMG_8665、显示名改成 IMG_8664，搜「8664」搜不到、搜「8665」才搜到。根因=`filteredUmbrellas()` 的搜索 haystack 只放了 `item.id`，没放 `item.displayId`。修法：haystack 加一项 `item.displayId`（真实文件名 id 仍在，所以两者都能搜到）。四处版本号 143→144。**验证**：本地 preview 用真实数据模拟同一套 haystack——搜「8664」命中 IMG_8665(disp:IMG_8664)、搜「8665」也仍命中。

- **2026-07-04 删掉半成品投稿 yueliang_1_ + 限制上传格式（防呆） v142→v143（Claude Code）**：① 按用户要求删掉 `submission(pending)/yueliang_1_`（media 空 + HEIC 打不开的半成品，就是上一轮拖垮构建的那个），重建后 138 条、无警告。② 上传/新建标点加**格式白名单**，浏览器打不开的（HEIC 等）从源头禁止：**前端**——`app.js` 加 `mediaFormatError()` 校验，`onCreateRecord`（新建主图，仅图片 jpg/jpeg/png/webp/gif/avif）和 `onUploadImages`（追加媒体，图片或视频 mp4/mov/webm/m4v）传之前逐个把关，不合格弹红字提示、整批不传；两个 `<input>` 的 `accept` 从 `image/*`(会放进 HEIC) 收紧成明确扩展名列表（`CREATE_ACCEPT`/`UPLOAD_ACCEPT`）。**后端兜底**——`editor-api.mjs` 引入 record-utils 的 `isImageFile/isVideoFile`，加 `assertImageFilename`（create-record 主图）+ `assertMediaFilename`（upload-image），不合格直接 400 中文报错，谁都绕不过。四处版本号 142→143。改了 editor-api.mjs 已**杀 node 重启**。**验证**：curl 打后端——create-record 传 .HEIC/.mp4 都 400 拒绝（主图必须图片）；upload-image 传 .heic 400 拒绝、.mp4 通过格式关（后段因空数据才报 Missing image data=证明过了格式关，未落盘）；node --check app.js/editor-api.mjs 通过；本地 preview 读到两个 file input 的 accept 已是明确扩展名列表。

- **2026-07-04 修「无法新增标点/编辑模式坏」+ 地址 unknown 不显示 + 去掉新增改名弹窗 v141→v142（Claude Code）**：用户报 3 件事。① **根因 bug**：新投稿文件夹 `submission(pending)/yueliang_1_` 是半成品（media 空、图片是浏览器不支持的 `.HEIC`），`build-umbrellas.mjs` 因「没有主图」直接 throw→整站构建挂掉→编辑器每次保存/新增都会自动重建 = 全部失败（这就是「无法新增、编辑模式坏」）。修法：`buildUmbrellaItem` 遇到没有可显示主图的 record 不再 throw，改成 `console.warn` 跳过并 `return null`（record.json 原样保留，补齐主图后重建即恢复），主循环过滤掉 null。重建通过：138 条，只跳过 yueliang_1_。② 地址层级选了 `unknown` 不再对外显示：`normalizeLocationLevels`（所有展示地址的正规化入口）过滤掉值为 `unknown` 的层级；保存值不变（编辑器复原读的是 raw，不受影响）。如 rednote_Last.Coffee 从「Miyagi, Sendai City, unknown」→「Miyagi, Sendai City」。③ 去掉新增标点选图后「是否修改文件名」的 `window.prompt`（有了显示名 displayId 功能就不必再在这步动真实文件名），直接用原文件名。四处版本号 141→142。**验证**：build 通过跳过半成品；node 用真实数据确认含 unknown 的 9 条记录显示地址不再带 unknown；本地 preview app.js 无语法/运行错误。**注**：yueliang_1_ 需用户把 HEIC 转成 jpg 再在编辑器补图才会上图。

- **2026-07-04 编辑器加「显示名」功能：给标点起对外显示名，替换页面上的 ID v140→v141（Claude Code）**：用户要能改主图 ID 在页面上的显示，但不想动文件夹/文件名（直接改 ID 会牵连文件夹命名、linkedId、历史等，太乱）。方案 A（用户拍板）：新增 record 字段 `displayId`（单语字符串），填了就**完全替换**所有页面上显示的 ID，内部匹配/文件夹/linkedId 仍用真实 `item.id` 不变。① 数据层：`record-utils.mjs` 序列化加 `displayId`（带中文注释，排在 title 后）、`build-umbrellas.mjs` 透传、`editor-api.mjs` save-record 白名单加 `displayId`（trim 存）。② 显示层：`displayUmbrellaId()` 改成「有 displayId 就用它、否则回退原逻辑」，并把所有还在直接用 `item.id` 显示的地方都改走它——列表按钮 strong+alt、详情头部(本就走)、卡片 id+alt+`renderItemText` 的 h3、统计页 idLabel(含多伞 `(i+1)` 后缀)、关联下拉 option 文字、编辑历史 label。data-id / option value / 标记匹配一律保持真实 id。③ 编辑器：头部加「显示名」勾选框，勾选展开一行输入框 + 行内常驻「原文件（真实文件夹/文件名，不会变）：<真实 id>」提醒（用户要求留原文件提醒）；`openEditor` 预填、`saveEditor` payload 带 `displayId`；`styles.css` 加 `.editor-displayid-hint` 灰小字样式。四处版本号 140→141。**验证**（本地 preview 4173）：临时给 IMG_0981 设 displayId=京都-01 并 rebuild→列表/卡片/详情头部全显示「京都-01」、原 IMG_0981 不再出现、`data-id` 仍是 IMG_0981（点击选中/详情跳转正常=按真实 id 查找不受影响）；编辑器「显示名」勾选框自动勾上、输入框预填京都-01、原文件提醒显示 IMG_0981、编辑器标题栏仍「编辑：IMG_0981」。测试数据已 `git checkout` 还原干净（0 处残留）。

- **2026-07-03 默认比例尺 16→15 + 定位新增「无标点时缩到 z11 落到最近伞簇」规则 v139→v140（Claude Code）**：① `DEFAULT_MAP_ZOOM` 16→15（进场动画落点、加点后归位等都跟着变）。② 定位系统在原三条规则（默认东京 / 有授权且在日本用当前位置 / 不在日本回东京）基础上加第 4 条：用户在日本、但当前位置以 z15 打开屏幕内一个标点都没有时，缩放到 z11 并把地图移到「离用户最近、且 z11 屏幕内能看到≥3 个标点」的伞簇中心（「聚集判定」，用户拍板）。统计标点时**所有标点都算**，不管图层筛选是否隐藏（用户拍板）。找不到任何≥3 簇时回落东京 z15。③ 实现：新纯函数 `latLngToWorldPoint`(Web Mercator)/`countMarkersOnScreen`(算某中心+zoom+画布尺寸下屏内标点数，不依赖地图 idle)/`findNearestClusterCenter`(遍历每个标点当候选中心，筛≥3 取离用户最近)/`resolveInJapanView`；`getInitialMapCenter` 改为返回 `{center, zoom}`；`initGoogleMap` 存 `state.entryTargetZoom`；`playEntryZoom` 用动态目标 zoom 而非写死 DEFAULT。四处版本号 139→140。**验证**：node 用真实 129 个标点跑逻辑——名古屋(附近无伞点)z15 屏内 0→回退到最近≥3 簇(z11 可见 65 点)；站在某标点上 z15 屏内≥1→保持用户位置 z15。本地 preview：进场落点 zoom readout=「z 15」、东京站正常渲染、无 console/network 报错。**在日定位触发第 4 条的真机路径无头浏览器无法模拟授权，需真机确认。**

- **2026-07-03 图片提速：全站改用 webp 分级图 + 放大渐进式加载原图 v138→v139（Claude Code）**：上线后用户反映图片加载慢。根因=790MB 原图（单张 2~10MB），网站没缩略图，`thumb` 缺失时直接拿原图当小图用，GitHub Pages 又不压缩。方案（用户拍板）：装 **sharp**（本机后端依赖，不影响线上），每张原图旁生成两个 webp——`NAME.thumb.webp`(400px,~30KB)+`NAME.web.webp`(1280px,~350KB)，原图保留只在放大时按需下。① 新工具 `scripts/image-derivatives.mjs`（生成/删除/命名，THUMB=400 q70 / WEB=1280 q78，`.rotate()` 烘 EXIF 方向）；② 批量脚本 `scripts/build-image-derivatives.mjs` 给存量 208 张补齐（只新增、不动原图，共缩略 6.5MB+网页 63MB）；③ `build-umbrellas.mjs` 每条 media 产出 `thumb`(400)/`src`(1280)/`original`(原图) 三路径，缺失优雅回退；④ 前端 `normalizeMedia` 带上 `original`，放大 `loadExpandedImage` 先显示 1280、再 XHR 后台下原图（左下角胶囊进度条 `#focus-hires-bar`，无文字，真 Content-Length 进度）、下完淡入替换；细节：切图/关闭中断下载(Abort/token)、失败静默、原图≈网页版则跳过、最短显示 320ms、blob 用完 revoke；⑤ `editor-api.mjs` upload/create 自动生成、delete 连带删生成物，`stillHasImage` 排除生成物；⑥ `record-utils.mjs` 文件夹扫描 `isDerivativeFile` 排除生成图（否则会被当新照片塞进 media，已验证修好，IMG_0101 由 6 条回到 2 条）；⑦ `sw.js` 只离线缓存小 webp、不缓存几 MB 原图（用户 #5）。四处版本号 138→139。**验证**（本地 preview 4173）：umbrellas.json 三路径正确、列表小图=thumb.webp、详情封面=web.webp、放大先 web.webp→后台换成 blob(原图)、进度条 is-active→is-done 淡出、关闭还原 web.webp 且 blob 已撤销、无 console/network 报错、全库 media 无生成物残留。**胶囊进度条中途视觉+放大淡入真机再瞄一眼**（无头浏览器难完全证明动画）。

- **2026-07-03 根治：设定改存进 data/site-settings.json，本地面板调完自动写回=push 即上线 v137→v138（Claude Code）**：接上一轮——v137 只是把当时的值写死进代码，用户以后再调还得手动导。本轮做根治：新建 `data/site-settings.json` 作为「聚焦模糊参数 + 三张地图文字筛选」的**真源**（像 texts.json）。① 前端启动 `loadSiteSettings()` fetch 它 → `defaultBlurSettings()` 和新的 `defaultCategorySetFor()` 优先用文件值（叠加在代码 `hard*Set`/`BLUR_PARAMS.def` 回退之上）；`init` 里在 `loadMapCategoryState` 前 await 加载。② 本机在模糊面板/地图图层面板调任何值→原有 `saveBlurSettings`/`saveMapCategoryState`（存 localStorage）里新增 `persistSiteSettings()`：防抖 600ms 把「当前模糊参数+三套地图」POST 到新接口 `/api/save-site-settings`，写回文件。仅 `IS_LOCAL` 生效（线上无 /api 自动跳过）。③ 后端 `editor-api.mjs` 加 `saveSiteSettings`（严格 sanitize：只认已知类别键/vis 值/数字或空的 zoom），路由 `/api/save-site-settings`。④ 把 v135→v137 的 IMG_1417/信封/模糊按钮那批版本号一路推到 v138。**验证**（本地 preview 端口 61488）：接口往返 POST 原值→文件值不变(只排版展开)；写特殊值 radiusN=200 重载→`--fb-radius-n` 变 200px（证明读的是文件不是代码回退），改回 136；真拖 featherN 滑块→1 秒内文件 featherN 变 90（证明面板调完自动写回）→恢复 36；`node --check` 三文件通过；最终校验文件 blur 9 项+地图抽查 4 项全对。**注**：改了 `editor-api.mjs`，已重启 node（preview 重开）验证。以后用户在面板调完，`data/site-settings.json` 会自动改，直接 push 就上线，不用再开控制台导。**地图观感真机切卫星1/2 再瞄一眼**（本地 Google 地图未加载）。
- **2026-07-03 把本机调好的地图文字筛选/聚焦模糊/圆环文字位置固化成代码默认值上线 v136→v137（Claude Code）**：用户反馈上线后和本地不一样——卫星1/2 文字筛选、聚焦模糊、圆环文字位置都没上线。根因=这些参数只存在**用户浏览器 localStorage**（`fu-map-layers`/`fu-blur-settings`），从没写进代码文件，push 带不上；线上无 localStorage 只能用代码写死的默认值。修法（代码注释本就写明「Tuned values are meant to be read off + baked into defaults」）：让用户在 Edge 控制台 `copy(JSON.stringify(...))` 导出三个 key 的真实值，把它们写进代码默认：① `BLUR_PARAMS` 的 `def`——radiusN 138→136、featherN 34→36、blurA 7→6、radiusA 200→126、featherA 60→138、labelDistanceA 245→260、labelRotateA 0→-135（blurN/veilA 不变）；② 地图三套默认：新增 `defaultRoadmapSet`(POI 文字/图标压到 zoom≥19)、`defaultSat1Set`(几乎全 hide，只留行政/水域名，公共交通 zoom14~17.5)，重写 `defaultSat2Set`(常用文字类 show、POI 图标 fade、道路线 hide)；`loadMapCategoryState` 三套 seed 换成这三个，`activeMapCategoryState` fallback 抽成 `defaultCategorySetFor(key)`。panels(编辑器栏宽)是本机便利项、只本地用，不固化。四处版本号 136→137。**验证**：`node --check` 通过；预览里**清空 localStorage 重载**（模拟线上无存储）后读 `--fb-*` CSS 变量＝全部等于用户值(136/36/6/126/138/260/-135)，证明默认值在无 localStorage 时也会 apply 上线。**注**：用户本机已有 localStorage，本地视图不变；地图默认真机切到卫星1/2 再瞄一眼最稳（本地端口非 4173 地图未加载）。**遗留**：以后本地面板再调仍只进 localStorage，要再导一次才上线；根治需把这些设定像 texts.json 那样存进代码文件（待办）。
- **2026-07-03 数量unknown不显示object/state + 投稿信封图例换「下箭头入信封」并移到ID左边 + 编辑用模糊按钮上线隐藏 v135→v136（Claude Code）**：用户 3 条。① **数量=unknown 时详情页不再显示 object/state 行**：`normalizeUmbrellaData` 里把 count=unknown 的 `objectLines`/`statusLines` 从 `["unknown"]` 改成 `[]`（空数组），`renderFocusInfo` 本来就 `if(lines?.length)` 才渲染=两行整体不出现；`objectText`/`statusText` 仍保留 "unknown" 供搜索匹配。② **投稿伞图例换样式+挪位**：`renderFocusHeader` 的 SVG 从原「信封+翻盖」换成「向下箭头落进信封」(下载入箱样式，参考用户图3)；并把 `badge` 从 id 右边移到**左边**（`${badge}${title}`），CSS `.focus-badge` 的 `margin-left:8px`→`margin-right:8px`。③ **编辑用模糊度按钮(圆环+中心点)上线隐藏**：`syncBlurAdjust` 里 `const show=true` 改成 `const show=IS_LOCAL`——只在 127.0.0.1/localhost 出现，线上不显示。四处版本号 135→136。**验证**：本地 preview 双击投稿伞 ZHANG_ZHONGPU(1)→详情页标题 HTML 确认新箭头信封 SVG 在 id 左侧；`#blur-adjust` 在 localhost `hidden=false`（本机可见），线上因 IS_LOCAL=false 会隐藏；数量 unknown 逻辑经源码核对（空数组→跳过行）。控制台除 Google Maps referer(端口非 4173)外无 app.js 报错；地图未加载故详情页整体视觉建议真机再瞄一眼。
- **2026-07-03 修 IMG_0101 天气横轴 -24h 和 now 标签重叠发糊 v134→v135（Claude Code）**：用户反馈只有 IMG_0101 一个标点的天气有问题（图：右下角时间标签重影发糊）。根因=IMG_0101 的 24 小时天气**完全没变**（全是 code 3 阴），`weatherChangePoints` 的「相邻同类合并」+`reduceWeatherPoints` 的 collapse 会把头尾两个端点**并成一个点**，只剩 index=24(now) 一个，导致 `-24h` 和 `now` 两个时间标签都定位到 94%、叠在一起=文字重影。修法：在 `renderFocusWeatherAxis` 里 `reduceWeatherPoints` 之后加保底——**若只剩 1 个点，补回头(index 0)尾(index last)两个端点**（同一天气），让两端标签各归 6%/94%。**验证**：node 复刻真实数据+源码逻辑，修前=1 点(位置 94)、修后=2 点(位置 6/94)；全量扫描主图共 5 个「24h 单一天气」的点(IMG_6774/6781/6753/0101/8918)都会受益于此修复。四处版本号 134→135。**注**：Google 地图因本地端口非 4173 未加载，真实详情页视觉需真机点 IMG_0101 瞄一眼；控制台除 Maps referer 报错外无 app.js 错误。
- **2026-07-03 圆环地址文字去阴影描边并减轻字重 v132→v133（codex）**：按用户截图，把 `.focus-approx-label-text` 的 `font-weight` 从 600 改成 300，并删除黑色描边与 drop-shadow；四处前端版本号同步到 v133。
- **2026-07-03 天气中间图例减到3+中雪归入小雪+圆环文字加深描边阴影+修再点已聚焦标点详情回顶bug+去标点浏览器蓝框 v131→v132（Claude Code）**：用户 5 条。① **中间图例最多 3 个**（`reduceWeatherPoints` 的 maxMiddle 4→3）：IMG_8680 从 5 个(中间3)、IMG_8508 缩到 4 个，全量 83 主图中间>3 的 0 条、相邻重复 0 条。② **中雪(WMO 73)归入小雪**：`weatherCategory` 把 73 从「大雪 snow」挪到「小雪 light-snow」；大雪现在只剩 75/86。③ **圆环地址文字加深描边+加阴影**（图1：发白模糊卫星图上看不清）：`.focus-approx-label-text` 描边 rgba(0,0,0,0.46→0.72)、字重 500→600，并加 `filter:drop-shadow(0 1px 2.5px rgba(0,0,0,.65))`（SVG text 用 filter，CSS text-shadow 无效）。浅灰背景实测对比明显。④ **修 bug：已在某标点详情页时再点该标点，详情页会跳回顶部主图**。根因=`selectUmbrella` 无脑走 `render()`+`openFocusMode()→scrollTo(top:0)`。新增 `reFocusingSame` 判断（options.focus && selectedId===id && 已在 is-focus-mode）：命中就**只重新聚焦地图(`focusUmbrellaOnMap`)+恢复模糊(`setFocusBlurSuppressed(false)`)后 return**，不重渲染详情、不重置滚动/放大/图片索引。点别的标点仍正常切换。⑤ **去掉标点的浏览器蓝色选定框**（图2）：Google 给标点 `div[role=button]` 的浏览器聚焦轮廓/tap 高亮，加 CSS `.gm-style [role="button"]{outline:none;-webkit-tap-highlight-color:transparent}`（含 `:focus`/`:focus-visible`）。合成同结构元素实测 outlineStyle=none、tap 透明。**四处版本号 131→132 已同步**。**验证**：node 脚本跑天气逻辑全过；圆环文字浅灰背景截图清晰；蓝框 CSS 用同结构合成元素验证生效；控制台无报错。**注**：本会话内 Google 地图因多次刷新未再加载（额度/时序），所以 **bug④(再点同标点) 与蓝框在真实地图上的最终效果需真机点一下确认**；天气横轴真实详情页视觉同样真机再瞄。临时测试 DOM 已清。
- **2026-07-03 天气图例夜版只在now出现+中间图例最多4个(删最短段)+取消小雨归入中雨 v130→v131（Claude Code）**：接 v130 用户看 IMG_8680(24h一直下雨→图例一大排很丑)提的两条优化。① **夜间版只在 now 出现**：`weatherChangePoints` 划分变化段改用**不带昼夜的基础分类** `weatherCategory`（之前用 `weatherCategoryAt`，会因白天→夜晚切换在中间多分出「晴↔晴夜」段=多余图例）；晴/多云间晴的月亮版只在渲染 now 那一点时按拍摄当时钟点单独判断。中间一律白天版。② **中间图例最多 4 个（总≤6）**：新增 `reduceWeatherPoints`——头(-24h)尾(now)永远保留、位置不动，中间若超过 4 个就**反复删掉「持续时间最短」的那段天气**（最不有代表性），删完若相邻两段变同类就合并，直到中间≤4。③ **取消小雨归入中雨**：`weatherCategory` 把原小雨的 51/53/61/80 并进「中雨 rain（斜线2条）」，删掉 light-rain 分类和它的 1 斜线图标——现在最小的雨就是中雨。图标从 11 种减到 10 种。**四处版本号 130→131 已同步**。**验证**：node 脚本跑改后逻辑——IMG_8680 从原始一排缩到「中雨→阴→中雨→阴→中雨」5 个；IMG_8508 缩到 6 个(中间4，阴/雨交替)；IMG_8632「阴→多云间晴→阴→多云间晴→晴」中间全白天版；**全量 83 主图：中间>4 的 0 条、相邻重复 0 条**；夜版抽查 IMG_8302「阴→多云间晴→晴夜(月亮)」等 7 条都只在 now 出月亮、中间是白天版。10 图标本地并排截图确认（小雨已消失、最小雨=中雨2斜线）。控制台无报错。**天气横轴在真实详情页的最终视觉仍建议真机瞄一眼**（无头地图投影未就绪，只能验证到图例 HTML 逻辑层）。：本会话把交接里剩下的 🅐🅑🅒 三件做完（用户对图标另有定夺，已按用户意见做）。① **🅒 问题X 合并算法重写**：老算法「丢点」会漏掉中间短变化、留下前后两个相同天气=相邻重复图标（实测 8508 出现相邻两个「雨」）。按用户方案改成**不丢点**——所有变化点全保留（`weatherChangePoints` 保证相邻天气不同），只把挨太近(<13%)的**整体往两边推开**留出最小间距；右端(now)顶出去就钉回真实位置再回推；极端(变化太多塞不下)才退化成等距。另加一处：当最后一小时(now)天气和上一个变化点相同、被强制当尾点加入时会撞成相邻重复，`weatherChangePoints` 末尾**合并连续同类**（保留后一个 index，让这段延伸到 now）。**验证**：node 脚本对 8632/8508 跑改后逻辑——8508 现在是「阴→小雨→阴→小雨→阴→小雨→中雨」7 图例、能看出雨强度、无相邻重复、间距≥13%；**全量 83 个主图扫描：相邻重复 0**。② **🅒 问题Y 图标做细**：`weatherCategory` 从 7 类扩到 9 类 + 夜 2 变体=11 图标。按用户定：小雨/中雨/大雨=云+斜线 **1/2/3 条**；暴雨(雷)=云+斜线+闪电；小雪=云+雪花(含雨夹雪)、大雪=只雪花(含中雪)；雾并入阴；**只有晴/多云间晴分昼夜**（夜里 18:00–翌6:00 近似判断→换月亮版，其余天气不分昼夜）。全 11 图标本地并排截图确认外观正确。③ **🅐 短内容整体居中(方案A)**：`updateFocusScrollHint` 里按是否溢出给面板加/去 `is-focus-short`；CSS 用 flex `auto` 外边距（滚动区 `margin-top:auto` + footer `margin-bottom:auto`）把「内容+Back to map」当一组在固定高面板里**垂直居中**、footer 贴内容正下方；长内容不加此类=保持内部滚动、footer 固定底部。实测短记录 IMG_8508 上下留白各 66px（居中）、长记录 IMG_8622 footer 固定底、内容顶端对齐可滚。④ **🅑 放大进入/退出淡入淡出**：进入把 `focus-expand-pop` 关键帧改成 opacity **0→1**（完整淡入）；退出 `closeExpandedImage(animate)` 拆两段——用户点遮罩/按 Esc 传 `true`：先加 `.is-collapsing`(opacity→0、仍 is-expanded 保持大图居中)播 200ms 淡出，再 finalize 收回；切换标点/离开详情页等程序性调用传 `false`=立即收起。`expandImageAt` 会取消未完的淡出定时器+清 `.is-collapsing`。实测退出时序：立即 is-collapsing+is-expanded 并存→270ms 后两者都清、`imageExpanded=false`。**四处版本号 129→130 已同步**。**验证环境**：本地无头 preview(1280×750/860)；放大**真实播放**因无头地图投影(`fromLatLngToPoint`)未就绪只能靠类/时序验证，**淡入淡出的视觉与 FLIP 动画需真机再确认**。临时调试钩子 `window.__fu` 已删除、`node --check` 通过、控制台无报错。
- **2026-07-03 详情页放大切换动画修复+天气横轴放回图片之上(65%左对齐)+两端标签对齐端点图例+补充细节图详情页也显天气+Back to map贴内容 v128→v129（Claude Code）**：一批 6 项。① **1.1 放大后切换不同比例图的怪动画**：根因=v127 已把放大居中改成 `inset:0+margin:auto`，但切换图片的 FLIP 缩放动画(`flipExpandedPanel`)还带旧的 `translate(-50%,-50%) scale()`——那个 translate 现在多余，会把面板挪到左上再缩回=「跳一下」。改成**纯 `scale()`**（默认 transform-origin 中心，margin:auto 已居中，从中心缩放不偏移）。本地实测竖图↔横图切换：整个过渡 offCx/offCy 恒 0、matrix 无平移分量。② **2.4 天气横轴放回图片之上**：从 v128 的「主图正下方独立 `#focus-weather`」改回**覆盖信息块底部**（即图片之上、和 v128 前位置一样），由 `renderFocusInfo` 直接拼在 INFORMATION 网格后面；删掉 `#focus-weather` 元素和 `renderFocusImage` 里的单独渲染。宽度=**图片(文字栏)的 65%、左对齐**。③ **2.1 放大页不显示天气横轴**：因为②把横轴放进了 `.focus-overlay-info`，放大灯箱本来就 `display:none` 整块覆盖信息=顺带隐藏，无需额外处理。④ **2.2 两端标签**：`-24`→`-24h`；不再贴容器最左/最右，改成**各自对齐到首/尾图例的正下方**（JS 给标签写 `left:pos[0]%`/`pos[末]%` + CSS `translateX(-50%)`）。实测标签中心与端点图例中心对齐。⑤ **2.3 补充/细节图天气进详情页**：`renderFocusBlockHtml` 的图注左边加 `singleWeatherIconFor`（勾了「显示天气」才出，插图不出），和放大灯箱角标题一样；图注排成 flex 一行（图例左、标题右）。实测 IMG_8622 六张补充图详情页图注都出现天气图例。⑥ **1.2 Back to map 贴内容**：`.focus-scroll` 从 `flex:1` 改 `flex:0 1 auto`——短内容(不溢出)时滚动区只占内容高、footer 紧贴图片正下方(红框位置)；长文章(溢出)时滚动区压到剩余高度内部滚、footer 仍固定面板底部不滚动。实测短记录 IMG_8632 footer 贴内容、长记录 IMG_8622 footer 固定底部且滚动时不动。**四处版本号 128→129 已同步**。**本地(750/860 高)验证**：均为无头浏览器 preview 实测；FLIP 动画/真机再瞄一眼最稳。

- **2026-07-03 补充 Claude 行为规范（codex）**：对照一次 Claude Code 50 分钟任务记录，把 `CLAUDE.md` 里的协作规则写具体：必须中文大白话、讨论类需求先停、推倒重写先定边界、批量改真实数据前说明影响/可回退、验证不能夸大、最终回复要短；顺手更新 v128、95 条记录和 media 天气字段说明。
- **2026-07-02 每张图加「获取天气」按钮+「显示天气」勾选框+天气横轴重画(去温度/两端只标-24与now/线段遮挡/移到主图下方) v127→v128（Claude Code）**：把天气做完（用户 2.2–2.7）。① **编辑器每张图（主图/补充/细节，插图除外）的修改栏里加「☁ 获取天气」按钮 +「显示天气」勾选框**（图1/图3）：按钮=先保存再按 `mediaId` 抓（主图 24h、补充/细节只拍摄当时1点），抓到变绿「天气✓」；勾选框存 `media.showWeather`。删掉底栏原来的记录级抓天气按钮。② **详情页主图天气横轴重画（2.3/2.5/2.6/2.7）**：`renderFocusWeatherAxis` 从覆盖在图上改成**放到主图正下方**（新 `#focus-weather` 槽）；**不显示温度**，两端只标 `-24`/`now`，中间只放变化点图例（2.5）；**强制最小间距**——按真实时间贪心保留、离上一个不到 13% 的丢掉（合并太频繁的变化，不再重叠也不失真，2.3，实测某 13 变化点标点被压到 6 个）；连接线**和图例同样粗（2px）且只画在图例之间的空隙**（图例处断开=遮挡，镂空处不露线，2.6）。③ **补充/细节图**：放大(灯箱)看时，在右下角【标题/文件名/时间】左边放一个「拍摄当时」的单个天气图例（勾了显示天气才出，2.4）。④ 修 3 处会把天气字段丢掉的地方：前端 `normalizeMedia`、后端 `applyMediaMetadata`、`mergeRecordMediaWithFolder` 都保留 `weather`/`showWeather`；编辑器 `photoItem`/save/preview 也带上。**已本地(1280×860)验证**：主图横轴在图下方、6 图例真实间距、线段在图例间断开；编辑器主图=「天气✓」勾选、细节=「获取天气」未勾；点细节「获取天气」→保存+抓 1 点→变「天气✓」；放大细节图角标题左侧出现单个天气图例。**注**：改了 `editor-api.mjs` 已杀 node 重启验证。
- **2026-07-02 天气改「按每张图」存+批量抓所有主图天气+新建自动抓（用户2.1）（Claude Code）**：把天气从记录级 `record.weather` 改成**按 media 存**（每张图各有 `weather`+`showWeather`），为「补充/细节图各自抓天气」铺路。① `weather.mjs`：`fetchWeatherData` 加 `hoursBefore` 参数（主图=24 画横轴、补充/细节=0 只拍摄当时1点）；时间解析放宽——除 ISO 外也认投稿伞的点格式「2026.05.02, 19:56」「…around 18:00」（要同时有完整日期+时分，只有日期不编造钟点）。② 数据层：`record-utils.mjs` 序列化每张 media 增 `weather`/`showWeather` 且 `mergeRecordMediaWithFolder` 保留（否则一保存就丢）；`build-umbrellas.mjs` 每张 media 带 weather+showWeather，`item.weather`=主图天气（兼容旧 record.weather）；`editor-api.mjs` `applyMediaMetadata` 也保留、`fetchWeather` 改成按 `mediaId` 抓（坐标用记录级、时间用该图 photoTime→记录时间、按 role 定 24h/1点）。③ 新脚本 `scripts/fetch-all-weather.mjs`：走遍所有 record，迁移旧 record.weather→主图、给有坐标+时间的主图抓 24h 天气、主图 showWeather 默认 true，抓完重建。**已跑**：抓到 83 个主图天气（4 个从旧字段迁移），剩 12 个投稿伞因只有日期没钟点跳过（逐个补时再抓）。④ `createRecord` 新建标点时**自动抓主图 24h 天气**（`tryAutoFetchPrimaryWeather`，失败不阻断建卡）。⑤ 本次批量把 91 个 record.json 重新序列化（加天气字段+统一格式，用户原有编辑都保留没动）。**注**：改了 `editor-api.mjs` 记得杀 node 重启。
- **2026-07-02 详情页图片排布推倒重写=固定高宽容器·矮则居中高则顶端滚动·放大动画不再奇怪 v126→v127（Claude Code）**：按用户要求把详情面板排布逻辑重写干净。① 面板从「随内容撑高的 `max-height` 容器」改成**固定高宽的框**（`height:calc(100vh - --focus-top - --focus-bottom)`，`overflow:hidden`）；里面 `.focus-scroll` 做成 flex 纵向滚动区，`.focus-stage` 用 `margin:auto 0`——**内容比框矮=上下居中放，比框高=顶到上面往下内部滚动**（auto 外边距在溢出时自动塌成 0，不会像 justify-content:center 那样裁掉顶部）。**一举修好**问题B（单张横图/短内容不居中→现在居中）和问题C（图片加载后容器变高再重定位的下跳→容器高度恒定不再动）。② **放大动画奇怪(问题D)修复**：灯箱(`.is-expanded`)居中从 `left/top:50% + transform:translate(-50%,-50%)` 改成 **`inset:0 + margin:auto`**，把 transform 让给入场滑动、放大时不再用 900ms 慢慢从停靠位滑到画面中央；放大只做 200ms 快速淡入(`focus-expand-pop`)。③ 窄屏(≤820px)仍保持底部抽屉（`height:auto` 随内容撑、封顶一屏），放大同样改 inset+margin 居中。**已本地(1280×860)验证**：短内容 IMG_6859 上方留白=128px 正好居中、长图 IMG_8755 顶端对齐可滚动、面板高度恒 731px 顶部恒 95px；放大后面板中心 (640,430)=视口正中、transform:none 无滑动；关闭干净回到停靠位。
- **2026-07-02 详情面板改固定顶端(统一顶部留白)+天气联动(编辑器按钮抓取,详情页横轴图) v125→v126（Claude Code）**：① **详情面板从 v125 的垂直居中改成「固定顶端」**：新增 CSS 变量 `--focus-top:clamp(84px,11vh,120px)`，面板 `top:var(--focus-top)`、`transform:translate(28px,0)`（去掉 `-50%`）、`max-height:calc(100vh - var(--focus-top) - clamp(24px,4vh,48px))`。根因=居中方案下「内容越长顶部离导航栏越近」（长横图 8665 差点贴导航栏），现在**所有标点顶部留白一致**（短内容停在这留白下、长内容顶部也在这、往下超出内部滚动）。本地实测 top=86px 稳定清导航栏。② **天气联动（用户 T3，做法A=抓取写进 record）**：新增 `scripts/weather.mjs` 用 **Open-Meteo 免费历史档案 API**（无 key，`timezone=Asia/Tokyo`）按坐标+拍摄时间抓「**拍摄前 24 小时**」逐时天气（25 点，weather_code+气温）。编辑器底栏加**「☁抓取天气」/「✕天气」**按钮 → `/api/fetch-weather`（`editor-api.mjs` 新增 `fetchWeather`：坐标 locationCoordinates→photoCoordinates，时间 time→photoTime，抓完写 `record.weather` 重建；clear 清空）。`weather` 字段已进 `record-utils.mjs` 序列化 + `build-umbrellas.mjs` 透传 + 前端 `...item` 自然带过。详情页 INFORMATION 底部渲染**横轴图**（`renderFocusWeather`）：**只在变化点+头尾**放**极简线条 SVG 图标**（晴/多云/云/雾/雨/雪/雷），右端(now)=拍摄当时、图标略大。**已本地全链路验证**：weather.mjs 对 2024 与数据集 2026 日期都取回 25 点；curl 对 IMG_8591 成功写回重建；浏览器详情页横轴出 3 变化点（-24h 多云 17°→-14h 转雨 14°→now 雨 13°）。**注意**：我为测试给 **IMG_8591/record.json 写了真实天气**（可用「✕天气」清掉或留作示例）；很高竖图滚到最底时横轴文字会贴近底部返回栏，真机再瞄一眼。
- **2026-07-02 详情面板改回垂直居中(短横图不再贴顶)+地图导航文字加自适应阴影 v124→v125（Claude Code）**：① 详情面板从 v124 的「固定顶端」改回**垂直居中**：`top:50%`+`transform:translate(…,-50%)`，`max-height` 收到 `calc(100vh - clamp(96px,12vh,140px))`。短内容（一张横图无段落）现在停在屏幕中间；内容长到超过 max-height 时被卡住、居中即上下等边距（顶端稳定不下跳）；面板本来就在图片加载完前 opacity:0/hidden，所以以最终高度淡入，切换记录不会先低后跳。② 地图页右上角导航（未选中是浅灰 --muted，糊在明亮/杂乱底图或模糊标点上）加**自适应文字阴影**：卫星/深底用半透明**深色** drop-shadow（明底垫深阴影=对比明显，深底与背景融合=几乎看不见，天然像 PS 图层效果，选中/hover 阴影更实一点）；普通路网(is-roadmap，浅底黑字)改用**浅色发光**同理托字。已验证：桌面 1280×800 下短内容面板 centerY=400=视口中心（translateY(-146px)=-50%高度）、max-height=704；导航非选中 tab 已带 text-shadow。（注：本地预览端口非 4173，Google 地图未加载，未能点真实标点，逻辑与计算已核对。）
- **2026-07-02 圆环文字外向+横图详情下跳再修+恢复聚焦hover放大+撤回无效行政细分 v123→v124（codex）**：① 模糊标点圆形文字路径反向，文字改为向外。② 详情面板不再按自身高度垂直居中，改成固定顶端位置；主图加载后等浏览器排版稳定再淡入，减少横向主图造成的下跳。③ 恢复聚焦动画中 marker hover 放大。④ Google 地图行政文字细分按钮实际不可可靠单独控制，撤回到原本有效的「行政文字」整体控制。已验证：`node --check app.js` 通过；浏览器 v124 实测 IMG_8766/IMG_8755 聚焦后面板 top 稳定 72px；地图文字面板只剩「行政文字」；圆环 SVG 路径已反向。
- **2026-07-02 模糊度实时调节+模糊标点圆环文字+地图行政文字细分+横图打开下跳修复 v122→v123（codex）**：① 聚焦模糊度按钮不再只限编辑模式，打开面板即实时预览；打开某个标点后也能直接调当前真实效果。② “模糊标点”文字改成围绕焦点圆心的圆环文字；调节面板新增“文字距离中心”(-600px~600px)和“文字旋转角度”，滑动实时改圆环。③ 地图文字调节新增更细行政层级：国名/都道府县名/市町村名/町名・地区名/街区・地番，仍支持显示/淡化/隐藏和缩放上下限。④ 8665/8755/8766 下跳按根因补强：横向主图打开前先保留稳定比例框，图片加载完成后再显示；聚焦动画中禁用 marker hover 放大，避免点击瞬间图标尺寸切换造成视觉下坠。⑤ 新规则写入：以后每次更新 `交接.md` 必须署名完成任务的智能体。已验证：`node --check app.js`/`server.js` 通过；本地 v123 页面中模糊面板无需编辑模式可打开，圆环距离滑块实时改半径，焦点模式打开 IMG_8766 后面板显示“当前标点实时生效”。
- **2026-07-02 聚焦模糊两套合一(可调+可视化)+标点下跳彻底修(optimized:true)+修改记录绑定编辑栏+三栏可拖拽改大小+档案卡待改红点 v121→v122**：① **T2 模糊效果从底层重做成一套**（之前普通标点=全屏 overlay 有动画，模糊标点=floatPane+复刻标点 瞬时无动画，且复刻标点下的真标点能被 hover 触发放大=bug）。现在**两种标点共用同一个 `.focus-blur` 全屏 overlay**（在地图上方，backdrop-filter 柔化地图+其他标点，径向 mask 留出清晰/白雾圈让选中的标点透出）——删掉 floatPane 层和复刻标点(`FOCUS_PIN_SVG`/`ensureBlurOverlay`/`showBlurApproxOverlay`)。差异只在 CSS 变量：普通=`--fb-*-n`(清晰圈)，模糊=`--fb-*-a`(更大半径+白雾 tint+中心 veil)。**都有 opacity 动画**，切换用 `@property` 注册 `--fb-radius/veil/blur` 让模式互切也平滑；**没有复刻标点=没有 hover 穿透 bug**。② **T1 模糊度可视化调整**：编辑模式左侧加「聚焦模糊度」按钮(`#blur-adjust`)→面板 7 个滑块(普通/模糊各 模糊强度/半径/羽化 + 模糊的中心白雾浓度)，实时写 CSS 变量、存 localStorage `fu-blur-settings`；「预览」按钮直接开 overlay(不进详情页,圈心挪到 58vw 避开面板)让你边拖边看。③ **T3 8665/8755/8766 打开下跳彻底修**：根因=标点 `optimized:false`(独立 DOM)，逐帧 `moveCamera` 动画时 Google 把它的 left/top 重新投影**慢一帧**、和瓦片 transform 不同步→视觉上先掉再归位。改回 **`optimized:true`**(标点画在地图自己的 canvas 上、和瓦片锁帧同步移动=不掉)，配合已有的**按纬度固定 distinct zIndex** 防重叠闪烁。④ **T4 修改记录绑定**：`syncEditHistory` 从「编辑模式就显示」改成「有标点打开(editingId)才显示」，`openEditor`/`closeEditor` 各调一次——预览栏+修改记录栏+编辑栏**一起出现一起消失**(之前关编辑栏后修改记录栏还赖着)。**三栏可拖拽改大小**(像窗口)：左列宽(`--preview-w`)、预览/记录分界(`--preview-split`)、编辑栏宽(`--editor-w`)各一个拖条(`.panel-resizer`)，存 localStorage `fu-panel-sizes`。⑤ **T5 档案卡待改红点**：`renderPhotoCard` 对有 `editFlag` 的记录在卡左上角加红点(`.card-flag-dot`)，和地图标点待改变色一样**只在编辑模式显示**。**已本地验证**：普通/模糊标点都用同一 overlay(普通清晰圈+周围标点柔化、模糊白雾圈更大、都有动画、无复刻标点)、模糊度面板滑块+预览实时生效、修改记录随编辑栏开关一起显隐、三个拖条都能改尺寸并持久化、档案 9 个待改记录显示 9 个红点。**T3 下跳属动画/时序类,本地无头页面 RAF 不跑无法逐帧截,但已从根因(optimized)修复,真机再确认最稳。改了 index.html/optimized 需硬刷新。**
- **2026-07-02 模糊标点改整体柔化(不再丑)+重叠点不闪+标点打开不下跳+筛选加上限阈值+语言只加粗+裁剪预览与详情一致+新增「修改记录」撤回/再编辑功能 v120→v121**：① **模糊地址标点重做**（之前把其他标点换成模糊SVG图标，很丑很违和）：改成模糊层放进地图 `floatPane`（在标点**上方**），一层 backdrop-filter 把地图和所有标点**一起**柔化（和普通聚焦模糊同款和谐观感）；只有选中的点要清晰→在模糊层上再画一个**像素级复刻的清晰蓝标点**（`FOCUS_PIN_SVG`+`.focus-blur-approx-pin`，几何和 `markerIcon` 完全对齐：24×30×1.55、锚点 (18.6,40.3)）。移动/缩放地图时 `is-focus-map-active` 让模糊层(连同复刻标点)淡出→下面真标点全部恢复正常清晰。删了 `blurredMarkerIcon`/`blurApproxFocusId`。② **重叠点(aaa(1)/8680)缩放闪烁 + 标点打开下跳**：根因=`renderMapMarkers` 每次全量销毁重建标点（清空一帧→点会闪/跳），且无显式 z 序（缩放时重叠点前后打架）。改成**原地更新**（只删真正消失的、其余复用+更新图标/坐标/draggable），并给每个标点**按纬度算固定 zIndex**（南边在前，聚焦点最上）——固定数字永不互换=不闪。③ **地图筛选加上限阈值**（用户要第二个阈值）：每类从 1 个「から」(≥此缩放显示)加到 2 个「から/まで」(超过就再隐藏)，`categoryStyleRules` 按 `[min,max]` 窗口外=隐藏，存 `zoomMax`。④ **语言菜单**当前语言只**加粗**、去掉「✓」。⑤ **裁剪预览与详情页不一致修复**：根因=详情封面 frame 用 `aspect-ratio` 定高(2:1盒→560×279)，但 frame 是 grid+place-items:center，带固有比例的 `<img>` **忽略 height:100%**→退回自然高(560×420)，`object-fit:cover` 面对同比例盒**不裁**、frame 只是裁掉顶部→显示区域和裁剪弹窗不同。修法=给定比例裁剪(`.is-cropped:not(.is-crop-free)`)的 img 改 `position:absolute;inset:0` 强制填满定高 frame，cover 就按弹窗一样裁。灯箱同理一并正确。⑥ **新增「修改记录」功能**（用户要）：编辑器**预览栏收到 40vh**、内容字号整体调小(尤其对话/段落)，**下方**新增「修改记录」面板（`.editor-history`，仅编辑模式）：以标点为单位记录最近 50 条**新增/修改/删除**，每条可**撤回**(改=还原到改前;新增=删除它;删除=从回收站恢复)或**点行再编辑**。后端(`editor-api.mjs`)配合：`save-record` 返回改前快照(撤回基线)、`delete-record` 改成**软删除**(移到 `filebox/.trash`+meta，已 gitignore)、新增 `restore-record-data`/`restore-trashed` 两个接口。历史存 localStorage `fu-edit-history`。**已本地验证**：模糊标点整体柔化+移动恢复正常、8665/aaa 原地更新、筛选双阈值渲染、语言只加粗无✓、裁剪封面与弹窗一致(含灯箱)、修改记录 改/删/撤回/再编辑全流程(含软删除移入 .trash 再恢复)均通过。**改了 editor-api.mjs/server 相关需杀 node 重启（已重启验证）。**
- **2026-07-02 一批11项：标点打开不再下跳/模糊只选中清晰其余糊/缩放不闪/裁剪加自由比例/卫星筛选彻底重构(卫星1卫星2各一套+文字全可关)/筛选弹窗单选排+区分卫星12/logo统一纤细/档案选中框白/语言选中标记 v119→v120**：① **T1 标点打开先下跳再归位**：动画起点原来读标点DOM位置(和投影用的视觉中心偏移量不一致→第一帧算错位置)，改成统一用投影 `getLatLngScreenPoint`（删 `getMarkerButtonScreenPoint`），第一帧不再位移。② **T2 模糊地址只选中清晰**：模糊层在标点下导致所有标点都清晰→改成聚焦模糊点时把**其他标点换成模糊图标**(`blurredMarkerIcon` 用 data-URI SVG feGaussianBlur，白描边黑白区都清楚)、选中的仍清晰蓝；hover/rebuild/关闭都正确恢复（关闭时先去 `is-blur-approx` 再 `updateMarkerIcons`）。③ **T3 缩放闪烁**：标点加 `optimized:false`（各自独立DOM，不再共享canvas导致重叠点z序打架）。④ **T4 裁剪加「自由」比例**：新增 custom 模式，右下角把手可拖任意比例（存 `aspect:"custom"`+`ar`，全链路含 editor-api sanitizeCrop 已透传）。⑤⑥⑦ **T5/6/7 卫星地图信息彻底重构**：根因=卫星底图之前按缩放显隐POI且没覆盖全部文字类型→关不干净。现在**卫星统一用 hybrid 底图**，base 一句 `{elementType:"labels",visibility:"off"}` 关掉**所有**文字(不漏)+关道路线，筛选系统再按类叠加显示；**卫星1(文字なし)/卫星2(文字あり)各一套独立筛选**(`mapCategoryState={roadmap,sat1,sat2}`+`activeMapBaseKey`)，卫星1也能开文字了；弹窗每类改成**一排4按钮单选**(自動/表示/淡化/隠す，`.map-layer-seg`)，标题显示在调「衛星①/②」。**已本地真机地图验证：卫星2全隐藏后无任何残留文字。** ⑨ **T9 logo统一**：档案分组折叠钮从 `—`/`▾` 混用改成和「back to map」一样的纤细描边 chevron(SVG stroke-width2)。⑩ **T10 档案列表选中框**红→白。⑪ **T11 语言菜单**当前语言加 ✓ + 加粗。**T5/6/7/T10/T11/T4/T2 已本地(localhost:4173 地图可渲染)验证；T1/T3 为动画/闪烁类，逻辑修复到位，视觉建议真机再确认一次。**
- **2026-07-02 地图筛选分普通/卫星两套 + 打开详情强制回顶显主图 + 地址层级修复(不再乱填子级/unknown可存) v118→v119**：① **地图样式调试面板（T8）拆成两套**：普通地图、卫星地图各自独立一套「自動/表示/淡化/隠す」+缩放阈值（`state.mapCategoryState` 改成 `{roadmap:{…}, satellite:{…}}`，`activeMapCategoryState()` 取当前所在地图那套；改哪张就只影响哪张，另一张不动）；面板顶部加标题显示正在调「普通地図/衛星地図」；localStorage 存两套（旧的扁平格式自动迁移到两套）；两张地图都实时生效（`categoryStyleRules` 用当前那套）。② **打开标点详情页强制回到顶部（显示主图）**：`openFocusMode` 里 `focusScroll.scrollTo(top:0)`，不再沿用上一个详情页滚到的位置。③ **地址层级修复**：a) 选了都道府县**不再自动乱填第一个市/区**（以前选东京会被塞成「千代田区」这种没选过的值→看着像下一级卡死、还会把错的区存进去），现在选完府县只把市下拉打开让你自己选（留空=只存到府县）；b) **「unknown」现在能正常存**（`collectLevelsForSave` 把字面 unknown 当真值：东京+市unknown→存 `["Tokyo","unknown"]`，府县unknown→`["unknown"]`；`hydrateLevels` 重新打开也能还原 unknown）。**均已本地验证**：东京选完市为空/unknown/真实市 三种存值正确；重新载入 `["Tokyo","unknown"]`/`["Tokyo","Nishitama District","Okutama Town"]`/`["unknown"]`/`["Kyoto","Kyoto City","Sakyo Ward"]` 全部无损回填；地图两套独立（改普通的 poiLabels 卫星那套不变）。
- **2026-07-01 修放大后视频无法播放 + 裁剪真正应用到详情页/放大页 v117→v118**：① **修「视频放大后无法播放」bug**：根因=`pauseFocusVideos()` 用 `querySelectorAll("video")` 抓了详情面板里**所有**视频（含灯箱的 `#focus-expanded-video`），把 `controls` 全部关掉；放大视频时先调它→放大出来的视频没有播放控件、点不动。改成只针对内页文章视频 `video.focus-video`（灯箱视频不动），并在 `loadExpandedImage` 视频分支显式 `controls=true` 兜底。② **裁剪真正应用到详情页封面 + 灯箱放大**（之前 v117 只在编辑器预览里能看到）：根因=`normalizeMedia` 把 `crop` 字段**丢了**（前端 `state.umbrellas[].media[].crop` 一直是 undefined），所以公开详情页连内容图裁剪都没生效。补上 `crop:entry.crop||null` 后，内容图裁剪立刻生效；再给**共用的 `#focus-image`**（封面+灯箱同一个 img）加 `applyFocusCrop()`：把 `.focus-image-frame` 变成裁剪比例盒 + 图 `object-fit:cover`+焦点 `object-position`，缩放折进 `--crop-scale`（灯箱平移缩放仍能在其上叠加）；`setExpandedImageFrame` 按裁剪比例算放大框尺寸；切视频/关灯箱都会重置/还原封面裁剪。**已本地验证**：LYUCHEN(1) 封面 3:4 裁剪（object-position 73.96% 0%）、3 张内容图裁剪、灯箱放大裁剪一致、视频放大可播放（currentTime 前进+原生控件）。给编辑器「内容」里每张**图片**加裁剪功能（视频不裁）。**非破坏性**：不改本地文件，只在 record.json 存 `media[].crop={aspect,scale,posX,posY,(ar)}`（null=原图），网站按 crop 显示，可「还原原图」。**裁剪钮**在缩略图**左下角**圆钮（`.editor-media-crop`，已裁高亮），点开**裁剪弹窗**（`openCropModal`）：选比例（原图/1:1/4:3/3:4/16:9/9:16）+拖动平移+缩放滑块+保存/还原/取消。显示用 `cropStyles()`：定比例=`aspect-ratio`盒+`object-fit:cover`+`object-position`+`transform:scale`；原图=保留自然比例只缩放平移（存 `ar`）。全链路透传：`build-umbrellas.mjs`/`record-utils.mjs`(序列化+merge)/`editor-api.mjs`(`sanitizeCrop`)/前端 `photoItem`/`buildPreviewDraft`/`saveEditor`。**已本地验证**：裁剪显示（1:1 缩放对比原图）、弹窗（比例切换/缩放/保存后缩略图更新+钮高亮）、build 重建 92 条无损。**注意**：cover 详情大图和灯箱放大目前仍显示原图（未套 crop，避免和灯箱平移缩放冲突）；视频裁剪未做。
- **2026-07-01 灯箱视频缩略图修复+视频放大不再先错位+关联下划线延到logo+对话加折叠/标签移顶+段落加左竖线 v115→v116**：① 灯箱缩略图栏**视频缩略图**永远用 `<video src#t=0.1>`（之前有无效 thumb 时渲染成坏图 img），加播放三角。② 修**点视频放大先偏下偏左再跳**：视频尺寸要等 `loadedmetadata`(~0.5s) 才知道，之前用旧尺寸先显示→给面板加 `is-media-sizing`(opacity:0) 藏到量好尺寸再显。③ **关联链接**加回下划线，改用 `border-bottom` 让线从链接 logo 一直延到文字末尾。④ 编辑器**对话块**加 ▾/▸ **折叠**按钮（同段落，折叠隐藏各句显「已折叠N句」）。⑤ 对话「对话」标签**移到顶部**（tag+lines 包成 `.editor-dlg-col` 竖排）。⑥ 详情页**段落**左边加白色竖线（文字段落包 `.focus-text-block` 加 `border-left`，和对话一致）。**③⑥已本地截图验证；①②④⑤逻辑直接。#8 图片裁剪见下条。**
- **2026-07-01 视频可放大(灯箱)+放大主图投稿隐藏文件名去00:00+关联链接白色无下划线+层级unknown挪到底+对话单句可上下排序 v114→v115**：① **#9 视频放大**：`getExpandableMedia` 不再排除视频；灯箱新增 `#focus-expanded-video`，点视频右上角放大镜（补充/细节视频都加了放大镜）→进灯箱用 `<video controls>` 按视频尺寸显示、不做平移缩放（`loadExpandedImage`/`setExpandedImageFrame` 按视频 videoWidth/Height 分支；退出/切换暂停并还原图片；缩略图栏视频用 `<video>`+播放三角徽标）。② **图2/#2 放大主图**：投稿伞在灯箱 caption 也隐藏文件名(id)，且去掉多余的「00:00」（`formatMediaCaptionTime` 用正则删尾部 0:00；详情页图注也用）。③ **#5 关联链接**：文字改回**默认白色、无下划线**（只留链接图标），hover 变淡。④ **#3 地址层级 unknown 挪到下拉最下面**（`fillDatalist` 改成 append）。⑤ **#4 对话单句可排序**：每句左侧竖排工具栏（✕删除 + ↑↓ 上下移，`.editor-dlg-line-tools` 在左 gutter），之前只能加到最后。**②③④已本地验证；①灯箱视频已本地截图验证（原生控件+按尺寸显示）；⑤编辑器本地(127.0.0.1)可见，逻辑直接。#8 图片裁剪仍待下一轮专项。**
- **2026-07-01 档案卡有视频时右下角加视频logo v113→v114**：档案图片卡（`renderPhotoCard`）当记录含视频时，右下角 `.card-badges` 里加一个视频描边 logo（`CARD_ICON_VIDEO`，和多图/文本 logo 同风格）。已 DOM 验证（只 LYUCHEN(1) 有视频的卡显示）。**用户确认：#9 视频放大=要加；视频裁剪只在不影响其他功能前提下做——都并入下一轮裁剪专项。**
- **2026-07-01 补充/细节图加放大镜+层级加unknown+编辑模式遵守筛选+待改自拍黑投稿白+凡例日语按序+关联链接logo+视频大播放键静音/暂停/进度条不挡字+对话删除键挪左上+投稿隐藏文件名+视频缩略图去画中画 v112→v113**：① 详情页**补充/细节图**右上角也加放大镜（`.focus-photo-zoom`，插图不加）；② 地址级联**每级加 unknown** 选项（`fillDatalist` 前置，投稿伞层级不全时用）；③ **编辑模式也遵守标点筛选**（删掉 `state.editMode ||` 绕过）；④ **待改**勾选：自拍伞标点变**黑**、投稿伞变**白**（`onFlagToggle` 按 `isContributedItem` 存 black/white）；⑤ About 凡例+地图筛选**日语系统用日语**（`MARKER_LABELS` 改双语 + `markerLabel()`）且顺序改 Fieldwork/Fieldwork·titled/Contributed/Contributed·story；⑥ 详情页**关联标点**去箭头改**水平链接 logo**（`.focus-link-icon`）+ 蓝色提亮 `#2f6fe0→#5aa2ff`；⑦ 视频**中间大播放键**+**默认静音**（`.focus-video-play` 覆盖，点击才 play+显原生控件，用户自行开声）；⑩ 退出详情页/放大其他图时**暂停视频**（`pauseFocusVideos`）；⑬ 视频**字幕挪到顶部**（`.focus-video-cap`）避免和底部进度条重叠；⑪ 编辑器对话**每句删除键挪到该句左上角**圆钮（复用 `.editor-media-del`，只 1 句时不显示）；⑫ 详情页**投稿伞的补充/细节隐藏文件名(id)**（自拍不变，`renderFocusBlockHtml` 传 `isContributed`）；⑧ 编辑器视频缩略图**禁用画中画**+`pointer-events:none`（`disablepictureinpicture`，解决 fig5 画中画提示挡住裁剪/删除钮）。**均已本地验证。⑧的裁剪主功能(可改比例/非破坏性/可还原)+⑨视频放大 待下一轮专门做（改动大、涉及 record schema，单独做更稳）。**
- **2026-07-01 修模糊标点放大后点空白回不去详情+投稿标记改信封+词条列宽自适应(submitted不显示时正文左移) v111→v112**：① **修"模糊地点标点放大图片后点空白回不到详情"**：根因=点空白关闭放大图的处理挂在 `.focus-blur` 上，而模糊地点把 `.focus-blur` 设了 `display:none`（is-blur-approx），放大态没有背景能接收点击→卡住。加规则 `.is-blur-approx.is-image-expanded .focus-blur{display:block}`：放大态重新显示这层当点击背景，和普通标点同一套逻辑（已验证放大态 display:block+pointer-events:auto）。② **投稿标记改信封**（用户觉得小人不能表达"被投稿到我这里"，选了信封 envelope；`.focus-badge` 内嵌信封描边 SVG）。③ **词条列宽自适应**（`.focus-info` 改 `display:grid` 两列 `auto minmax(0,1fr)`、`.focus-info-row` 用 `display:contents`，标签列自动=当前显示的最宽标签宽：自拍伞没 submitted→列窄到 ~43px、正文自动左移；之前固定 84px 害得 time/type 间距很大）。**①已逻辑验证；②③已本地截图验证。**
- **2026-07-01 模糊内圈1px外圈6px不接缝(叠加+羽化)+修主图低位再跳/描边变浅/submitted挪到by前/放大镜提示/投稿改人形logo v110→v111**：① **模糊内外不同程度且无接缝**：v110 单层匀模糊（白区也 6px）→用户想白区更清晰（1px）但仍不要接缝。改成**叠加式**：底层全屏 `blur(1px)`+径向色洗（中心白更白 0.3→边缘黑 0.24），`::after` 再叠 `blur(6px)`、用**渐变蒙版**（0.4cr 透明→边缘实）让额外模糊**渐进**加上去（中心≈1px、边缘≈1+6≈普通6px），渐变蒙版=平滑过渡无硬接缝。可调 `--blur-inner`/`--blur-outer`/`--cr`/白黑 alpha。② **修"模糊标点主图先在低位再跳上去"bug**：根因=`.focus-image-panel.is-loading` 的 opacity 有 900ms 过渡，慢加载的投稿图加载时面板没真正藏住、frame 从占位高撑到全图高（移动端底部锚定→图往上长）。给 is-loading 加 `transition:none` **瞬间藏**，加载完再淡入全尺寸，无回流。③ **浮层描边变浅**（text-shadow 0.9/0.7→0.6/0.45）。④ **submitted 挪到 by 前**（顺序 time→type→object→state→place→submitted→by）。⑤ **主图右上角加放大镜提示**（`#focus-zoom-hint`，和返回箭头同描边风格，点它=点图都放大；灯箱里隐藏）。⑥ **投稿伞标记从"Contributed"胶囊改成小人形 logo**（`.focus-badge` 内嵌描边 SVG 人像，和放大镜/返回箭头统一风格）。**②③④⑤⑥已本地截图验证；① 模糊本地看不到地图需真机看。**
- **2026-07-01 模糊去接缝(单层匀模糊+白弱黑强)+详情页去阴影/竖图不溢出/词条重排加投稿时间/滑动提示+about右栏 v109→v110**：① **模糊接缝修掉**：v109 拆成内外两个不同 blur 的伪元素→有明显接缝。改回**单层匀模糊**（`.focus-blur-approx-layer` 一个 `backdrop-filter:blur(6px)`，和普通标点聚焦 `.focus-blur` 的 6px 完全一致），只叠一层径向**色洗**：中心白雾 0.16（比黑弱）→边缘黑暗角 0.24（≈普通 0.22）。单层 blur 无接缝。可调 `--blur`/`--cr`/白黑 alpha。② **详情页主图去阴影**：A1/A2+C 浮层原来加了深色渐变垫底（用户看成"莫名其妙的阴影"）→**删掉渐变**，改用文字 `text-shadow` 保证白字在亮图上可读。③ **竖图文字溢出图外修掉**：主图原 `max-height:82vh`+`object-fit:contain` 把高竖图压成上下留透明边，底部 A2+C 浮层落到图下方透明带（显示到图外）。改成主图**不限高、按列宽自然高**（frame 永远=图，浮层永远在图上），过高就滚动（配 ⑤ 提示）；frame 加深色底+缩小 min-height 兜底极宽全景的残留留白。④ **information 词条重排+加投稿时间**：顺序固定 time→type→object→state→place→by→**submitted**（兼容自拍/投稿，缺的跳过）；新增 `submitted`（投稿时间 `submissionTime`，英文标签）；标签列宽加到 84px 让"submitted:"不换行、值对齐。⑤ **滑动提示**：详情内容超出可滚动时，「Back to map」右边显示「Scroll for more ⌄」（`.focus-footer` 两端对齐，`updateFocusScrollHint` 按 `#focus-scroll` 溢出/到底切换显隐，箭头轻微上下浮动），到底/灯箱时隐藏。⑥ **About 页**：4 个数字框（地点/フィールド/投稿/记录开始）移到**右栏顶部**，地图凡例放到其**下方**（新 `.about-side` 竖向容器；移动端仍单列堆叠）。**②③④⑤⑥已本地截图验证；① 模糊本地看不到地图需真机看。用户还提到"点标点偶发动画bug"——本地无法复现，待真机给复现场景再查。**
- **2026-07-01 T7移动地图去模糊+内圈半模糊+文字等地图稳了再出+详情页单栏浮层+返回箭头 v108→v109**：① **移动/缩放地图时去掉 pane 模糊**（用户说之前忘了做）：拖动/缩放进入 `is-focus-map-active` 时，新增 CSS 让 `.focus-blur-approx-layer` opacity→0（200ms 过渡），和全屏 `.focus-blur` 一样被抑制，地图交互时清晰。② **内圈白雾模糊减半**：原来整层统一 `blur(4px)`；现在拆成两个伪元素——外圈 `::after` 全模糊 `--blur-outer:6px`+黑暗角（mask 只露圆外），内圈 `::before` 半模糊 `--blur-inner:3px`+白雾（mask 只露圆内），两 mask 在 `0.5cr~0.62cr` 羽化重叠藏接缝。可调两个 blur px + `--cr` + 白/黑 alpha。③ **标点下文字等地图稳了再出现**：聚焦时给 `#focus-approx-label` 加 `is-pending`(opacity 0)，相机动画结束（`animateMarkerToFocus` 的完成回调 `revealApproxLabel`）才去掉、淡入；不再在地图还没移到位时就闪出来。④ **详情页改回单栏**（用户要求）：删两栏 `.focus-cols`/`renderFocusFlows`/`focusBlockLane`，主图下方 D1+D2 按原始 blocks 顺序单流（复用 `renderFocusArticle`）。⑤ **A1+A2+C 浮在主图上**：`renderFocusHeader` 精简成只有 ID(标题)+徽章=**A1 浮左上角**（`.focus-overlay-head`，顶部黑色渐变垫底）；`renderFocusInfo` 合并 A2+C=**浮左下角**（`.focus-overlay-info`，底部黑渐变），同一个 label:值 网格、对齐一致——给 place/time 也加了 `place:`/`time:` 词条（英文，投稿伞再加 `by:`），其后才是 `type:`/`object:`/`state:`。面板收窄成 `min(88vw,560px)`。⑥ **左下角返回箭头重画**（参考用户图）：U 形回转箭头、三角尖朝下、长尾巴（顶部横线长≈整体）、stroke-width 2 同原箭头；移到**左下角**（`align-self:flex-start`）并加英文 **Back to map** 字样。**①②③ 本地无地图渲染（API Key 限域名），模糊/文字时序需真机确认；④⑤⑥ 已本地 DOM+截图验证。**
- **2026-07-01 T7模糊加回黑白径向(白圈+黑边)+标点下文字再远一点 v107→v108**：① **加回黑白模糊**：v107 的 pane 模糊层是均匀米白，丢了原来的「圆内白模糊+圆外黑模糊」径向效果；现在 `.focus-blur-approx-layer` 加 radial-gradient（中心白 0.5→0.38、外圈黑 0.32），并在 `draw()` 里把径向中心 `--cx/--cy` 对准标点（用 `state.blurOverlayCenter`=聚焦点坐标 + `fromLatLngToDivPixel`）。可调：`--cr`=圆大小、白/黑 alpha。② **标点下文字距离** 22→30px（`.focus-approx-label` transform）。**本地地图渲染不稳定，黑白模糊视觉需真机确认。**① **T7 标点彻底从根源解决**：放弃"藏原生标点+画假标点"的亡羊补牢做法（残影、重进时圆和假标点先在别处出现再移动，很假）。改成用 **Google Maps OverlayView 把模糊层注入到 marker 下面的 pane**（`overlayLayer`，在 markerLayer/overlayMouseTarget 之下）——这样地图被模糊、但**真实标点天然画在模糊之上、始终清晰**，不用藏标点、不用假标点。`ensureBlurOverlay`/`showBlurApproxOverlay`/`hideBlurApproxOverlay`，`draw()` 按地图视野定位覆盖、跟随平移缩放（确认过 marker 在 z103 pane、下面的 pane 空）。模糊态把全屏 `.focus-blur` 隐藏，改用这个 pane 层（uniform 4px 模糊+米白 0.34 滤镜，可调 `.focus-blur-approx-layer`）。删掉假标点 HTML/CSS/JS、`blurApproxFocusId`、藏标点逻辑。② **修主图变小 bug**：上一版竖图被 70vh/620px 限高压窄（只 451px、比列窄一圈）；改成 `max-height:82vh`（去掉 px 上限），正常 3:4 竖图能填满整列宽度、恢复到最初大小，仍一屏可见。**T7 模糊整体效果本地看不到地图（API Key 限域名、且本地渲染不稳定），需真机验证；逻辑用的是 Google 官方 OverlayView pane 机制，paint 顺序保证标点在上。**① **修 T7 标点移动地图时消失的 bug**：聚焦模糊态藏了原生标点、用假标点浮在模糊上；但用户拖动/缩放地图时模糊被抑制（`is-focus-map-active`）假标点淡出，原生标点又没还原→什么都没有。改为：模糊被抑制时**重新显示原生标点**（它会跟着地图动）、模糊回来时再藏起来显假标点（`state.blurApproxFocusId`+`setBlurApproxMarkerHidden`，退出 `restoreBlurHiddenMarker` 还原）。② **详情页主图变大**：之前两栏把图挤小了；现在图所在列变宽（面板加宽到 `min(74vw,980px)`），图限高放宽到 `min(70vh,620px)`+contain（竖图仍一屏内）。③ **左右栏互换**：现在**左栏=主图(B)+补充/细节图(D2)**、**右栏=ID/标题/地点/时间(A)+information(C)+段落+插图(D1)**（class 改名 `focus-col-media`/`focus-col-text`，DOM 顺序媒体在前）。④ **右栏对齐右上角导航**：右栏固定宽 400px（≈导航「Umbrella map…Language」簇宽），面板右偏移改成和 topbar 一致 `clamp(18px,4vw,42px)`，右栏右边缘和导航右边缘对齐（已 DOM 验证 1238=1238）。灯箱隐藏改 `.focus-col-text`。**T7 标点/模糊整体仍需真机看。**① **T7 标点浮在模糊之上**：真机发现原生标点其实在模糊层**之下**（被糊了），而且 Google 标点无法抬到全屏模糊浮层之上（除非整张地图不糊）。解决：模糊地址聚焦时**把原生标点 setVisible(false) 藏掉**，在模糊层上方画一个**清晰标点 `#focus-approx-pin`**（蓝色 #1f8bb8 对齐选中态、SVG、尖端对坐标），退出时 setVisible(true) 还原（`state.blurHiddenMarkerId`+`restoreBlurHiddenMarker`）。② **标点下文字拉开距离**：`#focus-approx-label` 距标点 8px→22px。③ **详情页推翻方案 B，按新规重排两栏**（自拍+投稿通用）：**左栏从上到下 A+C+D1**（ID+标题 / 地点 / 时间 → information → 段落+「插图」类图片），**右栏从上到下 B+D2**（主图 → 「补充」+「细节」类图片）。主图仍限高 `min(60vh,540px)`+contain（竖图一眼看全）。两栏在 `.focus-scroll` 里一起滚、关闭钮固定底部；放大灯箱隐藏左栏+右栏附图、网格塌成单列、主图解除限高。④ 实现：`renderFocusArticle` 拆出单块渲染 `renderFocusBlockHtml`+分栏判定 `focusBlockLane`（text/对话/插图→左，补充/细节→右，主图跳过）+`renderFocusFlows` 返回 {left,right}；编辑器实时预览仍单栏按原顺序显示。⑤ **编辑器「内容」加图排序系统暂不动**：原来段落/插图/补充/细节是一个有序流，现在渲染时按角色分到左右两栏（各自保持流内顺序），所以编辑器单一有序列表仍能用——跨栏顺序无意义，左栏内/右栏内顺序仍由 blocks 顺序决定。**T7 标点/文字效果仍需真机看（本地无地图）。**① **T7 模糊标点修正**：删掉 v103 加的额外浮层标点（用户指出地图上出现了两个标点——原生 Google 标点本来就浮在模糊层之上是清晰的，不需要再画一个）；现在只靠原生标点。② **圆形区域改成弱模糊+白滤镜**：原来圆内是完全清晰（mask 中心透明）；现在 mask 中心改成半透明（alpha 0.4，弱模糊），外圈仍全模糊+深色；background 中心白色加强（rgba 白 0.6）当白色滤镜，圆外保持深色。数值都标了可调（搜 `is-blur-approx`）。③ **详情页改方案 B**（自拍+投稿通用）：详情面板加宽到 `min(60vw,760px)`，上半部分两栏=**左信息**（ID/标题/地点/时间 + INFORMATION）+**右主图**；**主图限高 `min(62vh,560px)`+contain**，竖图一眼看全不用滚轮；段落/补充图/对话在下方整宽流动。把 INFORMATION 从 `renderFocusArticle` 拆成 `renderFocusInfo` 放进左栏（编辑器实时预览也补上）；放大灯箱模式隐藏左栏、网格塌成单列、主图解除限高。地址不再限 70% 宽。**T7 视觉仍需真机看（本地无地图）。**① **T7 标点改成浮层**：原来是在模糊层用 mask 抠一个清晰洞让地图自带标点露出来（用户说这不是想要的——洞里那块地图是清晰的）；现在改成**整张地图统一模糊**，另在模糊层之上单独画一个**清晰的标点 SVG**（`#focus-approx-pin`，尖端对准坐标，颜色按 markerCategory 取，默认绿）。删掉 `--pin-clear` 抠洞。② **标点下文字去背景**：`#focus-approx-label` 去掉底色块/圆角/内边距/backdrop 模糊，只留白字+深色文字阴影（保证在模糊地图上可读），距标点尖端约 8px（`translate(-50%, 8px)`）。③ **投稿总览「投稿者(contributor)」列加反转**：原来点投稿者只能恢复默认排序、不能反转；现在和其它列一样——再次点击切换 ↑↓（已 DOM 验证 ↑→↓→↑）。④ 详情页排版优化只做了**预览方案**（A 左图右信息加宽双栏 / B 右图左信息 / C 单栏主图限高），等用户选定再改代码。① 投稿伞 id 显示修正：只有**人名类**（ZHANG_ZHONGPU→ZHANG ZHONGPU、LIU_SIJIA→LIU SIJIA）把下划线换空格；**rednote_ 句柄保留下划线**（rednote_kankan、rednote_621639248）；自拍 IMG_ 不变。（`displayUmbrellaId` 加 `/^rednote/` 例外）② **T7 白模糊细调**：A 白色更白（中心 alpha 0.16→0.34）；B 模糊更弱（backdrop blur 6px→3.5px）；C **标点不再被糊**——在标点处用 mask 开一个小清晰洞 `--pin-clear`(34px)，标点清晰、周围白模糊。③ **T7 新增标点下文字**：record 加 `blurLabel`（全链路透传）；编辑器勾「模糊地址」后多出「标点下文字」框（留空=用显示地址 locationText，placeholder 显示该地址）；聚焦模糊视图时标点正下方显示 `#focus-approx-label`（z-index 在模糊层之上，平移/缩放时淡出）。（含一处用户真机数据 rednote_621639248 的 blurLabel 字段）① 档案「場所」分组的「海外」在日语系统也用英文 **Overseas**。② 投稿伞 id 显示把下划线换成空格（ZHANG_ZHONGPU→**ZHANG ZHONGPU**，详情页标题+档案卡，和总览一致；自拍 IMG_ 不变）。③ **T8 重做成编辑模式专用的调试面板**（`#map-layers` 只在编辑模式出现，不面向用户）：13 个整类，每类一个「自動/表示/淡化/隠す」循环按钮 + 缩放阈值数字框（低于该缩放隐藏）；**普通图+卫星图都生效**（`currentMapStyles()`=底图+`categoryStyleRules()`，`zoom_changed` 时按阈值重算）；选择存 localStorage（仅本机；线上用户没有 localStorage→只看底图默认）。底图默认值（POI/transit 隐藏）已写回 `mapStyles`。④ **T7 白模糊修正**：原本模糊的外圈保持深色模糊不变，原本清晰的圆内改成**轻白模糊**（白色径向渐变→外圈黑模糊，白色不浓），放大半径保留（搜 `is-blur-approx`）。⑤ **退出详情页恢复到点击前的缩放（取整）**，不再硬恢复到 16（`state.preFocusZoom`）；⑥ 顺带修了 approxZoom=当前缩放时退出会抖一下的 bug（缩放相同就跳过回弹动画）。⑦ 编辑器封面图块「封面」标签移到文件名左边（不再压在缩略图下方让行变高）。⑧ 详情页对话：説話者后面统一加冒号（編者：/投稿者：，英文 Editor:/Sender:），正文左边对齐（説話者定宽列+悬挂缩进）。（rednote_621639248 是用户真机设的模糊地址 blurApprox/approxZoom=14，一并入库）① **T8 地图整类开关**：普通地图新增一个展开式按钮（地图层工具栏，仅普通地图显示，卫星图自动隐藏），里面 4 个整类开关——店舗・地点名/駅・バス停/道路名/道路の線，点一下整类显隐，选择记到 localStorage。技术上是给地图 `styles` 追加整类 visibility 规则（`roadmapStyles()`=底图样式+各开关），真透明度做不到（Google 不支持 alpha），按缩放阈值显隐则可做（项目卫星 POI 已用 `zoom_changed` 换样式实现，本次先只做纯开关）。② **T7 模糊地址**（投稿伞专用，**需真机看地图调**）：record 加 `blurApprox`(布尔)+`approxZoom`(该点专属聚焦缩放，留空=18)，全链路透传（record-utils/build/editor-api）；编辑器「投稿信息」里加「模糊地址」勾选+缩放数字框；聚焦该点时用**白色**模糊（区别普通深色）、清晰圈半径+50px、清晰区也留一层轻白模糊、并按 approxZoom 缩放（越小越含糊）。CSS 变量都可调（搜 `is-blur-approx`）。已端到端验证存盘/重建/对话不丢，但白模糊视觉效果本地看不到地图，等真机微调。① 档案 Fieldwork/投稿 的「撮影時間」按月分组标题统一成 `2026/05` 格式（不再随日语显示「2026年5月」，日英系统一致）。② 内容三个「＋加段落/＋对话/＋上传」按钮统一盒子（同字号/内边距/边框），等高对齐、窄屏自动换行。③ 编辑器**封面图块精简**：只显示 封面 标签+文件名+缩略图，去掉标题/时间/右侧功能钮。④ 内容其他图片的删除键改成**圆形浮在缩略图右上角**。⑤ 「细节」等下拉框宽度=缩略图宽（删掉一条把缩略图压成 48px 的旧 CSS，缩略图回到 56px）。⑥ **按角色显隐**编辑框：补充=标题+时间、细节=只标题(时间不显示)、插图=标题/时间/文件名都不显示；详情页 caption 同步（细节不显示时间、插图不显示任何信息）。⑦ **对话编辑系统重做**（原来是一整块双语 textarea）：现在每句一张卡片=「投稿者/編者/旁白」下拉 + 日文框 + 英文框，可逐句增删；「＋添加一句」按上一句自动交替（編者↔投稿者），新对话默认一句「投稿者」。存盘格式不变（仍是带「投稿者：」前缀的 ja/en 多行文本），只在编辑器里解析/回写，详情页渲染不动；能正确解析 ja-only 的「[日期]」旁白行不串行。① 投稿伞 id 中文括号（N）改回英文 (N)（脚本重命名+修 linkedId+重建）。② 欢迎页 UMBRELLA→UMBRELLAS。③ 编辑栏/预览栏宽度改 `clamp()`（宽屏固定 px、窄屏按 vw 收缩，保证露出地图）；新增标点/放到地图都用 `centerInEditorGap()` 把标点落到可见地图区（含 EXIF GPS 情况），不再被编辑栏挡住。④ 显示地址改成自动撑高 textarea。⑤ 档案页 `editor-open` 预留宽度改用宽度变量，不再被挡。⑥ 右上角加本地可见的缩放数字 `#scale-readout`。⑦ 档案：種類→タイプ；投稿统计标题「総覧」；投稿总览箭头改 ↑↓ 同 Fieldwork。⑧ 投稿网格删「投稿时间」排序，改 撮影時間(按月分组)/場所(按都道府县分组，非日本→海外)/統計，与 Fieldwork 同构。⑨ 内容里段落/对话操作钮改竖排（主图仍横排）。⑩ 新增「＋对话」块：编辑器双语 textarea+「对话」标签，详情页斜体+左竖线、編者/投稿者左、正文右；把原来的 9 条原话段落迁成对话块。⑪ 备注框只在勾「待改」后显示。⑫ 删编辑器「投稿者原话」框。⑬ 补充/细节/插图都加 EXIF 默认时间框（空=用 EXIF，filled=覆盖）。⑭ 下拉移到缩略图下方、原位显示文件名。⑮ 新增标点选完照片可弹窗改文件名。⑯ 左侧档案栏加 Fieldwork/投稿 切换；ESC 可关左栏。**T7(模糊地点视觉) 待和用户当面调；T8(改地图文字标注) 评估为难度高/Google 限制多，建议简化。**
- **2026-06-29 投稿伞重命名+原话进内容+四色标记+投稿档案完整化 v96→v97**：① 一次性脚本把 22 条投稿伞 id 重命名（只投稿一次去 _1，多次 _N→（N）中文括号），自动修 2 条 linkedId 并重建。② 把 9 条投稿者原话脚本迁进「内容」第一个文字段落，按语言拆进 ja/en 槽（編者/投稿者→ja，Editor/Sender→en），详情页删掉单独引用块；原话按行渲染成段落（編者/投稿者分行美化）。③ 详情页图片**去掉阴影**。④ 详情页投稿徽章/「约」/「投稿者」一律用**英文**（approx./Contributed/Contributed by）。⑤ 地图标记**四色**（深红=有标题自拍/红=普通自拍/深绿=有故事段落投稿/绿=普通投稿）；地图加一个**展开式筛选**钮（点开后 4 色各自显/隐）；About 页加**四色图例**。⑥ 编辑器：右栏内容上方加「备注 Remarks」框；图片块操作钮改**竖排**在右侧，「补充图片」→「补充」，时间框默认空白，图片显示**文件名**在下拉左边、下拉收窄；新增标点选完照片**弹窗可改文件名**（留空=不改，文件名即 id）；编辑按钮组在打开标点编辑时移到左预览与右编辑栏中间空隙的**顶部横排**。⑦ 投稿档案页做成和 Fieldwork **一样**：照片卡网格 + 投稿/拍摄/地点排序 + 统计总览（列 contributor(同投稿者(N))/submitted/taken/place，默认 contributor 字母序，点表头排序，点 contributor 复位；原 Fieldwork 总览 IMG 表头也可点复位；全英文）。
- **2026-06-29 编辑器修复+Archive切换重做+投稿时间精简 v95→v96**：① **根因修复**：`.archive-content`/`.editor-row` 等的 `display:grid/flex` 覆盖了 `[hidden]`，导致「切换无变化」「title/关联/投稿信息不勾也显示」「勾选框被拉成 100% 宽把文字挤竖排」——补 `[hidden]{display:none}` + `input[type=checkbox/radio]{width:auto}`。② Archive 标题改成「Archive ( Fieldwork / Contributed )」：Archive(/) 是死文本，只有 Fieldwork/Contributed 可点，当前页底部加下划线。③ 「大概」勾选移到地址/时间输入框**右边**，文字精简成「大概」。④ 删编辑器「投稿渠道/日期」框。⑤ 投稿时间全部**只到年月日**（写脚本把 22 条 record.json 的 submissionTime 去掉时分并重建；编辑器存取走 `formatDateOnly`；字段上方加说明）。⑥ 编辑器所有文本框**自动高度**显示全文（`field-sizing:content`），内容段落可 ▾ 折叠。⑦ ESC 改成**先关当前标点编辑（带未保存检测）**，没开编辑卡再按一次才退编辑模式。⑧ About 页加「フィールド数/Fieldwork」方框（=总数−投稿数=71）。
- **2026-06-29 编辑器大改 + Archive标题切换 v94→v95**：① 「Original/オリジナル」标签→「Fieldwork/フィールド」。② 删掉「Archive(contributed)」导航标签；改成 Archive 页大标题旁的切换按钮（own=アーカイブ / contributed=アーカイブ（投稿）），布局/交互与原 Archive 一致，投稿总览排序按钮也用同款 ↑↓ 箭头。③ 编辑器全面重构：抽屉加宽成**两列**（左=各字段、右=内容图文）；字段重排为 来源→类型→标题→关联→坐标→显示地址→地址层级→拍摄时间→投稿信息→伞属性；「投稿信息」仅选「投稿的伞」时显示，「类型/type」仅「自己拍的」时显示（且下拉**隐藏 submission 文件夹**）；「地点/时间只是大概」两个勾选移到对应文本框后面；**删掉只读 ID 框**（改 ID 风险高、收益低）；「关联标点」「标题」改成**右上角勾选**，勾了才在下方出现；伞属性栏精简文字；**所有勾选框统一放到文字右边**（待改/关联/标题/大概/伞属性/状态）；删除按钮只留🗑放到 保存/取消 右边。④ 左侧预览改**实时**刷新（边改边显示）。⑤ 编辑/文案/新增按钮改成**只有 logo 的圆钮**，叠在右下角地球按钮上方；文案+新增仅进入编辑模式后出现。⑥ 新增标点先弹窗选 来源+类型（选投稿则隐藏类型），再选照片。⑦ 按 **ESC 退出编辑模式**；有未保存修改会提示先保存。
- **2026-06-29 投稿伞二期：独立Archive+编辑器重构+拆分 v93→v94**：① schema 加 `submissionTime`(投稿时间,默认读原始照片建立时间)、`remarks`(备注占位)。② 拆分 `rednote_1164552325` 成两把伞(_1金丝町站/_2公园厕所)并 linkedId 互链。③ 编辑器重构：抽屉加宽 380→520px；标记改成头部「待改」单色勾选(去掉旧4色按钮行)；投稿伞加可编辑「投稿时间」字段；数量/颜色/状态整合成带勾选的折叠区(自拍默认勾选展开、投稿默认不勾折叠，不勾=保存时清空属性)。④ 顶部加「Archive(contributed)」标签页=投稿伞独立总览(只排序表,无交叉表,列 IMG/投稿时间/拍摄时间/地点/备注,默认按投稿时间排,可按拍摄时间/地点)；主 Archive 与统计改为只显示自拍。⑤ 详情页投稿伞隐藏 type/object/state 行。⑥ `formatLooseDate` 统一时间格式(斜杠/去00:00/去around)用到详情页与总览。⑦ 「My own」标签→「Original/オリジナル」。总记录 92→93。
- **2026-06-29 视频媒介+绿点+批量导入投稿伞 v92→v93**：① 加视频播放（media 支持 .mp4/.mov/.webm，视频永不当封面、详情页 `<video controls>` 播放、灯箱排除、编辑器可传视频；server.js 补 video/webp MIME）。② 投稿伞公开地图标点改**绿色**（`markerIcon` 按 submissionType）。③ 写一次性导入脚本 `scripts/import-collected.mjs`，从 `filebox/collected` 的 xlsx + iCloud「other people」照片，批量生成 **21 条投稿伞**到 `submission(pending)/`（来源=投稿、署名、对话原话、地点文字、大概坐标+approx；TIFF/BMP 自动转 JPG）。④ `formatDateTime` 解析失败时显示原始文字（投稿粗略日期不丢）。跳过无照片的 Sliden_1/ygrons_1 和无表格行的 IMG_8583。总记录 71→92。
- **2026-06-27 投稿系统(一期) v91→v92**：区分「投稿的伞 vs 自己拍的」。record 新增 6 字段（`submissionType`/`submitter`/`submissionChannel`/`submitterNote`/`locationApprox`/`timeApprox`，旧 71 条默认 own、完全兼容）；3 个脚本透传/消毒。编辑器加「来源」切换 + 投稿专属字段（署名/渠道/原话 + 地点·时间「大概」勾选），「新增标点」旁加来源选择（投稿默认勾大概）。详情页投稿伞显示「投稿」标记 + 投稿者致谢 + 地点/时间「約」前缀 + 投稿者原话引用块；缺字段沿用空则隐藏。统计改两套并列（自分で撮影／投稿，按钮切换，各一整套交叉表+总览），About 填充一直空着的「投稿数」。Google 表单自动同步脚本待后续。已本地预览端到端验证（详情/统计/投稿数）。
- **2026-06-27 迁移到 Mac**：装 Node 24（用户目录 `~/.local`，免管理员）+ 设 git 署名；修 `.claude/launch.json` 的 node 路径（Win→Mac 绝对路径，符号链接会导致预览 spawn 失败）；更新 CLAUDE.md 过期信息（v82→v91、76→71 条、行数）；精简本文件；删「迁移到MAC手册.md」。
- **2026-06-27 清理无用数据**：删 22 个孤儿缩略图 + 本地日志；IMG_8062 补空 `linkedId`。
- **2026-06-27 修复线上插图不显示**：加 `.nojekyll`（`_` 开头文件被 Jekyll 忽略）。
- **第27轮 v88→v91**：详情页文字全强制白色（`.focus-image-panel *`，保留蓝色跳转链接）；About 统计三卡右移。
- **第26轮 v87→v88**：关闭箭头移出滚动区；地址只在逗号换行（≤图 70%）；地图/文字按钮改 logo 移左下；档案按钮移左侧居中；About 改双段文本（texts.json `about`）；统计三卡右移下对齐 + 内容改版（投稿数/记录开始）。已 push。
- **第25轮 v86→v87**：插图 PNG 去阴影；段距再减半；统计颜色两级（单元格具体色/筛选大类）；编辑器恒浅色；地址换行不断词；关闭按钮改右下箭头。
- **第24轮 v85→v86**：插图不显信息；关联标点链接（`linkedId`）；有标题标点深红；档案卡「有文本」logo；档案深色不发灰；地图页固定深色；欢迎页深色反色。
- **第23轮 v84→v85**：详情正文与图同宽；段距改小；统计 object 显具体颜色（`colorDetail`）。
- **第22轮 v83→v84**：导入 Excel 数据（标题16/地点67/故事17，双语）；详情 `\n` 分段+对齐；地图默认 zoom16、minZoom5；档案栏双击跳详情。
- **第21轮 v82→v83**：文本对齐通用规则（日 justify / 英 left）；下拉框空间不够时向上弹；补全英文界面（`data-i18n`）。
- **第20轮 v81→v82**：删除灯箱准星功能（`crosshair` 字段/接口全删）；保留落地页入场准星。
- **第19轮 v80→v81**：准星重做（悬停动画 + 圆环/粗细可调 + 图标按钮）。
- **第18轮 v79→v80**：新增灯箱准星锁定点（per-image `crosshair{x,y}`）。
- **第17轮 v78→v79**：下拉磨砂玻璃；下拉改 `fixed` 防撑表；总览计数 `(x/80)`；卡片悬停暗角。
- **第16轮 v77→v78**：文案編集按钮移位 + 修不消失 bug；object 拼全 umbrella；总览大改（排序/筛选）；修月份重复（`.`→`-`）。已 push。
- **第15轮 v76→v77**：文案集中到 `texts.json`（9 类型 + 统计，补英文）；本地文案編集面板（`/api/save-texts`）。
- **第14轮 v75→v76**：数量 unknown 规则；总览多伞分行；排序/筛选；修 unknown 继承；Archive 独立筛选；卡片悬停反馈。
- **第13轮 v74→v75**：object 改「颜色+种类」合并；总览 ID 去提示；档案图也加小手；渐隐 260→500ms。
- **第12轮 v73→v74**：统计交叉表全英文；合并 color+kind=object；总览改版；按钮渐隐；统计说明文。
- **第11轮补 v72→v73**：放大切换 FLIP 平滑缩放。
- **第11轮 v71→v72**：退出放大滚到对应图；修空白点击返回；类型说明分段；统计页（交叉表+总览）。
- **第10轮 v70→v71**：放大动画优化（opacity/transform）；退出保留当前图；修手动时间；按钮移顶部；类型说明文。
- **第9轮 v69→v70**：修缩略图列偏移；字幕叠右下；字幕 +2pt；档案卡片改版。
- **第8轮 v68→v69**：详情主图铺满栏宽；缩略图列定位；放大切换顺滑；空白滑动切换；放大显字幕；左栏标题同行。
- **第7轮 v62→v68**：双语系统；地图卫星切换 + EXIF 解析；左栏/Archive 改版；图片放大；详情页修白边。
- **第6轮 v61→v62**：界面中文→日语（编辑模式留中文）。
- **第5轮**：详情页 INFORMATION 网格；补充图小字；伞种类优先级；欢迎页字距。
- **第4轮**：object 文本分组；状态并进 `umbrellaUnits`；标记色 yellow/black/white；76 条迁移。
- **第2·3轮**：拖动标点 bug 修复；地址下拉；数量 1 不显 one；罗马音后缀；详情页固定头部+滚动；新增标点 move-record。
- **2026-06-22 文档**：新建 CLAUDE.md / 交接.md；更新 README；装 skills。
- **2026-06-22 接手 + 本地编辑器**：Node24；gitignore choice；Pages workflow；编辑器保存接口；地址级联下拉；`blocks` 图文穿插。
