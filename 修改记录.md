# 修改记录 / Change Log

> 每完成一个任务就在最上面加一条。绝对日期，大白话，尽量简短。

---

## 2026-06-26 · 第二十七轮 · 详情页强制深色 + About统计卡右移（v88→v90）

- **① 详情页强制深色（修正）**：起初只加 `color-scheme: dark` 没生效——因为 `--ink/--muted` 在 `:root` 用 `light-dark()` 声明，浅色系统时在 root 就固化成黑字再继承下来，子元素改 color-scheme 不会重新解析。最终在 `.focus-image-panel` 直接覆写 `--ink:#f4f1ea; --muted:#a7aaa4;`，保证 ID/标题恒为浅色（v90）。
- **② About 统计三卡移至最右**：`.about-view` 加 `justify-content: space-between`，右列贴屏幕右侧。

---

## 2026-06-26 · 第二十六轮 · 关闭箭头下移 + 地址逗号换行 + 地图按钮改logo + 档案按钮居中 + About改双段文本 + 统计卡片改版（v87→v88）

本轮 7 项（+存档上线）：
- **① 关闭箭头移出滚动区**：详情页右下角的「向右长箭头」从浮在滚动内容上，改成放在滚动区**下方**的面板页脚（DOM 顺序：header → scroll → close），不再随正文滚动遮挡。
- **② 长地址按逗号换行**：详情页地址不再按空格断行，改为**只在逗号处换行**（JS 把空格换成不换行空格、逗号后插 `<wbr>`），且宽度**不超过图片的 70%**（`.focus-meta-address{max-width:70%}`）。
- **③ 地图/文字按钮改 logo 移左下**：「地図/卫星」切换和「文字」切换从顶部文字按钮改成**圆形图标按钮**（图层 icon / 文字 icon），竖排放在**左下角**（`.map-layer-tools`/`.map-layer-btn`）。文字按钮开启时高亮。
- **④ 档案按钮移到左边中间**：地图页打开档案列表的按钮（`#toggle-list`）从左上角移到**左侧垂直居中**（沿用 `.panel-toggle-left` 的 left-middle 定位；不再放进原 `.map-top-tools` 顶部簇，该容器已删）。
- **⑤ About 页改双段文本（进文案編集）**：删掉旧的占位文本，改成两段「観察のきっかけ / 記録の作法」（各带日英标题 + 3 段正文），存到 `data/texts.json` 的新 `about` 字段；前端 `renderAbout()` 渲染（日语两边对齐、英语左对齐）；本地「文案編集」面板新增 About 两段的标题/正文编辑框，可改并存回。
- **⑥ 统计卡片移到右侧并下移对齐**：About 右侧三张统计卡顶部下移（`margin-top:43px`），与左侧第一段标题下方对齐。
- **⑦ 统计卡片内容改版**：卡1「記録された地点」(数=71)不变；卡2 旧「プロトタイプ/2026」→「投稿数 Submissions」(数先占位 0)；卡3 旧「アプリとしてインストール可能/PWA」→「記録開始 Recording since / 2025.03」。（投稿数是未来"别人投稿的伞"独立系统的占位，后续会做。）
- 涉及文件：`app.js`、`styles.css`、`index.html`、`data/texts.json`、`scripts/editor-api.mjs`（save-texts 存 about）。版本号 v87→v88。
- 验证：①②④⑤⑥⑦ 已本地验证（About 双段/对齐/卡片、文案編集 about 字段、按钮位置、关闭箭头在滚动区下、地址逗号换行+70%、save-texts 往返）。③⑥ 的真实地图视觉仍需你在自己浏览器看。
- **本轮按用户要求 commit 并 push 上线**（含 v86/v87 之前只 commit 未 push 的内容）。

---

## 2026-06-26 · 第二十五轮 · 插图去阴影 + 段距再减半 + 统计颜色粒度 + 编辑器恒浅色 + 地址换行 + 关闭按钮改右箭头（v86→v87）

本轮 7 项：
- **① 插图透明 PNG 去掉阴影**：详情页里「插图」图片是透明 PNG 时，原来继承的方形投影会在透明边缘画出一圈奇怪的阴影。给插图图片加 `is-illustration` 类、`box-shadow:none`，阴影消失。
- **② 段落间距再减半**：详情页正文 `.focus-caption-inner` 的 grid gap `8px→4px`，段落 `.item-story` 下边距 `0.25em→0.12em`（总段距约 12px→6px）。
- **③④ 统计「颜色粒度」分两级**：colored/patterned/other 这三类是「大类」（选完还能填具体颜色）。
  - **总览表格单元格**：显示**具体颜色**，如 `black long umbrella`、`blue folding umbrella`、`dark red long umbrella`（`statsObjectValue`）。
  - **总览的 object 下拉筛选** + **上方纵横交叉表**：显示**大类**，如 `colored long umbrella`（新函数 `statsObjectCategory`）。选「colored long umbrella」会筛出底下所有 black/blue/… 的行。每行多带一个 `objectCategory` 字段供筛选用。
- **⑤ 编辑器永远浅色模式**：给 `.editor-drawer`/`.editor-preview`/`.editor-toolbar` 等加 `color-scheme:light`，深色系统下编辑面板（含实时预览里的正文）也强制浅色、文字不再白底白字。
- **⑥ 详情页长地址换行不断词**：`.focus-header .focus-meta` 加 `white-space:normal; overflow-wrap:normal; word-break:keep-all`，地址超长时按空格折到多行，不在单词中间断开。
- **⑦ 详情页关闭按钮改右箭头**：删掉右上角突兀的「×」圆钮，改成**右下角一条向右的长箭头**（表示「向右收起」），`id` 仍是 `focus-close`，点击照旧关闭；放大看图时自动隐藏（沿用旧规则）。
- 涉及文件：`app.js`、`styles.css`、`index.html`。版本号 v86→v87。
- 验证：①③④⑤已本地验证（统计粒度/筛选/编辑器浅色/插图无阴影都正确）；②⑥⑦及插图真实透明效果需用户在自己浏览器（地图能渲染、能进详情页）核对。

---

## 2026-06-26 · 第二十四轮 · 插图无信息 + 关联标点链接 + 标题标点深红 + 文本logo + 深浅色模式（v85→v86）

本轮 7 项：
- **① 详情页插图不显示信息**：设为「插图(illustration)」的图片，在详情页正文里不再显示右下角那行「标题/ID/时间」说明。
- **② 关联标点链接（新功能）**：编辑模式新增「关联标点 Linked point」下拉框，可选另一个标点。详情页**主图右下角**会显示一个蓝色带下划线的链接（在图下方、不浮在图上），点击跳转到关联标点的详情页；图片放大时不显示。数据存在 record.json 的新字段 `linkedId`（空=无）。
- **③ 有标题的标点用更深的红色**：地图上凡是填了标题的标点，pin 颜色从 `#c54f35` 加深为 `#8f2310`（没标题的保持原色；选中仍是蓝绿色）。
- **④ 档案卡「有文本」logo**：卡片右上角原有「多图」「插图」两个 logo，新增第三个「有文本」logo（三横线，风格一致），有正文段落的卡片会显示，三个并排。
- **⑤ 档案页深色模式不再发灰**：`.archive-group` 面板背景在深色下从「半透明白」(发灰)改成极淡的近黑色调 `rgba(255,255,255,0.045)`。
- **⑥ 地图页固定用深色样式**：`#map-view` 强制 `color-scheme:dark`，不跟随系统浅/深；**例外**：左边栏 + 左上角工具 + 右下角按钮仍跟随系统（`color-scheme:light dark`）；顶部右上角导航（视图标签栏）在地图页用深色，切到「普通地图(roadmap)」时变黑色（靠 body 上的 `view-map`/`is-roadmap` 类控制）。
- **⑦ 深色模式欢迎页图片反色**：深色系统下 `.welcome-card` 加 `filter:invert(1)`。
- 涉及文件：`app.js`、`styles.css`、`index.html`、`scripts/editor-api.mjs`（保存 linkedId）、`scripts/record-utils.mjs`（序列化 linkedId）、`scripts/build-umbrellas.mjs`（输出 linkedId）。版本号 v85→v86。
- 备注：本地 127.0.0.1 地图不渲染，③⑥及详情页链接的真实效果需用户在自己浏览器看；④⑤⑦及编辑器下拉、保存链路已本地验证。

---

## 2026-06-26 · 第二十三轮 · 详情页正文宽度/段距微调 + 统计 object 显示具体颜色（v84→v85）

- **详情页正文与图片同宽**：去掉正文 `.item-story` 的 `max-width:56ch`，正文现在和上方图片一样宽，日语两边对齐就能对齐到图片右边缘（之前右侧空一块）。
- **段落间距改小**：段间距从 `0.85em` 降到 `0.25em`（加上容器本身 8px 的 grid gap，总间距约从 ~20px 降到 ~11px）。还想更紧可继续调这个值或 `.focus-caption-inner` 的 gap。
- **统计总览 object 显示具体颜色**：`statsObjectValue` 对 colored/patterned/other 改用 `colorDetail`（如 black/blue/floral），不再只显示「colored」。没填具体颜色的（如两条 patterned 空 detail）回退显示分类词「patterned」。交叉表 object 维度同步生效。
- 版本号 v84→v85。

---

## 2026-06-26 · 第二十二轮 · 导入文档数据 + 详情页换行/对齐 + 地图缩放 + 档案栏双击（v83→v84）

- **从 Excel 导入数据**（用户的 `forgotten_umbrellas_extracted_data.xlsx`，70 行）：把「标题/地点(Found At)/故事」填入对应记录详情页。
  - 标题 `初回記録/First record` 形式 → 拆成双语 `title:{ja,en}`；地点(Found At) → 填入 `locationText`（不动地址层级）；故事 `日文 / 英文` → 拆双语放进 `blocks` 文字段落。
  - 共填：标题 16 条、地点 67 条、故事 17 条。已有内容（IMG_5917/5956/8622）保留不覆盖。
  - 特殊情况：「See IMG_XXXX」的故事自动引用目标记录；多图记录（如 IMG_8622）故事取自成员照片且保留原有照片块顺序。
  - 4 条记录文档无数据，未填：IMG_8591 / IMG_6383 / IMG_8251 / IMG_6720。文档里 IMG_8755 / IMG_8766 系统里没有对应记录，跳过。
- **详情页换行**：正文 `\n` 现在正确分段（每段一个 `<p>`，之前挤成一行）。同时套对齐规则：日语两边对齐、英语左对齐。
- **地图默认比例尺拉近 2 级**（14→16）。
- **限制地图无限拉远**：加最小缩放 `minZoom=5`（约「整个日本」尺度）。
- **左边档案栏双击图片**：双击列表项跳到该点并打开详情页（单击仍只选中/平移）。用手写双击判定（350ms 内同一项），因单击会重渲染列表、原生 dblclick 不可靠。
- 版本号 v83→v84。

---

## 2026-06-24 · 第二十一轮 · 文本对齐规则 + 下拉框修复 + 英文界面补全（v82→v83）

- **统计页说明文对齐**：日语两边对齐(justify)，英语改成左对齐（英语两边对齐会有难看的大空隙）。这条作为**通用规则**：以后任何大段文本（含手动加的详情页故事）都按"日语 justify / 英语 left"。
- **修总览下拉框遮挡**：表格很短时（如筛选后只剩两行），页面不能再往下滚，长下拉框会超出屏幕底部点不到。改成：下方空间不够时下拉框**自动向上弹**，并按可用空间限高、内部可滚动，保证所有选项都点得到。
- **补全英文界面**（切英文时）：
  - 档案页：「並び替え→Sort by」「時間/種類/場所/統計→Time/Type/Place/Stats」。
  - 地图左边栏排序按钮：時間/種類/場所→Time/Type/Place。
  - 地图/卫星/文字按钮：地図→Map、衛星→Satellite、文字オン/オフ→Labels on/off（含悬停提示）。
  - About 页残留日语：記録された地点→Recorded locations、プロトタイプ→Prototype、アプリとしてインストール可能→Installable as an app。
  - 做法：静态文案统一用 `data-i18n` 标记 + `I18N` 字典，切语言时批量替换；地图按钮走 `UI_TEXT` 双语。
- 版本号 v82→v83。

---

## 2026-06-24 · 第二十轮 · 删除灯箱准星功能（v81→v82）

- **彻底删除"灯箱准星锁定点"功能**（第十八~十九轮做的、一直无法实现的那个）。用户决定不要了。
- 删掉：灯箱放大图上的准星显示（`focus-crosshair`）、本地编辑器每张图的圆形取点按钮、取点弹窗、`/api/save-crosshair` 接口、`crosshair` 数据字段（record.json/umbrellas.json 已重建去掉）。
- **保留**：进入网站前落地页那个跟着鼠标移动的入场准星（`welcome-crosshair`），那是另一个一直正常的视觉效果，没动。
- 顺手清理：相关 CSS、未用的 `clamp01`/`CROSSHAIR_DEFAULTS` 常量、后端 `sanitizeCrosshair` 都删了；record.json 序列化不再写 crosshair 字段。
- 71 条记录跑了 `records:format` + `records:build`，数据正常。版本号 v81→v82。

---

## 2026-06-24 · 第十九轮 · 准星重做：悬停动画+可调+图标按钮（v80→v81）

- **修复"准星完全不显示"**：根因是 `normalizeMedia`（app.js）重建 media 时漏掉了 `crosshair` 字段，导致前端永远读不到。已补上。
- **改成悬停驱动动画**：默认不显示十字线/准星；鼠标从图片边缘进入灯箱图→准星从鼠标进入点滑动到锁定点；鼠标离开→滑向离开点并淡出。（位置用 CSS 过渡，淡入淡出用 opacity。）
- **圆环大小 + 线条粗细可调**：取点弹窗加了两个滑块，per-image 存进 `crosshair:{x,y,ring,thickness}`；后端 `sanitizeCrosshair` 接受并夹取 ring(6~80)/thickness(0.5~10)。
- **编辑器按钮改图标**：原来的「✛准星·已设」文字按钮 → 圆形十字 logo（SVG），无文字；设了准星后变橙色描边（`.flow-crosshair-btn.is-set`）。
- 不支持移动端（按需求）。

---

## 2026-06-24 · 第十八轮 · 灯箱图片准星锁定点（v79→v80）

- **新功能：灯箱准星**。放大后的灯箱图上可显示一个"锁定准星"（横线+竖线+圆环，`mix-blend-mode:difference` 在任何照片上都看得清，打开时圆环收缩做锁定动画）。
- **每图设点**：在本地编辑器里，每张图片多了「✛准星」按钮，点开一个取点弹窗→在图上点一下设锁定点→保存（再点别处可改，"清除"按钮可去掉）。已设的图按钮显示「✛准星·已设」。
- 数据：媒体新增 `crosshair:{x,y}`（归一化 0~1）字段；后端新增 `/api/save-crosshair` 只改单张图、其它不动；`record-utils`、`build-umbrellas` 都已带上该字段。
- 只在 127.0.0.1 能设点；线上只读显示。没设点的图灯箱不显示准星（保持原样）。

---

## 2026-06-24 · 第十七轮 · 总览下拉/卡片悬停微调（v78→v79）

- **下拉栏看不清**：object/state 下拉框改成磨砂玻璃（半透明 + 背景模糊），后面表格文字不再透出重叠。
- **下拉撑大表格的 bug**：下拉框改成 `position:fixed` 悬浮在表格之上（JS 定位到按钮下方，带右边界夹取），不再撑高滚动容器、不再出现右侧滑块和左右缩放抖动。
- **总览计数**：筛选时标题数字改成 `(x/80)`，未筛选时仍是 `(80)`。
- **图片卡悬停**：去掉 ID 变灰；改成图片淡入一个微弱暗角（vignette），提示更明显但不动下方文字。

---

## 2026-06-24 · 第十六轮 · 批量微调 + 上线（v77→v78）

- **文案編集按钮**移到「编辑模式」右边，同尺寸同样式（共用一个工具条容器）。
- **修复**：文案编辑窗口按保存/取消/关闭后不消失的 bug（`display:flex` 盖过了 `[hidden]`，补 `.texts-editor-overlay[hidden]{display:none}`）。
- **object 文本**：交叉表/总览统一拼全 "umbrella"（如 `unknown long umbrella`、`transparent long umbrella`），不再只显示 long。
- **总览大改**：① IMG 去掉点击排序；② type、③ area 改成点击排序（首字母升降序，和 date 一样）；④ object/state 改成点击表头文字弹下拉筛选（默认显示列名=全部）；⑤ object 用分类（transparent/colored/patterned/translucent/unknown + 种类），不细分到具体颜色；⑥ state 下拉是单状态选项（选 fastened 会筛出"fastened"和"fastened, worn"）；⑦ `in umbrella sleeve` 归入 `other`（交叉表和总览都生效）。
- **修复月份重复**：有条记录 time 用了点号（`2026.05.02`），导致交叉表出现 `2026.05` 和 `2026-05` 两格；月份键统一成 `-`。
- 已 push 上线。

---

## 2026-06-24 · 第十五轮 · item12 文案 JSON + 文案编辑模式（v76→v77）

- **文案集中到 `data/texts.json`**：9 个类型说明文（ja/en）+ 统计页说明文（ja/en，**新补了英文**）。前端启动时 fetch 读取，旧的 `TYPE_DESCRIPTIONS`/`STATS_INTRO` 常量已删除。
- **本地「文案編集」面板**：仅 127.0.0.1 出现（左上「文 文案編集」按钮）。可改类型说明 + 统计说明的日/英文；多段落用空行分隔。**只能改现有文案，不能加片段**（按你的要求）。保存走新接口 `/api/save-texts` 写回 `data/texts.json`，线上也会更新。
- 后端 `scripts/editor-api.mjs` 新增 `saveTexts`；`sw.js` 缓存清单加入 `data/texts.json`。
- 注：改了 `editor-api.mjs` 已重启 node 预览服务。

---

## 2026-06-24 · 第十四轮 · item5/6 + 3 个问题修复（v75→v76）

- **item5 数量=unknown 规则**：详情页 object/state 显示 "unknown"（原来空白）；统计交叉表**排除**数量unknown的记录（不知道几把）；数量为具体数字但颜色/种类未知的仍计入（显示 unknown…）。
- **item6a 总览多伞分行**：一张多伞的图每把伞各占一行，ID 写成 `IMG_8056(1)/(2)…`；数量unknown 的仍只占一行。
- **item6b 总览排序/筛选**：IMG、date 表头点击排序（再点切升降序）；type/area/object/state 各加下拉筛选。
- **问题1 unknown unknown 排查**：原因是多伞记录第2把留空，统计没套用「空白伞=同第1把」的继承规则。已修复（统计与详情一致）。剩下的 `unknown long` 是 IMG_8056、IMG_8682 你确实选了 unknown 颜色，属真实数据。
- **问题2 修复**：地图左侧栏搜索不再连带过滤 Archive 页（Archive 改为始终用全量数据，自己的筛选独立）。
- **问题3**：图片卡加悬停反馈（图片柔和阴影+轻微变暗、ID 浅灰底），提示可点。

---

## 2026-06-24 · 第十三轮 · 微调上轮 4 点（v74→v75）

- **object 维度**改成「颜色+种类」合并（如 `transparent long`/`transparent folding`），长伞与折叠伞分开统计（之前只有折叠伞才显 folding，否则只看颜色）。
- **总览 ID** 去掉悬浮提示「ダブルクリックで地図へ」，双击跳转和悬停小手保留。
- **アーカイブ卡片**：ID 上方的图片也加悬停小手（之前只有 ID 有）。
- **放大顶部按钮渐隐** 从 260ms 改 500ms，更柔和。

---

## 2026-06-24 · 第十二轮 · 统计页英文化/object合并/总览改版/按钮渐隐（v73→v74）

- **统计交叉表全英文**：纵横轴文字(rows/columns)和下拉选项都用英文，不随系统语言变。
- **合并 color+kind 为 object**：交叉表维度改为 type/object/state/month/place；object=折叠伞显示 folding，否则显示颜色。默认横轴 type、纵轴 object。
- **总览表改版**：默认按 IMG 名排序；列顺序改为 IMG/date/type/area/object/state；IMG 单元格双击跳转地图详情、悬停变小手。
- **アーカイブ图片卡 ID** 悬停也变小手（卡片本来就双击跳转）。
- **放大时顶部按钮渐隐**：左上/右上按钮组改为 260ms 渐变模糊+淡出（原来瞬间消失）。
- **统计页加说明文**（item11，日语，暂硬编码；后续随 item12 移入可编辑 JSON）。
- 本轮**未做**（已交接）：item5 数量unknown的object/state规则与统计计数、item6 总览多伞分行+筛选排序、item12 文字内容JSON+编辑模式。详见 交接.md。

## 2026-06-24 · 第十一轮补 · 放大切换 FLIP 平滑缩放（v72→v73）

- 切换不同尺寸照片时，用 FLIP（先量旧尺寸→设新尺寸→transform:scale 反推回旧尺寸→240ms 动画到 1）让放大框平滑"长/缩"到新尺寸；只动 transform（GPU 合成）不重排，所以平滑且不卡。缩略图列加 240ms left 过渡同步滑动。仅切换时触发，首次放大/窗口缩放不触发；关闭时清掉残留 transform。

## 2026-06-24 · 第十一轮 · 退出滚动到图/缩略图列点击/类型说明排版/統計页（v71→v72）

- **退出放大滚动到对应图**：上一轮改成"换主图"是误解；改回保留封面主图，退出放大时把详情页**滚动到你刚看的那张图**在文章里的位置（瞬间定位，避免被封面图重排打断）。
- **修放大空白处点击返回**：缩略图列容器原来挡住点击，列旁/缝隙点不动。把列容器设为穿透(pointer-events:none)，只有缩略图本身接收点击，其余空白都能点击返回。
- **类型说明排版**：按文档原换行拆成多段（每段一个 <p>）；日语段落两端对齐(justify)，英语不对齐。日语去掉了原文档软换行带进来的多余空格。
- **統計页**：アーカイブ加「統計」按钮（場所后）。统计页不显示图片卡，显示两张表：① 可交互交叉表——下拉选纵轴/横轴维度(タイプ/色/種別/状態/月/場所)，单元格统计伞数量，最下/最右有合计；② 总览表——每条记录一行(IMG/area/date/type/object/state)。

## 2026-06-23 · 第十轮 · 放大动画/返回当前图/手动时间/顶部按钮/类型说明（v70→v71）

- **优化放大动画卡顿**：放大面板原来过渡 width/height、缩略图列过渡 left，这些都是每帧触发重排的属性 → 卡顿。改成只过渡 opacity/transform（GPU 合成），尺寸变化瞬间生效，切换更干脆不卡。
- **退出放大保留当前图**：以前从放大退回详情页总是回到主图；现在停在你正在看的那张（记录 detailImageIndex；再点主图也从这张放大）。
- **修手动时间不显示**：图片本身无 EXIF 时间但手填了时间时，放大/详情右下角字幕原来显示空；现封面图回退用记录的手动时间（mediaDisplayTime）。
- **档案/地图/文字按钮移到顶部**：三个按钮收进左上角 `.map-top-tools`（z50，在详情遮罩之上）。详情页打开时顶部导航(标签+语言)和这三个按钮都可见可点；图片放大时（body.is-image-expanded）全部隐藏+禁用。点顶部标签切换视图会先关掉详情页。
- **类型说明文**：アーカイブ「種類」里选中某个类型时，在照片上方显示该类型的日/英说明文（来自用户文档，9 个类型，随语言切换）；选 all 不显示。

## 2026-06-23 · 第九轮 · 放大图缩略图列bug + 字幕叠放 + 档案卡片改版（v69→v70）

- **修缩略图列偏移 bug**：放大后切换不同宽度的照片时，图片宽度有 260ms 过渡动画，缩略图列原来按动画中途的位置定位，导致列偏移、不贴在图右边。改成按「图片最终宽度」(viewport中心+半宽) 计算，并给列加同样 260ms 过渡，跟着图一起滑到位。
- **详情页补充/细节图字幕叠放**：原来字幕在图下方，现改为叠放在图片右下角，和放大图字幕同款（白字+阴影）。
- **字幕字号 +2pt**：叠放字幕统一用 `calc(11px + 2pt)`。
- **档案卡片改版**：① 圆角边框只包住图片，ID 移到图片下方、左对齐、无边框背景；② 多图/插图标志移到图片右下角；编辑按钮挪到右上角避让。

## 2026-06-23 · 第八轮 · 详情页/放大图/左栏微调（v68→v69）

- **详情页主图对齐**：竖图原来被装进更宽的框里居中（左右各空约 53px），导致 ID 和正文比照片宽。改成封面图始终铺满栏宽（去掉高度上限的留白），文字自然与照片左右对齐。
- **放大图缩略图列**：从屏幕最右边改到「放大图右侧 15px」处（JS 按图片实际右边缘定位，自动随图宽变化）。
- **放大图切换更顺**：进入放大时预加载所有可放大图；点缩略图切换时只挪高亮、不再整列重建，去掉卡顿/闪烁。
- **空白处滑动切换**：放大状态下在图外空白处左右拖动可上一张/下一张（左滑下一张，右滑上一张，循环）；普通点击空白仍是关闭。
- **放大图显示文字**：放大图右下角加「标题, ID, 时间」小字幕（白字+阴影，照片上看得清），样式同详情页每张图的右下角小字；封面放大也显示。
- **左栏标题同行**：修 `.location-button span{display:block}` 优先级压过 idrow 的 flex 导致标题换行；标题现与 ID 同一行。

## 2026-06-23 · 第七轮（v62→v68）

- **双语系统**：日本語/English 真切换（记忆选择）；编辑器 title 和段落改「日文框+英文框」；切换同步 About、Archive 标题、详情页 title/段落；旧单语数据自动当日文，安全。`title`/`blocks[].text` 升级为 `{ja,en}`，兼容旧字符串。
- **地图**：兜底定位改东京、仅日本境内才跳当前；首屏缩放动画去闪烁；普通↔卫星主切换 + 卫星下「文字オン/オフ」；隐藏卫星道路白线；POI 放大(zoom≥17)才显示并淡化。
- **EXIF**：新增 `scripts/exif.mjs`（无依赖解析 GPS+时间）。新增/上传图片自动读 EXIF 时间/定位；`backfill-exif.mjs` 回填了旧图的 photoTime。
- **左栏/Archive**：删「デフォルト」默认按时间；ID 旁显示 title；小图地址/时间两行+加宽；档案图标按钮；编辑/新增按钮移左上；Archive 编辑挤压页面不挡卡片。
- **图片放大**：补充/细节可放大(插图不可)；放大后隐藏文字+右侧竖缩略图切换；放大/再点标点回清晰区；修放大抖动。
- **详情页**：修白边（去图片白底）+ 缩小图片与标题/文字间距；适中 min-height 防加载跳动。
- **卡片/排序**：时间排序整合进按钮(↑↓)；Archive 卡片只剩图+ID+多图/插图 logo；卡片 ✎ 就地编辑、双击跳地图；编辑器「放到地图上」按钮。

## 2026-06-22 · 第六轮 · 界面日语化（v61→v62）

- 公开界面中文→日语（排序按钮、About、标题、报错、日期 zh-CN→ja-JP）；语言菜单删「中文」；编辑模式保持中文（本地专用）。用户录入内容未动。

## 2026-06-22 · 第五轮 · 详情页排版

- INFORMATION 改「标签左、值左对齐」网格；补充图下方小字 `(title,) ID, time`；伞种类长柄优先+unknown；欢迎页标题字距收窄。

## 2026-06-22 · 第四轮 · 数据结构

- 修 object 文本（按描述分组、2+ 才加数字）；状态并进 `umbrellaUnits` 每把伞、随数量增；新增标记色 yellow/black/white（仅编辑模式显示）。76 条迁移。

## 2026-06-22 · 第二、三轮 · 编辑器与修复

- 拖动标点 bug 真因修复（dragend 后 500ms 忽略 trailing click）；地址下拉聚焦清空；数量=1 不显示 "one"；地址英文罗马音后缀；详情页固定头部+整面滚动；新增标点带分类、move-record。

## 2026-06-22 · 文档

- 新建 `CLAUDE.md`、`交接.md`；更新 `README.md`；安装 mattpocock/skills。

## 2026-06-22 · 接手项目 + 本地编辑器

- 装 Node 24、`.gitignore` 忽略 `filebox/choice/`、GitHub Pages workflow。
- 本地编辑器：`server.js`+`scripts/editor-api.mjs` 保存接口（仅本机）；图片增删/主图/排序/角色；新增删除标点；结构化字段（数量/颜色种类/状态+智能默认）。
- 地址级联下拉（`data/japan-areas.json` 全日本，日英双语，输入筛选）。
- 故事多段落+图文穿插，详情页改可滚动文章流，新增 `blocks` 字段。
